# Playhead — Claude Code Instructions

## Decision Authority

**Never swap frameworks, APIs, or architectural approaches without explicit approval.** Present the options and tradeoffs, then wait for a decision. This applies to:
- Switching between Apple framework APIs (e.g. SpeechAnalyzer vs SFSpeechRecognizer)
- Adding or removing dependencies
- Changing persistence strategies
- Altering the service/actor architecture

When investigation reveals a framework is broken, present findings and proposed alternatives — don't implement the swap.

## Issue Tracking

**Use `bd` (beads, Homebrew `beads` formula) for ALL issue tracking in this repo.** Canonical data lives in `.beads/dolt/`.

**Do NOT use `br` (beads_rust, cargo `beads_rust`) even if you find it installed.** It is a separate reimplementation with its own database and issue prefix (`bd-*` vs bd's `playhead-*`); using it creates a parallel ghost tracker whose IDs never resolve in the real `bd` database. If you find `br` installed, leave it alone and use `bd`. If any command or skill suggests `br`, substitute `bd`.

## Testing

### Lint first (`scripts/lint.sh`) — playhead-ia2s

**Run this before the test gate. It takes ~0.2s warm / ~2.4s cold and needs no build.** SwiftLint was installed on this box all along but the repo had no `.swiftlint.yml`, so for months "lint" was a no-op step in every bead gate and every review round — style, dead-code and complexity regressions landed unreviewed, and human reviewers burned rounds on things a linter catches mechanically.

```bash
scripts/lint.sh                    # whole repo, strict — the gate
scripts/lint.sh --changed          # only Swift files changed vs the merge-base
scripts/lint.sh --lenient          # report violations but exit 0
scripts/lint.sh --fix <paths>      # autocorrect; EXPLICIT PATHS ONLY, refuses repo-wide
```

`scripts/fast-gate.sh` runs it automatically before building, so a violation stops you in 2 seconds instead of after a 3-minute test run. Bypass with `PLAYHEAD_SKIP_LINT=1`.

**Warnings vs errors — the policy.** Every rule in `.swiftlint.yml` is `warning` severity, and `scripts/lint.sh` adds `--strict` to turn warnings into a non-zero exit. The split is deliberate:

- Bare `swiftlint lint` — what Xcode and editor integrations run — exits 0 and paints yellow. **Lint never blocks an unrelated build.**
- `scripts/lint.sh` is the gate and is zero-tolerance. It can afford to be: the baseline is **0 violations, measured**, so any failure is in your own diff.

A rule is promoted `warning` → `error` (so it bites even without `--strict`, i.e. inside Xcode) only when it has held at zero for a full release cycle **and** its violations are always genuine defects, never taste. Style rules are never promoted — a style rule that hard-errors in the IDE is how linters get uninstalled.

**The baseline is green and must stay green.** `.swiftlint.yml` was built by measurement, not taste: every candidate rule was run against the whole tree and admitted only at a violation count of exactly zero. 84 rules are enabled; nothing was reformatted and `swiftlint --fix` was never run repo-wide. Rules that fire today are **not** disabled quietly — they are listed at the bottom of `.swiftlint.yml` with their measured counts, tiered as a follow-up roadmap (Tier D is the high-value shortlist: `variable_shadowing` 144, `unused_parameter` 401, `async_without_await` 396, `force_unwrapping` 389, and `identical_operands` 3 which is probably three real defects).

**Two things not to do.** Do not run `swiftlint --fix` across the repo to adopt a new rule — a ~1,100-file reformat destroys `git blame` and collides with every open bead branch; `scripts/lint.sh --fix` refuses without explicit paths for exactly this reason. And do not add a rule to `only_rules` without measuring it first: one red rule and everyone learns to route around the gate, which is strictly worse than having no linter.

**`scripts/lint.sh` also runs the SHAPE-2 preflight (`scripts/singleton_slot_preflight.py`, playhead-mfeq).** It bans an optional stored `var` named `current*`/`pending*`/`last*` on an **`actor`** — a singleton standing for a set, which is the standing defect class's most productive shape here: `playhead-mk6z` (`currentJobId`, the last DISPATCHED job read as the running one) and `playhead-lmrx` F4/F9/R3 are four shipped defects with one rule between them. `actor` is the enforced scope because an actor is **reentrant**: every `await` is a point at which another operation enters and finds the same stored properties. It is a separate script rather than a SwiftLint `custom_rules` entry because the predicate needs the *enclosing type* of a declaration, which a per-line regex cannot see.

Three things to know before you argue with it:

- **It was measured before it was written, and the count is in the allowlist's header.** 56 matches across all type kinds on `b5e04fc6`, 47 after the scanner stopped counting function-LOCAL `var`s as stored properties (a local is per-invocation, so it is not a shared slot — `ShadowRetryObserver.lastSeen` is line 1 of a method). 8 were on actors; mfeq removed 3 and the rule shipped enabled with 7 allowlist entries and zero un-allowlisted violations. **It is 6 entries as of playhead-2kxd**, which fixed `AdDetectionService.currentPodcastProfile` and deleted the entry in the same change — the amnesty was temporary and it expired, which is the mechanism working rather than a count drifting. The ~40 non-actor matches are deliberately unenforced — measured, they are overwhelmingly genuinely singular — and `--report-all` prints them.
- **`scripts/singleton-slot-allowlist.json` is CLOSED IN BOTH DIRECTIONS.** A new match fails, and an entry that matches *nothing* also fails: a licence for a field nobody can find was renamed or moved, and whatever inherits the name inherits the amnesty. Same rule the gate baseline applies to a member that did not run. A `why` must say **what bounds the population to one**. This line used to add "and two entries are not licences at all, they name a filed bead (`playhead-2kxd`)" — **there was only ever ONE such entry**, and `git log -p` on the file will show you it never had a second; the miscount is corrected here rather than carried forward. That entry is gone: playhead-2kxd keyed the profile by show (`podcastProfilesByShowId`) and removed its own licence in the same diff. **All 6 remaining entries are real licences**, so a `why` that names a bead is now the exception and should be read as a promise somebody owes, not as a category this list routinely holds. The sibling defect 2kxd deliberately did NOT fix — `showPriors`/`scanner`, the same slot shape derived from the same profile on the same three lines — is `playhead-jjke`, and it is invisible to this rule by construction: one is non-optional and neither is named `current*`/`pending*`/`last*`. **A green preflight is evidence about the NAMES it can see, not about the shape.**
- **Verify it the way it was built:** `python3 -m unittest scripts.tests.test_singleton_slot_preflight` (30 tests, ~5 s, no build). Re-introducing a deleted slot must make `scripts/lint.sh` exit **2**; a non-optional `var` or a computed property must leave it silent. A rule that fires zero times because its pattern never matches is indistinguishable from a codebase with no violations — so prove it fires by making it fire, rather than reading a green result as evidence.

### Test plans

Two test plans exist. **Use the correct one for your context:**

**Per-bead work (implementation, review, fix cycles):**
```bash
xcodebuild test -scheme Playhead -testPlan PlayheadFastTests \
  -destination 'platform=iOS Simulator,name=iPhone 17'
```
Skips XCTest interruption-cycle integration suites. Runs in ~3 minutes on simulator.

**Self-healing gate wrapper (`scripts/fast-gate.sh`, playhead-qt8y/ekpn):** prefer the wrapper over the raw command — it bootstraps a fresh worktree (links the gitignored model from the main checkout + `xcodegen generate` when the scheme is missing the plan), caps concurrent compile jobs to survive the cold-build OOM, and auto-recovers a wedged sim (`Mach error -308`):
```bash
scripts/fast-gate.sh    # PlayheadFastTests, single-host; forwards -only-testing:... etc.
# PLAYHEAD_PLAN=PlayheadIntegrationTests scripts/fast-gate.sh   # phase-close superset
```
⚠️ **Run gates ONE AT A TIME, and do NOT add `-parallel-testing-worker-count ≥2` on this box.** Two memory drivers matter here: (1) a fresh-worktree **cold build** compiles the whole project with parallel `swiftc` — heavy enough on its own to get xcodebuild **OOM-killed** (`Killed: 9`), which is why the wrapper caps `-jobs` (default 4, env `PLAYHEAD_BUILD_JOBS`); (2) running **two gates/builds at once** exhausts the 16 GB box → one is killed mid-suite (`** BUILD INTERRUPTED **`, signal 144) with **no test failure** — pure resource exhaustion. **Clone-based parallel testing is unavailable here** (playhead-ekpn): worker-count ≥2 makes Xcode spawn sim clones, and the clone helper resolves `simctl` via the *global* `xcode-select` (`/Library/Developer/CommandLineTools`, no `simctl`), so it dies with `xcrun: error: unable to find utility "simctl"` (exit 65, ~18s, zero tests run) — `DEVELOPER_DIR` fixes `xcodebuild` but not the clone helper (the 2026-07-16 gotcha; enabling real clone parallelism would need a global `xcode-select -s` change — sudo, system-wide, Dan's call). The gate runs **single-host**: XCTest serial + Swift Testing's cheap **in-process** concurrency (the ~8,300-test bulk stays fast). Serialization + the `-jobs` cap, not in-gate clone parallelism, are the memory guardrails. Deferred (a coverage tradeoff for Dan's call): PerfGate-ing the load-sensitive behavioral flake families (gy2s pipeline-stall / RouteChange / Interruption / PlaybackService audio-session / playhead-7h2 runtime-shutdown) out of the default gate — those test real behaviors, so moving them is a coverage decision, not done here.

**The gate's verdict is about the DIFF, not the count (`scripts/gate_baseline.py`, playhead-voez).** The default gate is RED on a clean checkout — the load-sensitive families above blow their 60s time limits under the full run's own concurrency — so its exit code used to say nothing. The known-broken set is now committed in `scripts/gate-baseline.<plan>.json` and `fast-gate.sh` reports the difference:

```
RED (N known / 0 new)   -> exit 0
RED (N known / 2 NEW)   -> exit 65, both named
a baseline test PASSES  -> exit 65, named
```

Refresh with `scripts/fast-gate.sh --accept-baseline` and **justify the diff in the commit message** — the file is the record of what is known-broken, so a shrinking diff is good news and a growing one needs a reason. Do not reach for `PLAYHEAD_SKIP_BASELINE=1` to make a red gate quiet; that is the bypass this mechanism exists to remove.

Four things worth knowing before you argue with it:
- **The baseline is measured, not labelled.** Each entry carries how many observations it was seen in and failed in. `failed == seen` over ≥3 observations is *deterministic* and its **passing hard-fails** the gate (Dan's call: the baseline is exact, not a ceiling). Anything else is *load-sensitive*: passing is reported as a removal candidate but is not fatal, because one quiet run is not evidence a starvation flake is fixed. **How much this churns, measured 2026-08-01:** two full runs on identical code, quiet box, nothing else running gave 32 and 28 failures with only 19 in common — union 41, **Jaccard 0.46**. Fewer than half the failures recur. That is why a flat exact set is unusable here, and why promotion needs three observations rather than two. (Re-measured at o89d R3 over 703 pairs of 38 full-plan runs: median **0.505** (R2 read 0.515 over 378 pairs of 28). The churn is real and this bullet stands — but see the population paragraph below before extending it into "so the set cannot converge", which is a different claim and is false.)
- **The tolerance is not a hole, because identity includes the failure KIND.** A load-sensitive entry means "may TIME OUT", not "may fail". A known-timeout test that fails an *expectation* is reported NEW.
- **A baseline member that did not RUN fails the gate.** Renamed, deleted or newly skipped, a name nobody can reach is not evidence. This is what makes step 2 of the bead (PerfGate-ing a family) show up as a gate failure demanding a refresh, rather than silently shrinking coverage.
- **Selective runs are exempt.** `-only-testing:`/`-skip-testing:` (how `mutation-battery.sh` drives the gate) is a different population, so the check is skipped and the raw xcodebuild exit code passes through untouched.
- **A CRASHED HOST produces NO VERDICT, and the gate is ARMED on it (playhead-tl6l, armed by playhead-buvn).** A test whose host died emits no per-test line at all, so it was matched against the baseline as neither "known" nor reported as NEW — it fell out of the arithmetic in *both* directions, and the reassuring `RED (N known / 0 new)` could be printed by a run that lost a whole test family. It now reads `RED (85 known / 0 new) — 11 tests got NO VERDICT (crashed host)`, GREEN is unreachable while the count is non-zero, and `--accept-baseline` **carries those entries forward** instead of pruning them as renames. **The arm is a DIFF, not a count**, exactly like every other category, and **the record is shaped exactly like `tests`** — a **union of names carrying `seen_runs`/`lost_runs` observation counts**, with the tier falling out of the counts rather than a hand label. A name that loses its verdict and is *not* recorded is a NEW CASUALTY; a **deterministic** recorded name that reports again exits 65 (its licence has rotted — Dan's pass-direction rule, and the thing that lets a union ever shrink); a **load-sensitive** one reporting again is good news and exits 0; the count alone — or a bare host restart that cost nobody a verdict — is not fatal. Names rather than a count because substitution (eleven die, eleven different ones recover) is exactly what a count cannot see. **A union rather than a replaced set because the third run said so:** the two runs of 2026-08-12, on *different trees*, lost **the same eleven tests** (Jaccard 1.00), which is what the original replaced-set design was argued from — and the third full-plan run, on the armed code, lost **fifteen**: the same eleven again plus four that had **PASSED in both** earlier runs (Jaccard 1.00 → 0.73, nothing ever recovering). Under a replaced set an armed gate on a night like that reports four NEW casualties and exits 65 for healthy tests nobody can do anything about; under the union, accepting the three runs in order leaves the eleven at 3/3 (**deterministic**) and the four at 1/1 (**load-sensitive**). Read `1/1` as *one observation*, not *one of three* — a name accrues observations only from the accept that first records it, exactly as in `tests`. **UNRECORDED IS NOT ZERO, AND PROVISIONAL IS NEITHER** — three states, three claims: no `no_verdict` key means nobody has recorded the population, so the arm is **inert** and says so; **1–2 observations is PROVISIONAL**, where an unrecorded casualty is *named* but not fatal, because two observations do not bound a population this load-sensitive; **3+ observations is ARMED** (including a recorded *empty* census, which is a positive claim). That ladder is what let the arming land without turning main red today for the pre-existing crash `playhead-rouw` owns. Why the census gets a grace the failure baseline does not: a NEW FAILURE names a test that failed and can be triaged against your diff, while a test with **no verdict was never judged at all** and the only honest remedy is to re-run the whole plan. Blind spots, named: a test the host killed **before its start line** is in no set at all (it surfaces, if at all, as an unmatched `Failing tests:` entry, which is a LEAD and is deliberately never armed); identity is the **display name**, so same-named tests in different suites share a key; and a **rename** reads as one recovery plus one new casualty, i.e. a false RED whose remedy is the refresh a rename already demands. **The census number has been wrong three times and every correction was the same defect class** — a value that names one thing read as though it named another. 19/18 came from grepping the `Failing tests:` block's `Suite.function()` spelling against a console that prints `@Test` **display names**, which can only ever return zero. 33/15 came from requiring the literal word `seconds` when xcodebuild splices app output mid-token. 30/14 came from assuming the splice stops at the duration — **it does not**: it lands inside the name (`✔ Test "a b` + `yte-exact span…`), inside the verb (`" pa` + `ssed after 107.082 seconds.`), and once inside the word `Test` itself (`✔ Tes` + `t "watermark…`), because the intrusion carries its own newline and splits one logical line into two physical ones. 18 such lines on main and 3 on mn5e, **every one a passing test**. No pattern over `Test "` can see them; `gate_baseline.py` **rejoins** the halves before parsing, which is what makes the number **11 and 11**. **READ THAT AS THE HISTORY OF ONE FIX AND NOT AS THE CURRENT CONTRACT.** As written it says the name splice is handled, and playhead-phn3 was filed because it is not: the rejoin reached line N+1 and nothing else, which is 90.2 % of the shape and not all of it. What it says now is below. Two things not to misread: the census only works because **the 30 XCTest PerfGate skips are parsed as a third outcome** (Swift Testing's own skips contribute nothing — a trait-disabled test never emits a `started` line); and the direction that has never yet been hit is the dangerous one — 88 fail lines against 10,910 pass lines is the only reason it was passes that got severed, and a severed FAIL is a **lost failure**.

**THE CENSUS NO LONGER READS THE CONSOLE, AND THE PARAGRAPH ABOVE IS THE RECORD OF WHY IT HAD TO STOP (playhead-t53a, 2026-08-15).** Read the previous bullet's last third — the three corrections, 19/18, 33/15, 30/14 — as a history rather than a method. There was a fourth, and it is the one that ends the series: main's 06:01 full-plan run reported **seven tests with NO VERDICT (crashed host)** while carrying `host_restarts: 0` and not one crash marker in 9.9 MiB. The `.xcresult` for that same run records **all seven as `Passed`**. Three shapes, none of which any pattern over `Test "` could see **then**: a cut **mid-word** (`passed afte` + an app-log line), a cut **inside a UTF-8 codepoint** (the `✔` reduced to `\224`), and a verdict line **absent from the console entirely**. Two corrections to that sentence, both from playhead-phn3 below: the console reads the first two now, and `\224` is **not a bare continuation byte** — it is the four ASCII characters `\`, `2`, `2`, `4`, xcodebuild octal-escaping a token it could not decode, and reading the rendering as the thing rendered is the very defect class this paragraph is about. The instrument built to catch "a value that names one thing read as though it named another" was the thing exhibiting it.

**Measured across 68 preserved full-plan logs before the fix, and the split is the number this bead exists to establish:**

| | runs | lost verdicts | recoverable from the bytes | no trace at all |
|---|---|---|---|---|
| `host_restarts == 0` | 27 | 87 | **80 (92 %)** | 7 (8 %) |
| `host_restarts > 0` | 41 | 2,743 | 21 (0.8 %) | **2,722 (99.2 %)** |

So the census was **right about crashes and wrong about everything else**, and its false positives sat exactly where a run was otherwise clean. Not one of the 27 crash-free runs had a crash. The seven remaining no-trace names on those runs are unproven either way from the console — and the one of them for which a bundle survives (06:01's `tier validation resets invalid ascending order to defaults`) **passed**. One of the other six is not even a real test: an app `print()` with no os_log prefix was spliced into a *start* line and fabricated a name (`.enabled bunarl.normalizer: episodeId=… ⇒ context behaves as today`) that no verdict could ever match — a permanent phantom casualty, and a fifth splice shape, which is the argument against a fifth regex.

**What changed.** `scripts/fast-gate.sh` now passes `-resultBundlePath` and hands the bundle to `gate_baseline.py --xcresult`, which takes **every outcome** — passed, failed, skipped, and the failure KIND — from the bundle. Verified on the 06:01 run: the bundle's key set and the console's `ran | skipped | started` are the **same 12,085 names, zero unmatched in either direction**. Four things about the design, because each is a place it could have gone quiet instead of truthful:

- **The console keeps the STARTED roster, unioned rather than replaced.** A test the infrastructure never mentioned is still a casualty if its `◇ … started` line survived. Neither source can silence the other — a name leaves the census only by being *judged by the bundle*.
- **A DEAD HOST IS NOT A FAILING TEST, and the bundle says which in words.** Measured with a deliberate probe (a five-test suite, one calling `fatalError`): xcodebuild restarted the host, the restarted run executed `0 tests`, the console lost four verdicts — and the bundle recorded all four as `Failed` carrying `Test crashed with signal trap.`, identifiers intact. Taken at face value that would book three *healthy* tests as failures, and one `--accept-baseline` would write them into the known-broken file where their next genuine failure reads as known. So a crash-message failure is routed to the **census**, whose remedy ("run it again") is the right one. The census stops being *inferred from silence* and becomes something the bundle *states*. An unrecognised crash wording stays a FAILURE — the loud direction.
- **The residual re-run.** Whatever is left without a verdict is re-run scoped (`gate_baseline.py residual` prints the `-only-testing:` arguments; the bundle is what makes a display-named test addressable at all), one pass, full-plan runs only, capped at 120. On the probe: re-running A/B/C recovered all three and the deterministic killer **stayed a casualty**. The loop cannot launder a real crash.
- **`Failing tests:` stops being only a LEAD.** The block spells a test `Suite.function()` and the console prints its `@Test` display name, which is why the two could never be compared. The bundle carries both, so the match is exact — three "matched no console result" entries on the 06:01 run become three matches, and they are that run's three real failures. A blamed name no bundle knows is still reported unmatched.

**THE CONSOLE PATH IS STILL LOAD-BEARING AND IT HAD TWO MORE HOLES — the fifth correction, and the first one whose fix is CHEAP (playhead-phn3, 2026-08-18).** t53a routed the VERDICT to the bundle and left the console owning the STARTED roster, so "the console cannot read this" stopped being a reason to fix it. It is still a reason: the two sources are UNIONED, a test the console never mentions is one the bundle alone can silence, and the arm exists precisely so a run that lost a family cannot print a green. Two defects, both found on playhead-xul6's merge gate — `** TEST SUCCEEDED **`, 11,011 tests, zero failures, `host_restarts: 0`, and **four tests reported NO VERDICT (crashed host)**. All four passed.

- **The weld only ever reached line N+1.** The intrusion carries its own newline *and the app keeps writing*, so the displaced tail lands however many app-log lines later. **Measured over 136 preserved full-plan logs, 2,563 welds: span 1: 2312 · 2: 203 · 3: 33 · 4: 9 · 5: 4 · 6: 2.** The shipped bound recovered 90.2 % and left 251 records reading as silence. The scan now walks forward while — and only while — the lines in between ARE app output (timestamp and process at column 0), and stops dead at a record that stands alone, at anything unattributable, and at `_MAX_SPLICE_SPAN = 8` (observed max 6, plus a stated margin). The app-output-only rule is the safety property; the bound is the backstop.
- **The marker glyph itself gets severed, and what survives is an OCTAL ESCAPE.** Every glyph is three bytes and the cut lands on a byte, so xcodebuild escapes what it cannot decode *per damaged token* and the verdict arrives spelled `\224 Test "…"`, `\234\224 Test "…"` or `\342\227\207 Test "…"`. **92 such lines over 138 preserved logs, in all three spellings, and ZERO raw continuation bytes** — the raw em dash on the same physical line survives intact, which is how one can tell. **The bead's own text said the line "starts with a bare continuation byte 0x94"; the bytes on disk are `5c 32 32 34`, the four ASCII characters `\224`** — the standing defect class one more time, a rendering read as the thing it renders, and a byte-level repair pass would have fixed a spelling this box does not produce. The fix admits the wreckage as an alternative ANCHOR rather than dropping the anchor: **the glyph is decoration and the verb is the record**, but an anchorless pattern reads `[SkipOrchestrator] Test "x" passed after 1.0 s` as a verdict and invents a test, which is the fifth splice shape recorded above as a permanent phantom casualty.

**Measured across 138 preserved logs, old parser vs new: NO VERDICT 14,603 → 14,331 (−272), passed +271, skipped +1, started +51, failures 591 → 591 with none gained and none lost. 76 logs shed at least one casualty and ZERO gained one** — which is the check that matters, because recovering a severed START without its verdict would manufacture a casualty rather than remove one. On t53a's own merge gate, the log that motivated moving the census to the bundle: **9 → 0**. Do not read this as a reason to go back to the console for verdicts — the 68-log table above still stands and the bundle is still the instrument. It is a reason to keep the roster honest.

Rails: 26 new (246 total, ~3 s, no build), of which **20 fail against the parser as shipped and 6 are anti-fabrication guards that must hold in both directions**; the four specimens are byte-exact and are pinned against a committed 52 KB distillation of the whole 6.4 MB run, so a typo in one turns into a red rail rather than a test of the typo. 17 new mutants (RS01–RS17, 125 total, 124 killed, control survives). **Three survived first time and two of them were the new tests being vacuous** — one read the very constant it existed to pin (`APP_B * (gb._MAX_SPLICE_SPAN + 1)`, which rises with the mutant), one put an INTACT name after a damaged glyph so the tail parsed on its own and the head was never exercised, and the third was a *proven* equivalent (dropping `+` from the shard cannot matter when every pattern SEARCHES) and was repointed rather than excused.

The verdict now **names its instrument on every run**, including a clean one: a census of zero taken off the console is exactly the reading that most needs qualifying, because a severed *start* line makes a test invisible in both directions. Rails: 30 new unit tests (215 total, ~3 s) and 18 new mutation rails (RB01–RB18) in `scripts/mutation-battery-gate-baseline.py`, including the shell half — a stubbed `xcodebuild` that really writes a bundle and a stubbed `xcrun` that really reads it back, because "does the bundle reach the check" is a property of argument plumbing, not of Python.

**Expect the file to grow for a run or two before it settles, and do not read that as regressions.** The recorded set is the UNION of what has been observed, and with a 0.46 Jaccard each new run surfaces flakes the earlier ones missed. Capture–recapture on the first two runs (32 and 28 failures, 19 shared) estimates the true population at **~47**; 41 are recorded after two observations. So the next `--accept-baseline` or two will legitimately add a handful of names, and the gate will report them as NEW until they are recorded. That is the mechanism working — a newly-observed flake is indistinguishable from a regression until it has been seen, which is exactly why it is reported rather than absorbed. The third accepted observation is also what arms the pass-direction check.

**THE POPULATION IS BOUNDED AND THE OBSERVED UNION HAS VERY NEARLY ENUMERATED IT — measured directly at o89d R2, re-derived independently at R3, and two earlier versions of this paragraph claimed the opposite.** Both of those were estimates (a Chao1 "lower bound of 377", withdrawn below; then "the union does not saturate … there is no knee"). What follows is the direct measurement of the quantity they were estimating, so prefer it over either. **The numbers below are R3's; R2's are given beside them, because the two disagree on the SIZE of the log census and agree on everything the conclusion rests on.**

Method, so it can be re-run: every preserved log that is non-selective, started **more than 11,000 tests** and lost **fewer than 100 verdicts**, de-duplicated by `(start count, failure set)`, parsed by `gate_baseline.parse_run` itself rather than by a fresh regex. Re-run at R3 and independently again at R4 over **every** `.log` under `/private/tmp` (which is where `claude-501/-Users-dabrams-playhead/*/scratchpad` actually lives — there is no `claude-501` under `$TMPDIR`, so R3's stated root does not exist on this box), `$TMPDIR`, `/Users/dabrams/playhead` (which contains `.worktrees`) and `/Users/dabrams/.claude`: **39 qualifying logs, 38 after de-duplication, 2026-08-09 18:37 → 2026-08-13 12:41**. Union: **269 distinct failing tests**. R4 reproduced every figure in this section to the digit. Two mechanical details that matter if you re-run it: a size floor of 100 KiB is safe and provably non-binding (the smallest qualifying log is **6.8 MiB**, since >11,000 started tests cannot fit in less), and **"non-selective" must be tested on `-only-testing:` WITH THE COLON** — see the next paragraph for what happens otherwise.

**R2 stated the same criteria and reported 28 runs / union 243, and its own census of logs was the incomplete half** — the defect it had just charged R1 with ("there are nineteen preserved logs that day, not four"), one level up. Prefer 38/269, and **name the roots you walked**, because "every preserved log" is a claim about a search, not about a set. **The whole of R2's shortfall is now accounted for (R4); R3 left six of it "unexplained by any variant of the stated filter I tried", and the six are one bug.** Four of the 38 live outside the session scratchpad that R2 walked (`/private/tmp/gate-baseline-refresh.log` and three under a second session's directory). The other **six are full-plan runs whose own header line reads `REQUESTED_SUITES=ALL (PlayheadFastTests, no -only-testing)`** — a bare substring test for `-only-testing` matches the sentence that says there were none and drops the run as selective. `28 + 4 + 6 = 38`, exactly. R4 hit the identical bug on its own first pass and got 33/32/250 before fixing it, which is how it was found. It is the standing defect class in the census machinery for a third time: a string that names an ABSENCE read as evidence of a presence. Testing for the colon fixes it, and **zero** of the 48 logs that start >11,000 tests are genuinely selective, as one would expect.

- **LEAVE-ONE-OUT: the union of 37 runs covers the 38th run's failures at a median of 100 % and a mean of 97.5 %** (R2: 100 % / 97.3 % over 28). Twenty-five of the 38 are covered *completely*. The worst — 72.9 %, 76.0 %, 84.3 %, 85.9 % — are each a run carrying its own branch's breakage: 26 of one run's residual names are `SkipOrchestrator*`/`rfu-sad`, 14 of another's are a mutation control's intended kills, 13 of a third's are schema-migration tests. **The residual is the diff.** That is the gate's NEW column working, not the population moving.
- **LEAVE-ONE-OUT IS AN IN-SAMPLE STATISTIC AND THE QUANTITY YOU WANT IS FORWARD COVERAGE.** Holding run *k* out still lets it be covered by runs that came *after* it, so it is not a prediction. The forward version — what fraction of run *k*'s failures the union of runs 1…*k*−1 already contained — is computed from the same data and is the number to quote: over runs 11–38, **median 100 %, mean 96.7 %**; over the last ten runs, **mean 98.9 %**. The conclusion survives the stricter estimator, which is why it is worth stating in the stricter form.
- **The union has stopped growing.** Per-run additions in time order: `+79 +31 +18 +11 +18 +0 +0 +20 +0 +4 +1 +0 +0 +16 +2 +13 +6 +1 +0 +2 +0 +0 +0 +0 +0 +0 +0 +35 +10 +0 +0 +0 +0 +0 +0 +1 +1 +0`. Nine of the last eleven runs added nothing at all, and the one late spike (+35) is the `SkipOrchestrator` branch-breakage run above.
- **168 names have been seen in ≥3 of the 38 runs, and they alone cover a mean 96.6 % of any single run's failures** (184 names at ≥2 cover 97.5 %). The other 85 are singletons, mostly branch-specific. *That* is the size of the thing, and it is a name list.
- **The FILE is behind the population, not chasing a moving one.** All **121** committed entries appear in these 38 logs — zero are stale — but the file is a 10-observation sample and is missing **49 of the recurring 168**. So expect `--accept-baseline` to keep adding names for several more accepts. That is the file catching up to a bounded set, and it converges; it is not evidence of a tail nobody can bound.

**"THE MEASURED UNION" IS TWO DIFFERENT POPULATIONS AND THE PHRASE READS THEM AS ONE (R3) — AND THE MEASURED HALF IS A RECORD, NOT AN INFERENCE, WHICH IS WHERE R3's 136 CAME FROM (R4).** R3's structural point stands: "PerfGate the measured union" names two things. Its NUMBER for the measured half does not, and the corrected one is bigger and better evidenced.

**Read the five preserved logs rather than reconstructing a set from a count.** The isolated measurement is on disk, per test, at `…/-Users-dabrams-playhead/bee44b12-…/scratchpad/runs/scopedA-{1..5}.log`. Parsed: **52 `-only-testing:` targets, byte-identical across all five runs, 441 tests with an outcome plus one skip, zero lost verdicts, and 440 of the 441 pass 5/5.** The single 5/5 failure is `benchmark gate`, already known to be a benchmark rather than a defect. Against the **269**:

- **184 union names were measured in isolation, and 183 of them pass 5/5** (the 184th is `benchmark gate`).
- **85 have no isolated measurement** — not 133. Of those 85, **66 are singletons** and only **9 recur in ≥3 runs** (all nine timeout-kind).
- Of the recurring **168** — the population worth scoping anything from — **159 are already measured 5/5**. Nine are not.
- Of the **176** timeout-carrying names, 144 are measured and **all 144 pass 5/5**.

**R3's 136 is the SELECTION SEED, not the measured set** — the names the 52 suite targets were derived from — and it was reconstructed rather than read: by searching for a combination of the 117-entry pre-accept baseline and the 08-13 logs whose union had *cardinality* 136. A cardinality does not identify a set. **Four different pairs of 08-13 logs hit 136, and they are four different sets**, pairwise differing by 6 to 24 names; only one of the four is fully covered by the isolated run (the others contain 1, 3 and 7 never-measured names), and **two of the four reproduce R3's residual breakdown 65/68/35/82 exactly**. So the reconstruction landed on the right set and its method could not have told it so, and the residual breakdown cannot distinguish them either. The pre-accept file's own pin holds (`sha256 4d300374…`, 117 entries, 9 observations), so the seed half of the recipe is right; it is the "plus two logs" half that is under-determined.

Consequence for the advice below and for `playhead-06t7`: extending the measurement from the seed to **the recurring 168 is NINE names, not thirty-two**, and they can be appended to the existing 52 targets for one more ~28 s run. Every other name in that population already has its 5/5.

**THE UNION IS NOT THE LOAD-SENSITIVE POPULATION EITHER, AND ~93 OF IT HAS NEVER PRODUCED A TIMEOUT (measured at R3).** Split the 269 by the failure KIND the baseline already records: **155 have only ever timed out**, **21 have shown both kinds**, and **93 have only ever failed an EXPECTATION**. A timeout is the starvation signature PerfGate exists for; an assertion failure is a test reporting on the code, and moving it to the serial pass hides a real verdict — the more so while that pass is red and unread (`playhead-1och`). Of the 93, 53 appear in exactly one run (branch breakage, correctly reported as NEW at the time) and **29 recur in ≥3 of the 38 runs on many different trees** — `DownloadShowAttributionTests` ×7, `BackgroundGrantBudgetTests` ×7, `ForceQuitResumeTests` ×3, `BackgroundURLSessionTests` ×3. **The KIND does not settle which of those is a harness artefact** — measured at R4 off the five `scopedA` logs, **all 29 were run in isolation and 28 pass 5/5** (the 29th is `benchmark gate`), so an assertion CAN be the way starvation shows up (a poll loop's own bound trips before the 60 s time limit does), and `playhead-3f79`'s `settleWaitDoesNotLie` is recorded `assertion` for exactly that reason. What the split does establish is that **269 is an UPPER bound on the PerfGate-able population and 176 (the timeout-carrying names) a LOWER one**, that the isolated measurement is the only thing that discriminates inside that band, and that PerfGate-ing by NAME throws away the kind discrimination the gate's own tolerance is built on. Quote whichever bound you are using and say which.

**The failing set still ROTATES, and that half of the record is right — but rotation is CHURN INSIDE A BOUNDED SET, and the two were conflated.** Pairwise Jaccard over all **703 pairs** of the 38 runs: median **0.505**, mean 0.507, range 0.171–0.798 — consistent with the 0.46 measured on two runs on 2026-08-01 and with R2's 0.515 over 378 pairs. So *which* half of the known population fails tonight is close to a coin flip, which is exactly why a flat exact set is unusable and why the `seen`/`failed` tiering exists. It is **not** a reason a name list cannot bound the population: the same data says a 269-name list leaves a typical run with **zero** unabsorbed failures. Do not quote "the failing set rotates" as an argument that accumulation cannot converge — measured, it converges.

**WHAT THIS FOUR-DAY WINDOW CAN AND CANNOT SUPPORT.** Verified at R3: the oldest preserved `.log` of *any* size on this box is 2026-08-09 18:37, so the window is four days and there is no way to extend it backwards. It supports "the union converges over four days of ~28–38 runs" and it supports refuting the 07-29-vs-08-13 drift claim, because the 07-29 cluster is present and failing throughout. It does **not** support "the population is stable over fifteen days" — that is exactly the interval where the reading went wrong before, and the only evidence for it is one endpoint. Re-measure when the logs reach back further rather than extending the sentence.

**DO NOT QUOTE A SPECIES-RICHNESS ESTIMATE OFF THIS FILE — it is not stable enough to carry a number (measured at o89d review).** The first version of this paragraph said Chao1 gives "a lower bound of **377**", and it did: on the 9-observation / 117-entry file, f1 = 51 and f2 = 5, so `117 + 51²/10 = 377`. **The very next commit in the same series invalidated it.** One accepted run moved 21 names from f1 into f2 (f1 51 → 30, f2 5 → 30), and the same estimator on the file committed here reads **136.0**. Chao2 over the same-day full-plan runs reads anywhere from **252 to 818** depending purely on how many of them you include. Because `f1²/(2·f2)` explodes when f2 is small and this record's f2 was 5. Two further traps the original wording fell into, both the standing defect class: 377 was computed with `S_obs = 117` and then compared against "136 ever observed" (with `S_obs = 136` the same spectrum gives 396), so the "~35 % sample" mixed a numerator and denominator from different populations; and "390 tests carry a `.timeLimit(.minutes(1))`, which is where 377 lands" is a coincidence of two unrelated counts, not corroboration. **Every one of those estimates is now superseded by the leave-one-out measurement above, which asks the question directly instead of extrapolating a frequency spectrum. Estimate species richness only when you cannot go and count; here you can.**

**A related trap, because the correction repeated the defect it was correcting.** The withdrawn text said "**two** full-plan logs of 2026-08-13"; R1 corrected it to "**four** (10:23, 11:09, 11:47 and this bead's own 12:41)". There are **nineteen** preserved full-plan logs from that one day. R1's `+10, +17, +9, +6` are each contributions measured against the 117-entry file *individually* (and 11:47's is 13, not 9), while its "union of 146" is the *cumulative* union after only the first **three** of the four — the four together give **149**. Individual and cumulative increments are different quantities and the sentence mixed them, which is why `117 + 10 + 17 + 9 + 6` does not equal 146 and never could. When you fold logs into a union, say which base each increment is against, and count the logs before you name them.

**Two corrections to how a red gate is read, from the same measurement.** First, **`passed after N seconds` is enqueue-to-completion, not test cost, and the 97 s triage rule reads it as though it were cost.** In a full plan **~90 % of PASSING tests report over 60 s** (re-counted at R2 off the spliced-line rejoiner: **90.4 %** of 11,452 passing lines on the 11:47 log of 2026-08-13 and **90.5 %** of 11,421 on the 12:41 one) and the median is **115–153 s** depending on the run (152.6 s and 115.5 s respectively). The clean witness is a test that prints its own `ContinuousClock` reading: `benchmark gate` reported **220.149 s** while its own clock read **105,490.8 ms**. So "under 97 s" selects tests that failed EARLY in the run, not tests that failed for behavioural reasons. Second, **"asserts an absolute wall-clock budget" is the wrong predicate** — CPU-bound budgets are fine under full load (`evaluate() completes in <200ms`, `detectRefined … within budget` at 200 ms, and the 5 s `PerformanceSmokeTests` all PASS in the full plan). What starves is **scheduler-dependent waiting** measured on an absolute clock: a poll loop, an `AsyncStream` consumer, a cross-actor callback. **440 of the 441 tests in those 52 scoped suites pass 5/5** — re-derived at R4 from the five preserved `scopedA` logs rather than from the "443 / 135 of 136" summary, which counted the selection seed instead of what ran; a 12-suite subsample spanning both stale name lists re-ran **3/3 clean** at review (147 tests, ~22 s).

**THE PROPERTY A MUTATION BATTERY CANNOT REACH IS THE LINE THAT ISN'T THERE (playhead-o89d R5).** Four review rounds pinned four properties of `--accept-baseline`'s output — the banner's SPELLING, the EVENT it names, the CONSEQUENCE it states, and the COUNT it prints — each round closing the direction it was looking at and leaving the mirror for the next one. R5 enumerated the accept path a second time and independently (69 mutants against R4's 28) and the residual hole was not a property of any line: **a KIND WIDENING had no line at all.** `merge` UNIONS a run's failure kinds into an entry already in the file, so a name recorded `timeout` that fails an EXPECTATION comes out recorded `assertion+timeout` — its tolerance doubled. Measured end to end: `check` reports `FAILS DIFFERENTLY … recorded as timeout, failed as assertion` and exits 1, the accept prints `(membership unchanged; counts updated)`, and the identical failure afterwards is GREEN. **15 of the 121 committed entries carry two kinds today**, several at 10/10 — the widest tolerance the file can express, arrived at without anyone signing for it. It is the fifth event that makes the gate LOOSER (after the two tier demotions, the census prune and the record-level arm) and the only one that was silent. `TOLERANCE WIDENED:` closes it. **The method, since this is the fifth round on one shape: enumerate the EVENTS the command can perform and ask which of them prints nothing — a mutant can only ever interrogate a line that already exists.** R5 also found R4's own three new rails one-directional in the same way (RA29 pinned the promotion detail's `[kind]` and not the demotion's; RA22 pinned `DISARMED:`'s event and not `CENSUS DISARMED:`'s; RA27 pinned the crash headline's count and not `CARRIED FORWARD:`'s), all three mirrors survived, all three are rails now. And **`--only` can no longer drop the vacuity control**: `--only RA32` used to print `control not run` and then `All mutations killed, and the vacuity control survived.` and exit 0 — the same hole the untypeable battery closed, still open in the R engine the P series shares.

Rails: `scripts/mutation-battery-gate-baseline.py` (R series) and `scripts/mutation-battery-disk-preflight.py` (P series, playhead-3nfa, reusing the R engine); the D/E/J/K/L/Q series stay in `mutation-battery.sh`, which is structurally a Swift battery. `scripts/mutation-battery-untypeable.py` (TY series, playhead-x0lb) is Swift too but reports a different verdict — **`UNTYPEABLE`, the mutated source must FAIL TO COMPILE** — because the defect class it covers (two quantities sharing a unit, one read as the other) is invisible to any test: the two are equal on every fixture a test author would write. It is build-only, ~13 s per rail warm (38 rails, measured at R6 review; the earlier "~5 s / 35 rails" figure was neither re-timed nor re-counted after R5 added three), and checks the diagnostic KIND so a syntax break reports `WRONG-ERROR` instead of being credited. **It builds the APP target only, so it cannot see a test file** — that is deliberate (the question is whether production type-checks) and it is why a round that edits tests still owes a `build-for-testing`. Two anti-vacuity rules were added in R2 review and are worth knowing before you drive it: **`--only` cannot drop the control** (a selection that omits `TY99` has it appended, because a partial run printing `control not run` and exiting 0 is exactly the hole this battery replaces), and **`--check-inventory`** is a buildless preflight — it runs on every invocation — tying `UnsoundCursorPromotionSite` to the actual sites, since the Swift test can only read `allCases` and that is a property of the enum rather than of the code. R3 review out-spelled that preflight inside its own file — a sixth promotion written `EpisodeSeconds.init(…)` rather than `EpisodeSeconds(…)` passed it with rc=0 — so both spellings are counted now; the general escape (a promotion helper declared in another file) remains open by construction and is limit L-F. R6 review added a second clause to the same preflight — **region ASSEMBLY is confined to `AnalysisStore.swift` and `CoverageQuantities.swift`** — because `init()`/`append(start:end:)` on the four interval-carrier types are internal, so probe PJ1 assembled a `TranscribedRegion` out of the FAST ranges in `AnalysisJobRunner` and it compiled, reproducing playhead-9y9e's shipped defect one layer below rails TY32/TY34. **R6 wrote that clause as a grep over the four TYPE NAMES and R7 got three spellings past it** — `TranscribedRegion ()` (a space before the parens), `.append(start : …)` (a space before the label's colon), and probe **PK1**, `let region: TranscribedRegion = .init(fastPass: .init(spanningFromZeroTo: CoveredSeconds(watermark)), finalPass: .init())`, which models the fast watermark as a contiguous transcribed region and feeds it to the 0.95 finalize floor; the preflight returned rc=0 and the app BUILT. Dot-`.init` names no type, so no pattern over type names can ever see it. What replaced the grep is a **compile-enforced naming obligation**: `RegionFillDoor` is a token the six assembly entry points take, so a fabrication that omits it is `missing argument for parameter 'door'` (rail TY38) and one that supplies it must write the identifier `openedByCoverageReader`, which the preflight greps for — "one identifier, every spelling" instead of "the spellings the last reviewer thought of". **It is still not a capability**: the token is `internal`, so it stops an author who is not thinking about which population they hold, not a determined one, and that is L-F's worth arrived at honestly rather than by enumeration. Unit tests: `python3 -m unittest scripts.tests.test_gate_baseline` — about a second, no build.

**ONE BATTERY PER WORKTREE, ENFORCED (playhead-pu7e).** `scripts/mutation-battery.sh` now holds a lock in the worktree's own git directory for the whole run; a second invocation exits **75** naming the holder's pid, start time and `--only` argument. Do not go looking for a way around it — the reason it exists is that a concurrent battery is **undetectable from inside either run**. Between run A's restore and its next apply the tree is genuinely clean, so the dirty-tree guard lets B in, and from then on each run's restore reverts the other's mutant while both byte-exactness checks keep passing (each verifies only its OWN restore). It cost four destroyed verdict attempts in `playhead-9y9e` R1, every one of them reported as `the baseline did not run tests (rc=65)`; rails RT11, SC25, SC30 and SC33 still carry the implementer's own verdicts because no reviewer could obtain an independent one.

**Read the exit code, not just the refusal — `75` and `2` mean opposite things.** `75` is EX_TEMPFAIL: somebody holds this worktree, and waiting is the remedy. `2` means the lock could not be *established at all* and **nobody holds it**, so waiting will never help — a full volume, an unwritable git directory, or a run that took the directory but could not record who it was. The review round found the shipped code reporting every one of those as `75` with "Another run took it", which sends you to wait for a process that does not exist; and, worse, found that a holder whose identity write failed carried on mutating while its lock sat ownerless, to be reclaimed as ABANDONED by the next battery 300 s later — inside a normal 4–9 minute run. Both closed: taking the directory and recording the owner are now one step that either happens or does not, and `info` is renamed into place so no reader can ever see half of it.

Two corollaries worth knowing before the next `rc=65`:

- **`rc=65` with no tests is no longer self-diagnosed as a broken tree.** It now names CONTENTION, a WEDGED RUNNER (`blessSimulatorHub` / `service hub IS NOT still alive`, with the `simctl shutdown all` recovery), DISK, OOM, a real BUILD FAILURE, or **UNDIAGNOSED** — and prints the matched line as evidence. `xcrun: error: unable to find utility "simctl"` is labelled a **secondary** symptom: it is xcodebuild's diagnostic collection shelling out through the global `xcode-select`, not the 2026-07-16 clone-parallelism gotcha, and chasing it wastes the round.
- **The battery has never run `git checkout -- .`, and earlier prose here and in its own header said it did.** `restore_sources` is `git checkout -- "${MUTABLE_FILES[@]}"` and cannot touch a file it does not mutate. The blanket checkout was a *remedy people copied out of that wrong prose*, and on 2026-08-04 three agents destroyed their own uncommitted work running it by hand. If you want to know whether a run left residue, ask: `git status --porcelain -- Playhead`.

### The full plan runs ONCE, before merge — not per review round (Dan 2026-08-13)

**A review round uses `-only-testing:` over the suites its diff touches. The full plan is the merge gate and nothing else.** This is a measured decision, not a preference.

Across **61 preserved full-plan logs**: **649 NEW failures reported, and essentially none real.** Only 8 of the 61 contain a single Swift Testing failure under 97 s or any XCTest failure — and most of those 8 are deliberate mutant runs, controls or probes, where the failure *is* the intended result. Every other one of the 649 was `Time limit was exceeded: 60.000 seconds`, reported at 150–200 s wall clock, in the same handful of suites.

**Which suites, ranked rather than sampled (re-derived at o89d R3 over 38 full-plan runs, by the SOURCE FILE on the issue line, mean failures per run / runs present in):** `BackgroundGrantBudgetTests` 10.9 / 38-of-38 · `SilenceCompressionCoordinatorTests` 9.2 / 36 · `BackgroundProcessingServiceTests` 7.9 / 34 · `PlaybackInterruptionTests` 4.4 / 34 · `SpeechRecognitionRequestGateTests` 3.8 / 38 · `ForceQuitResumeTests` 3.7 / 37 · `DownloadShowAttributionTests` 3.7 / 20 · `BackgroundProcessingServiceLedgerTests` 3.5 / 35 · `RouteChangeTests` 2.9 / 37 · `BackgroundProcessingServiceTelemetryTests` 2.7 / 37 · `AnalysisPipelineStallInvariantHarnessIntegrationTests` 2.6 / 34 · `InterruptionHandlingTests` 1.8 / 35. R2's ranking over 28 runs reproduces within 0.4 on every shared entry, so **this is the stable part of the record**. Two additions worth reading: `DownloadShowAttributionTests` ranks 7th and is missing from R2's list, and it is an **assertion-kind** family rather than a starvation one — see the kind split above before treating the ranking as a PerfGate shortlist. **Two earlier top-6 lists — 2026-07-29's and 2026-08-13's — were read as evidence the population had MOVED ("near-disjoint, not one name in common"), and it had not.** They are two truncations of one stable ranking: the 07-29 list omitted the single largest contributor and the 08-13 list omitted the second and third, each of which is present in 34–36 of the 38 runs. Rank the whole thing before concluding anything drifted.

Those tests assert an **absolute wall-clock budget** while ~8,000 tests saturate a 16 GB box, so the quantity they measure is machine load and the quantity they are read as measuring is code correctness — this repo's standing defect class, living in the harness instead of the product. Selectively re-run, the same tests pass in **2–9 s** (measured repeatedly: 117 tests/10 suites in 8.6 s; 188/10 in 7.2 s; 74/11 in 4.4 s). `playhead-o89d` carries PerfGate-ing them.

The cost being removed is not machine time alone. Every RED gate obliges a reader to triage both failure formats, check every duration against 97 s, and read the `Failing tests:` summary block — a decision point on every round of every bead, which across those runs produced 649 judgements with no defects behind them.

**What this does NOT relax, because "fewer full runs" is easy to misread as "verify less":**

- **Mutation testing is the priority, not the casualty.** Across the seven beads of 2026-08-12/13, *every* real finding came from a surviving mutant, a behavioural probe, or a device pull — **not one came from a full-plan gate**. A scoped run is seconds, so a round should afford *more* mutants, not fewer.
- The merge gate is still a real full plan, still triaged in both formats, still with its `NO VERDICT` census read.
- `scripts/lint.sh`, the SHA-256 pin / byte-exact restore discipline, and the never-`git checkout --` rule are unchanged.

**Phase-close verification only (final gate before closing an epic):**
```bash
xcodebuild test -scheme Playhead -testPlan PlayheadIntegrationTests \
  -destination 'platform=iOS Simulator,name=iPhone 17'
```
True superset of FastTests — it skips nothing at all, so it adds back exactly the **twelve** XCTest classes the fast plan skips: ten in `InterruptionHarnessTests.swift` (eight `InterruptionCycleSuiteBase` subclasses plus `InterruptionHarnessTests` and `InterruptionHarnessInjectTests`), plus `MetricsCorpusIntegrationTests` and `FoundationModelShadowBenchmarkTests`, which are not interruption-cycle suites. **It said "the 20 XCTest interruption-cycle suites" until playhead-wwbr R1**, which is the same miscount the skip list itself carried — 20 is the number of entries that never worked, and it was never the number of classes this plan adds. `TestPlanSkipListCanaryTests` now pins the superset relation mechanically. Real-device runs are slower than the simulator because FM tests actually execute (on the simulator FoundationModels is gracefully unavailable). **The "~1 minute" this line used to claim, and the "~3 minutes" above, are both unmeasured and cannot both be right** — a superset cannot be three times faster than the plan it contains; the one datum on record is the 2026-08-13 wwbr merge gate, a full `PlayheadFastTests` at **~8 minutes** wall clock. Measure before quoting either.

**Why both plans use `skippedTests` only (no `selectedTests`):** Xcode's xctestplan filter honors XCTest class names but **silently ignores Swift Testing identifiers** in both `selectedTests` and `skippedTests`. Selecting Swift Testing tests via `selectedTests` ends up enabling 0 of them. We work around this by using `skippedTests` (which leaves Swift Testing always enabled) and only filtering XCTest classes. Per-class filtering of Swift Testing requires `-only-testing:'PlayheadTests/StructName/method()'` on the command line, not the test plan.

**That limitation had eaten two-thirds of the fast plan's declared contract, and the entries are now GONE rather than fixed (playhead-wwbr).** `skippedTests` listed 32 suites; **12 were honoured and 20 did nothing**, so 163 tests ran in every fast gate the plan said they did not (162 as of 8772c07d — see below). Every one of the 20 is a Swift Testing `@Suite`, and every one was added by the plan's very first commit (d55b0c1c, 2026-04-10), which declared 21 skips of which exactly one was an XCTest class; the two later commits added XCTest classes only and both worked. So the failure was total from day one and nothing regressed since. The harm was not the load. It was that **`Phase3ShadowReplayHarnessTests/benchmarkGate` failed every gate for four months** and was triaged each time as one more starvation flake, because nobody reads a test the plan says is not running — the 2026-08-13 gpdb log has it failing at **225.360 s against a 5,000 ms budget**, and the o89d log, one commit later, has it not starting at all because playhead-o89d `PerfGate`d it.

The 20 entries were **withdrawn, not re-implemented in source**, and the reason is measurement rather than taste. Scoped (`-only-testing:` over the 20 suites, 2026-08-13), in Swift Testing's own words: **`Test run with 163 tests in 20 suites passed after 25.503 seconds`** — and essentially all of it is one suite, `CombinedTuningReplayTests` at 25.50 s, against `FoundationModelClassifierTests` at **3.25 s for 107 tests** and every other suite at 2.2–4.3 s. None of the 163 appears in the 121-entry gate baseline, and the two triaged merge-gate logs of 2026-08-13 contain **zero** failures from them — the pre-o89d one contains exactly one, `benchmark gate`. Swept across **39 qualifying full-plan logs** (>11,000 started, non-selective, <100 lost verdicts, deduplicated by `(start count, failure set)`, parsed by `gate_baseline.parse_run`, walking `/private/tmp`, `$TMPDIR`, `/Users/dabrams/playhead`, `/Users/dabrams/.claude` and `/Users/dabrams/playhead-gate-artifacts`) the population's whole recorded failure history is **`benchmark gate` and one branch**:

- **`benchmark gate` failed 37, passed 0, and did not run 2** — the two are the post-o89d logs where the `PerfGate` holds. So it is not "32 of 40", it is **every single run in which it was reachable**, which is the stronger statement and the one the source comment already makes.
- **Five other names, all in two logs, and both logs are `.worktrees/mn5e`** — `mn5e-r2/fullgate.log` and `fullgate2.log`, the same branch in the same review round. `playhead-mn5e` is *the trust-scoring change* ("the trust counter counts EPISODES, and the auto rung opens only on rediff-grade evidence", 50dad4b5), and the names are `Sigmoid calibration…`, `Corpus aggregate quality…`, `Trust promotion speed…` (`CombinedTuningReplayTests`) plus `Full trust lifecycle…` (`TrustScoringLifecycleTests`). Two of the five are **pre-rename spellings**: 50dad4b5 renamed `Trust promotion speed: bonus=0.10 reaches auto mode within expected observations` → `…reaches manual fast, and auto not at all` and `Full trust lifecycle: shadow -> manual -> auto with observations` → `…the closed auto rung, then demotion`, which is why the two runs' failing sets differ — one is before that commit and one after.

**An earlier version of this paragraph read those two runs as "a ~5 % assertion flake … a NEW failure on roughly one merge gate in twenty", and that is the standing defect class again** — a quantity that names one thing (one branch, mid-flight, on tests it was deliberately rewriting) read as though it named another (a per-run flake rate over any future tree). It is not a flake and there is no rate: it is `the residual is the diff`, the case this file already documents under leave-one-out, and it is the population **doing its job** — the trust tests caught a semantic change on the branch that made it, and that branch then updated them. It also under-counted: four names in two suites, not three in one, and the two runs carried 101 and 88 failures, not "68–72". So the honest figure for the 163 really is **zero standing failures** once the already-`PerfGate`d benchmark is set aside, which strengthens the case for keeping them rather than weakening it. Their assertions are load-INDEPENDENT — the two "benchmark" suites assert recall and coverage deltas, never a clock; the only `Date()` reads are printed for eyeballing. So honouring a four-month-old skip would have removed 163 green, cheap, load-insensitive tests — including the detection-quality regression guards (`RealEpisodeBenchmarkTests` asserts six recall/coverage deltas at ≥ −0.05, of which **four bite and two cannot fail**: their baseline is 0.0 and both are non-negative rates — playhead-5do6) and the prompt-injection sanitization tests — from what is now the merge gate, in exchange for ~4 % of its wall clock. **The plan now says what it has actually done since April:** run everything except the twelve XCTest classes listed above. That is what the section heading always *meant*; it is not what it said, which claimed twenty — see the phase-close paragraph, corrected in the same round.

Two things that follow, so nobody re-derives them:

- **`playhead-06t7` is unblocked with the load UNCHANGED.** The fast plan's population is byte-identical before and after this change; the 163 were always running. Measure the load-sensitive population against today's plan — there is no reduction coming.
- **If you ever do need a Swift Testing suite out of a plan, the plan file cannot do it and `-skip-testing:` in `fast-gate.sh` must not.** A `-skip-testing:` argument sets `SELECTIVE=1`, which makes the gate skip the baseline diff **on every run** — it would silence the very mechanism the gate exists for. Gate it in SOURCE with an env-var trait: `PerfGate` (`PlayheadTests/Helpers/PerfGate.swift`) is the worked example, it works uniformly across XCTest and Swift Testing, and `benchmarkGate` is a live instance of it. Note the direction hazard before you copy it: `PerfGate` is opt-IN, and that shape already cost this repo six weeks of silently-unrun tests when the one pass that set the variable could not start.

`TestPlanSkipListCanaryTests` (in `PlayheadTests/Design/`) now fails the build's test phase if any plan's `skippedTests`/`selectedTests` names anything that is not a class reaching `XCTestCase` — transitively, since eight of the twelve honoured entries subclass `InterruptionCycleSuiteBase` rather than `XCTestCase` directly. It also pins the claim two paragraphs up that `PlayheadIntegrationTests` is a true superset, which nothing checked: a skip that existed only in the superset plan would leave those tests running in no plan at all.

The `PlayheadFastTests` plan is the default in Xcode (Cmd+U).

**Load-sensitive measurement tests (`scripts/perf-tests.sh`):** Latency/timing tests — `MainActorFreedomTests`, `PlayheadRuntimeLaunchPerfTests`, and the cancel-mid-decode scheduler tests — assert absolute wall-clock budgets that only hold on a quiescent CPU. The parallel FastTests suite (~7,900 tests) saturates the machine and makes them flake, so they are gated (`PerfGate`, opt-in via `PLAYHEAD_RUN_PERF=1`) to **skip** in FastTests/IntegrationTests and run **only** through the dedicated serial pass:
```bash
scripts/perf-tests.sh    # PlayheadPerfTests plan, parallelism off, measurement tests only
```
When adding a new load-sensitive test, gate it with `PerfGate` in the source **and** add it to the `MEASUREMENT_TESTS` list in the script. See playhead-zx0l.

**Run the pass after gating anything, and read its output — it was unrunnable for six weeks and nobody noticed (playhead-o89d).** The script's default destination was `iPhone 17 Pro, OS=27.0`, a device that does not exist on this box, so `xcodebuild` exited with `Unable to find a device matching the provided destination specifier` before running one test. That is not a typo-grade bug: this script is the ONLY place a `PerfGate`d test runs, so while it could not start, "gated" and "deleted" were the same thing for every test admitted since playhead-zx0l. Fixed to match `fast-gate.sh` (`name=iPhone 17`).

**READ BOTH FORMATS HERE TOO — `Test run with 13 tests in 12 suites passed` is the SWIFT TESTING summary, not the pass's verdict (playhead-o89d review).** This pass runs XCTest suites as well, and they report in the other format entirely; a reader who stops at the Swift Testing line sees a green pass while `** TEST FAILED **` sits at the bottom. Measured over two consecutive runs on a quiet box, 2026-08-13: the 13 Swift Testing tests passed both times, and **two XCTest tests failed both times** — `PlayheadRuntimeLaunchPerfTests.testInitFitsLaunchBudget` (init median 407 ms against a 250 ms budget) and `PlayheadRuntimeMainActorFreedomTests.testMainActorIsNotHeldDuringRuntimeInit`, which on one run took the **test host down** with `Fatal error: Index out of range`, after which xcodebuild restarted and printed `Test Suite 'PlayheadRuntimeMainActorFreedomTests' passed … Executed 0 tests`. Both are `PerfGate`d, so both had been unrun for six weeks and are newly visible rather than new — filed as **playhead-1och**, which triaged them and is summarised in the next paragraph. `AdmissionControllerTests` (`elapsed < 0.1`, playhead-0vju) tripped by 617 µs on the run that first fixed the destination and **PASSED 2/2 at review** at 0.022 s, so it is the marginal one, not the standing failure this pass is red for.

**THE PASS WAS RED FOR ONE PRODUCT DEFECT, NOT TWO FLAKY TESTS — AND IT IS GREEN NOW (playhead-1och diagnosed it, playhead-xul6 fixed it, 2026-08-13).** Both XCTest failures were `CapabilitiesService()` on the main actor; the fix took the `SystemLanguageModel` read out of the synchronous launch snapshot entirely. Measured over three consecutive serial passes on the fixed tree: `MainActorFreedom` **max = 6.5 / 3.4 / 2.9 ms** against the unchanged 100 ms budget (it had failed 7 of 7), and `LaunchPerf` **median = 2.0 / 2.3 / 2.1 ms** against the unchanged 250 ms budget. **No threshold was moved.** The paragraphs below are kept as the diagnosis record — read them in the past tense. The crash is gone: the count guard in `MainActorFreedomTests` was a non-fatal `XCTAssertGreaterThan` and the next line subscripted the empty post-warmup array, so a probe that had correctly detected it measured nothing walked off the end of its own array, killed the host, and let the restarted host print `Executed 0 tests`. It is a `guard … else { XCTFail(…); return }` now, and each hop records WHEN it was submitted as well as how long it took — a bare sample count cannot distinguish a starved probe from a held main actor, and the timeline can.

Both remaining failures are **`CapabilitiesService()`, one line of `PlayheadRuntime.init`, holding the main actor 0.47–2.13 s** (filed as **playhead-xul6**, P1). Per-section timing of the init showed `CapabilitiesService` at 473–2130 ms while *every other section of a 1,086-statement init is single-digit to tens of ms*; the probe agrees from the other side (`median=0.0ms max=557.7ms samples=304`, one hop spanning the whole init body and 304 completing in ~0 ms). It is `CapabilitiesService.init` → `captureSnapshot()` → `checkFoundationModelsState()` → `SystemLanguageModel.default` / `.contextSize`, and the repeated **2003 ms** readings are a 2-second timeout, not work. Note where it hid: `PlayheadRuntimeInitLaunchPathSourceCanaryTests` bans `SystemLanguageModel(` **in init's own body text** and PASSED — the construction lived one call deeper, in a service whose initialiser looks free at the call site. Same shape as every other canary this repo has out-spelled, plus a second blindness nobody had noticed: the pattern requires a `(`, so `let model = SystemLanguageModel.default` written **directly in init's body** would also have passed it. **playhead-xul6 added the transitive rail** — `testInitSynchronousCallGraphIsFreeOfFoundationModels` walks the call graph (`SwiftSourceCallGraph`), following only edges that run SYNCHRONOUSLY from init and treating a closure LITERAL as the value it is, so `Task { }` and `PermissiveClassifierBox { }` are invisible with **no allowlist** to grow. It indexes 470 files / ~12 MB in ~1.0 s. Re-introducing the defect at its original depth makes it report both paths by name while the text canary above still passes — that is the mutation proof, and the reason the two tests are not redundant. **Read a green result as "no CALL path", not as "no touch" (review round 1).** Two idiomatic shapes get past it and are named as limits L-6 and L-7 in `SwiftSourceCallGraph`: a member ACCESS that runs a getter (`_ = Self.someComputedProperty` — `callees` emits an edge only for `name(`, and a computed property's body is never indexed at all) and a **type-scope property initialiser** (`private let x = Foo.expensive()`, which runs inside every `init` of that type — i.e. exactly where this bead's defect lived). Both were re-introduced into `CapabilitiesService` as real main-actor `SystemLanguageModel` reads at review and the canary passed on both. They are documented rather than closed because both cures cost the property that makes the walker worth reading — that every path it reports is a real one: scanning type-scope text reads TYPE ANNOTATIONS as executed code (`private let model: SystemLanguageModel` in `PermissiveAdClassifier`, `private let session: LanguageModelSession` in `FoundationModelClassifier`), and telling an annotation from an expression is parsing rather than scanning.

**Which of the two you see red depends on the run, and that is the assertions' shapes, not noise.** `MainActorFreedomTests` asserts on the MAX hop and has failed 7 of 7 observations — its mildest reading is `max=138.0ms init body=138.0ms`, still over the 100 ms budget. `LaunchPerfTests` asserts on the MEDIAN of 5 constructions against 250 ms, so it only trips when at least three of the five hit the probe: 2 PASS / 4 FAIL observed, `median=170.1ms samples=[710.4, 478.6, 168.5, 170.1, 108.3]` on one of the passes. A green `LaunchPerf` is not evidence the hold is gone; read the `[MainActorFreedom]` line, which now prints the init-body duration and where the worst hop sits relative to it. Post-fix the two agree: `max=6.5/3.4/2.9 ms` with the init body itself at 6.5/3.3/2.9 ms, i.e. the worst hop IS the whole init body and the whole init body is now under 7 ms.

**And the cost of gating them is now a measured number rather than a worry.** The regression is bracketed to one commit: `b0a6cc58` (2026-07-04 19:04) added `let contextSize = model.contextSize` to that snapshot, and its direct parent `8e610c74` (2026-07-04 06:15) — the commit that made both tests `PerfGate` opt-in and shipped `perf-tests.sh` pointed at the destination that does not exist — records in its own message `Perf pass: 7/7 pass (MainActorFreedom max=11ms vs 100ms budget)`. **11 ms before, 138–2008 ms after, thirteen hours apart.** The two tests that exist precisely to catch a synchronous FoundationModels construction on the launch path were made opt-in hours before the thing they guard against landed, into a pass that could not start, and it held for six weeks.

**The 250 ms budget was deliberately NOT widened, and the measurement is why.** On the constructions that miss the probe, init reads 57.0 / 60.7 / 62.8 / 67.6 ms — the budget is met with 4× headroom and matches the "~50–80 ms on simulator" its own comment claims. The floor is not the problem; an intermittent 2 s block is. Widening to 2.5 s would have deleted the only two tests that can see it. **Everything above is the SIMULATOR** (FoundationModels is unavailable there, which is what the timeout is waiting out); **nobody has measured a device since this call landed, and that is still true after the fix** — playhead-jndk's precedent is a multi-minute device freeze from the same API, so do not assume the device is fine. What playhead-xul6 changes is that the cost, whatever it is, is no longer paid on the main actor: the read now runs on the capabilities actor's own executor (an `await`, i.e. a suspension) or, for `contextSize`, only when Settings asks. The fix is correct for either device answer; the device NUMBER remains unmeasured.

**What earns a place here, measured rather than argued (playhead-o89d).** Not "asserts a wall-clock budget" — 774 tests are subject to some absolute deadline and the great majority are fine. The sub-property that does not rotate with the box's load is **the wall-clock quantity IS the assertion**: a clock read plus an expectation comparing a derived value to a constant. Enumerated mechanically that is 16 tests, of which 11 are CPU-bound and measured to PASS under the full plan. **The "exactly 2 of them are load-sensitive" figure was one short**, found at review: `ExpiredWindowAttemptAccountingTests.settleWaitDoesNotLie` reads `ContinuousClock.now` and asserts the elapsed span is `< .seconds(2)` to separate "returns AT ONCE" from "burned the whole 5 s budget" — the same shape as the 20 s / 60 s bound that WAS gated, with a tighter margin, in the same file, and it is in the committed baseline as an `assertion` failure. It is **left un-gated deliberately**: moving it now would make its baseline entry read `DID NOT RUN` and force a second `--accept-baseline` in one branch. Filed as **playhead-3f79**, and deliberately sequenced BEHIND **playhead-1och**: that pass was red on two XCTest tests, so a test moved here would have landed where its result is not read — the same "gated and deleted were the same thing" defect this section exists to record, one turn later. **playhead-xul6 cleared that block**: the serial pass is green as of 2026-08-13, so 3f79 and playhead-06t7 can proceed. Everything else on the load-sensitive list — **269 names observed over 38 full-plan runs, of which 168 recur and cover ~97 % of any single run** (see the baseline section) — is a **coverage decision, not a measurement one**; see playhead-o89d before adding any of it here. Say which population you mean when you say it has been measured: **184 of the 269 have a recorded isolated result and 183 pass 5/5; the other 85 have none**, and 66 of those are singletons. **And 93 of those 269 have only ever failed an EXPECTATION, never a time limit** — do not read the union as a PerfGate shortlist; the kind split in the baseline section is what bounds it.

## Parallelism Ceiling

**Maximum 2 concurrent subagents running `xcodebuild` at any time on this machine (16 GB RAM).** Each parallel build can spike 1–3 GB during Swift compilation; combined with Xcode GUI, simulator, sourcekit indexers, and Claude itself, going past 2 has historically OOM'd Xcode (2026-04-17 incident). When orchestrating waves of beads, queue rather than fan out beyond 2. Sequential is always safe.

## Disk Hygiene

Each bead worktree runs `xcodebuild -derivedDataPath .derivedData`, producing ~2 GB of cache. Cleanup must be deliberate — a missed step leaves orphan gigabytes. Real paths are `.worktrees/<slug>/.derivedData` (depth 3, camelCase).

### Canonical bead-close sequence

Run from the repo root after PR merge. Substitute `<slug>` (e.g. `bd-r835`) and `<branch>` (e.g. `bead/playhead-r835`).

```bash
cd /Users/dabrams/playhead && git checkout main
git pull --ff-only || git pull --rebase   # see the divergence trap below
BR=<branch>; WT=/Users/dabrams/playhead/.worktrees/<slug>; PR=<pr-number>
# Merged-ness. PR state is the AUTHORITY on this repo because PRs are SQUASH-merged.
[ "$(gh pr view "$PR" --json state -q .state)" = MERGED ] || { echo "NOT MERGED — abort"; exit 1; }
[ -z "$(git -C "$WT" status --porcelain)" ] || { echo "DIRTY — stash or commit first"; exit 1; }
bd close playhead-<slug>
git worktree remove "$WT"
[ -d "$WT/.derivedData" ] && rm -rf "$WT/.derivedData" && echo "removed $WT/.derivedData"
git worktree prune -v
git push origin --delete "$BR"
git branch -D "$BR"   # -D is CORRECT after a squash merge; see below
```

**The squash-merge trap (hit on 2026-07-28 closing playhead-8m2w).** This repo squash-merges PRs, so
the branch tip is **never** an ancestor of `main` — the squash is a brand-new commit with a different
hash and no parent link to the branch. Two consequences that make the older sequence unusable:

- `git merge-base --is-ancestor "$BR" origin/main` **always reports NOT MERGED** after a squash, for
  branches that are fully merged. Use the PR's state instead; that is the only thing that actually
  knows a squash happened.
- `git branch -d` **always refuses** for the same reason, and no amount of deleting the remote first
  will help — the refusal is not about the remote lagging, it is about the commits genuinely not
  being reachable from `main`. So `-D` is correct **here**, and the PR-state check above is what
  replaces `-d`'s refusal as the safety net. Do not drop that check: without it `-D` is a real
  footgun. (If a future PR is merged with a true merge commit rather than squashed, `-d` will
  succeed on its own — prefer it when it does.)

**The diverged-main trap (same close).** `git pull --ff-only` aborts with "Not possible to
fast-forward" whenever local `main` carries a commit that was never pushed — easy to accumulate,
since main-branch commits (docs, scripts, CLAUDE.md edits) do not go through a bead worktree. Inspect
with `git log --oneline origin/main..main` and `main..origin/main` before doing anything. If the
divergence is your own unpushed commits and the tree is clean, `git pull --rebase` replays them on
top of the merge; then push. Do **not** reach for `--force` or reset — check what diverged first.

Two further traps this sequence exists to avoid (both hit on 2026-07-26):

- **Don't test merged-ness with `git branch --merged main | grep -qx "  <branch>"`.** A branch checked
  out in a linked worktree is listed as `+ bead/foo`, not `  bead/foo` — and at close time every bead
  branch is in a worktree, so that guard reports "NOT MERGED" for branches that are merged, every time.
  `git merge-base --is-ancestor` asks the real question and is indifferent to the listing prefix.
  Compare against `origin/main`, not `main`, so you also prove the merge was actually pushed.
- **`git branch -d` refuses while the remote branch still predates the merge**, with a confusing "not
  fully merged" error even though it is merged to HEAD. Delete the remote branch first and `-d`
  succeeds. This is a *different* cause from the squash-merge refusal above, and the remedies differ:
  here `-d` genuinely succeeds once the remote is gone, so use it; after a squash it never will.
  Either way the PR-state check is what authorises the delete — never let `-D` stand alone.

### Safety rails

- Before `rm -rf`: path must start with `/Users/dabrams/playhead/.worktrees/`, `/private/tmp/playhead-`, or `$TMPDIR/Deleting-` (CoreSimulator's own trash — see below), and must NOT appear in `git worktree list --porcelain`.
- Never pass `--force` to `git worktree remove` without explicit user approval — the refusal is the safety net.
- Echo what was removed so the transcript audits the session.

### The gate REFUSES to start below 13.5 GiB free (playhead-3nfa)

**You no longer have to remember to check.** `scripts/fast-gate.sh` runs `scripts/disk_preflight.py` before lint, before the xcodegen bootstrap, before xcodebuild — and if the volume is short it prints a loud refusal naming the remedy and **exits 28** (POSIX `ENOSPC`, the error this would otherwise have hidden). Nothing is deleted; the refusal only *reports* what `scripts/disk-cleanup.sh` could reclaim.

```bash
scripts/fast-gate.sh                  # refuses under 13.5 GiB, exit 28
scripts/fast-gate.sh --reclaim-disk   # run the cleaner ONCE, re-check, then proceed or refuse
PLAYHEAD_DISK_MIN_GIB=9 scripts/fast-gate.sh    # a different threshold, deliberately
```

This exists because the failure mode is not a failure. A gate that runs out of room **wedges**: xcodebuild stays alive with zero output and never exits, having failed to write its result bundle. No POSIX 28, no non-zero exit, nothing in the log — indistinguishable from a slow test run. This box hit 100 % capacity four times on 2026-08-01 and every one was caught by somebody happening to look.

**Where 13.5 comes from — measured 2026-08-02, two full runs to a terminal verdict**, sampling `df -k` on `/System/Volumes/Data` every 5 s with `scripts/gate-disk-sample.sh`. The quantity is the **drawdown** (free at start minus the minimum during the run), because a gate is a transient event and the end-state delta understates it by more than half:

| run | conditions | start | min | end | drawdown |
|---|---|---|---|---|---|
| 1 | fresh worktree, cache from empty, sim already at 7.88 GiB | 15.07 | **3.58** | 11.39 | **11.49 GiB** |
| 2 | cache warm, sim freshly erased to ~0 | 19.08 | **6.93** | 8.86 | **12.15 GiB** |

`13.5 = 12.15 (worst observed) + 1.35`, and the margin is measured too: the largest fall inside a single 5 s sampling gap was 1.27 GiB, so 1.35 covers what the sampler cannot see. **The old "~8 GB" figure was well under what a full plan actually draws** — do not restore it without re-measuring.

Three things that will bite whoever re-measures:

- **A cold/warm split was tried and the data rejected it.** The obvious refinement is a cheaper threshold when `.derivedData/Build` exists, since a fresh worktree must create ~2.8 GiB of cache. Run 2 was the warm one and drew down **more**. Simulator state moves by ~8 GiB depending on whether the destination was recently erased, and it swamps the cache saving. Two variables, two runs, neither isolated.
- **`df` Avail on APFS lags on the way back up.** Run 2 read 8.86 GiB free the moment it exited and 16.00 GiB two minutes later. The *minimum* is still the number to use — it is the same metric by which this box "hit 100 % capacity" — but never compute what a run keeps from a reading taken at exit.
- **The threshold applies to selective runs too** (`-only-testing:`, i.e. how `mutation-battery.sh` drives the gate). A selective run costs less, but how much less was not measured, so the conservative number governs. If that bites, `--reclaim-disk` or `PLAYHEAD_DISK_MIN_GIB` are the answers.

`PLAYHEAD_SKIP_DISK_PREFLIGHT=1` bypasses the check entirely. It is deliberately **not** printed in the refusal, for the same reason as `PLAYHEAD_SKIP_BASELINE`: an override quoted in the failure message stops being an override and becomes the documented workaround.

A wedged run is also the case the baseline check is built to survive: it printed `CANNOT EVALUATE — the log is incomplete` and passed xcodebuild's own exit through (143), rather than reading ~9,900 unfinished tests as a clean sweep.

### `simctl erase` can report success and free nothing (playhead-cgka)

**Measured 2026-08-02, and it is why this box kept arriving at ~8 GiB of headroom on a 228 GB disk.** `simctl erase` does not delete a device's data in place: CoreSimulator *moves* it to `$TMPDIR/Deleting-<uuid>/` and reaps it asynchronously. If that reap hits a directory it cannot read, it dies and the bytes stay forever — the erase still reports success, and `du` on the device directory still shows the space as freed.

The test suite creates exactly such a directory by design: `DownloadManagerTests` chmods a `complete/` directory to `0o300` to prove a permission failure is handled, and restores it in a `defer` that an abnormal exit skips. Seven orphaned devices had accumulated **15 GiB**, every one of them stuck on a single unreadable `PlayheadTestScratch/…/complete`. Clearing them took the volume from 9.0 GiB to 25.4 GiB free.

```bash
du -sk "$TMPDIR"Deleting-* 2>/dev/null | sort -rn      # what is stranded
chmod -R u+rwx "$TMPDIR"Deleting-*                     # u+w is NOT enough
rm -rf "$TMPDIR"Deleting-*
```

**`chmod -R u+w` does not work here and looks like it should.** `0o300` is `-wx------`: write is already granted; READ is what an enumerator needs to walk the directory, so `rm -rf` fails with EACCES on precisely the directories that need repairing. It is `u+rwx` or nothing. (The suite's own wipe now goes through `TestScratchReaper.forceRemove`, which repairs permissions before giving up, so new instances should not accumulate — but devices stranded before that landed are still out there.)

Check `$TMPDIR/Deleting-*` **before** concluding the box is out of disk. It is the largest single reservoir on this machine and it is invisible to every `du` of the worktree, the derivedData or the simulator.

### Orphan sweep script

`scripts/disk-cleanup.sh` runs weekly via cron. Safe to run manually:

```bash
scripts/disk-cleanup.sh --dry-run   # preview
scripts/disk-cleanup.sh             # actually clean
```

It removes `.worktrees/<slug>/.derivedData` whose worktree is no longer registered, and stale `/private/tmp/playhead-*` dirs older than 3 days that are not active worktrees. Logs to `.logs/disk-cleanup.log`.

**This is the only cleaner in the repo — extend it, don't write a second one.** `scripts/disk_preflight.py` measures and refuses but delegates every removal here, so there is one set of safety rails rather than two that drift. playhead-3nfa added two more classes and one guard:

- **Superseded `.xcresult` bundles** (~93 MB each, measured). The most recent per worktree is always kept — it is what you open after a failure — and only strictly older ones go. `.derivedData/Build` is untouched.
- **`$TMPDIR/Deleting-*`**, the stranded CoreSimulator trash. Every removal `chmod -R u+rwx` first; see the `simctl erase` section above for why `u+w` looks right and does nothing.
- **A live-build guard.** Working directories of running `xcodebuild`/`swift-frontend` are resolved with `lsof -a -p <pid> -d cwd` and anything under them is skipped, so a sweep during a long run cannot destroy it. Resolution uses **`pgrep -x`, never `pgrep -f`** — `-f` matches the full argv and self-matches any shell whose command line merely contains the string, which on 2026-08-01 killed a healthy gate and then raised a phantom second-build alarm.

Verify with `python3 -m unittest scripts.tests.test_disk_preflight` (47 tests, under a second, no build) and `scripts/mutation-battery-disk-preflight.py` (P series, 16 rails + a vacuity control).
