# Playhead test-harness record

**What this is.** On 2026-09-02 the project `CLAUDE.md` had grown to 176 KB / 747 lines
(commit `f0757bc1`): the whole measurement history of the test harness — tables, review-round
histories, the readings that were wrong and how they were corrected — loaded into every session.
The rules stayed in `CLAUDE.md`. The record behind them moved here **verbatim**, original
headings included, so nothing measured was lost and `git log -p CLAUDE.md` before that date still
lines up with this text.

**How to use it.** Each rule in `CLAUDE.md` cites the bead that owns it. Search this file for
that bead id before arguing with the rule. Put new measurement narrative here (or in
`docs/investigations/`), and put in `CLAUDE.md` only what an agent must know to act.

**Two sections, as they stood:** `## Testing` (lint, the test plans, the gate baseline and its
census, the mutation battery, memory, the simulator trim, the full-plan policy, PerfGate) and
`## Disk Hygiene` (the bead-close sequence, the disk preflight, `simctl erase`, the cleaner).

---

## Testing

### Lint first (`scripts/lint.sh`) — playhead-ia2s

**Run this before the test gate. It takes ~0.2s warm / ~2.4s cold and needs no build.** SwiftLint was installed on this box all along but the repo had no `.swiftlint.yml`, so for months "lint" was a no-op step in every bead gate and every review round — style, dead-code and complexity regressions landed unreviewed, and human reviewers burned rounds on things a linter catches mechanically.

```bash
scripts/lint.sh                    # whole repo, strict — the gate
scripts/lint.sh --changed          # only Swift files changed vs the merge-base
scripts/lint.sh --lenient          # report violations but exit 0
scripts/lint.sh --fix <paths>      # autocorrect; EXPLICIT PATHS ONLY, refuses repo-wide
```

`scripts/fast-gate.sh` runs it automatically before building, so a violation stops you in 2 seconds instead of after a 3-minute test run. Bypass with `PLAYHEAD_SKIP_LINT=1`.

**Warnings vs errors — the policy.** Every rule in `.swiftlint.yml` is `warning` severity, and `scripts/lint.sh` adds `--strict` to turn warnings into a non-zero exit. The split is deliberate:

- Bare `swiftlint lint` — what Xcode and editor integrations run — exits 0 and paints yellow. **Lint never blocks an unrelated build.**
- `scripts/lint.sh` is the gate and is zero-tolerance. It can afford to be: the baseline is **0 violations, measured**, so any failure is in your own diff.

A rule is promoted `warning` → `error` (so it bites even without `--strict`, i.e. inside Xcode) only when it has held at zero for a full release cycle **and** its violations are always genuine defects, never taste. Style rules are never promoted — a style rule that hard-errors in the IDE is how linters get uninstalled.

**The baseline is green and must stay green.** `.swiftlint.yml` was built by measurement, not taste: every candidate rule was run against the whole tree and admitted only at a violation count of exactly zero. 84 rules are enabled; nothing was reformatted and `swiftlint --fix` was never run repo-wide. Rules that fire today are **not** disabled quietly — they are listed at the bottom of `.swiftlint.yml` with their measured counts, tiered as a follow-up roadmap (Tier D is the high-value shortlist: `variable_shadowing` 144, `unused_parameter` 401, `async_without_await` 396, `force_unwrapping` 389, and `identical_operands` 3 which is probably three real defects).

**Two things not to do.** Do not run `swiftlint --fix` across the repo to adopt a new rule — a ~1,100-file reformat destroys `git blame` and collides with every open bead branch; `scripts/lint.sh --fix` refuses without explicit paths for exactly this reason. And do not add a rule to `only_rules` without measuring it first: one red rule and everyone learns to route around the gate, which is strictly worse than having no linter.

**`scripts/lint.sh` also runs the SHAPE-2 preflight (`scripts/singleton_slot_preflight.py`, playhead-mfeq).** It bans an optional stored `var` named `current*`/`pending*`/`last*` on an **`actor`** — a singleton standing for a set, which is the standing defect class's most productive shape here: `playhead-mk6z` (`currentJobId`, the last DISPATCHED job read as the running one) and `playhead-lmrx` F4/F9/R3 are four shipped defects with one rule between them. `actor` is the enforced scope because an actor is **reentrant**: every `await` is a point at which another operation enters and finds the same stored properties. It is a separate script rather than a SwiftLint `custom_rules` entry because the predicate needs the *enclosing type* of a declaration, which a per-line regex cannot see.

Three things to know before you argue with it:

- **It was measured before it was written, and the count is in the allowlist's header.** 56 matches across all type kinds on `b5e04fc6`, 47 after the scanner stopped counting function-LOCAL `var`s as stored properties (a local is per-invocation, so it is not a shared slot — `ShadowRetryObserver.lastSeen` is line 1 of a method). 8 were on actors; mfeq removed 3 and the rule shipped enabled with 7 allowlist entries and zero un-allowlisted violations. **It is 6 entries as of playhead-2kxd**, which fixed `AdDetectionService.currentPodcastProfile` and deleted the entry in the same change — the amnesty was temporary and it expired, which is the mechanism working rather than a count drifting. The ~40 non-actor matches are deliberately unenforced — measured, they are overwhelmingly genuinely singular — and `--report-all` prints them.
- **`scripts/singleton-slot-allowlist.json` is CLOSED IN BOTH DIRECTIONS.** A new match fails, and an entry that matches *nothing* also fails: a licence for a field nobody can find was renamed or moved, and whatever inherits the name inherits the amnesty. Same rule the gate baseline applies to a member that did not run. A `why` must say **what bounds the population to one**. This line used to add "and two entries are not licences at all, they name a filed bead (`playhead-2kxd`)" — **there was only ever ONE such entry**, and `git log -p` on the file will show you it never had a second; the miscount is corrected here rather than carried forward. That entry is gone: playhead-2kxd keyed the profile by show (`podcastProfilesByShowId`) and removed its own licence in the same diff. **All 6 remaining entries are real licences**, so a `why` that names a bead is now the exception and should be read as a promise somebody owes, not as a category this list routinely holds. The sibling defect 2kxd deliberately did NOT fix — `showPriors`/`scanner`, the same slot shape derived from the same profile on the same three lines — is `playhead-jjke`, and it is invisible to this rule by construction: one is non-optional and neither is named `current*`/`pending*`/`last*`. **A green preflight is evidence about the NAMES it can see, not about the shape.**
- **Verify it the way it was built:** `python3 -m unittest scripts.tests.test_singleton_slot_preflight` (30 tests, ~5 s, no build). Re-introducing a deleted slot must make `scripts/lint.sh` exit **2**; a non-optional `var` or a computed property must leave it silent. A rule that fires zero times because its pattern never matches is indistinguishable from a codebase with no violations — so prove it fires by making it fire, rather than reading a green result as evidence.

### Test plans

Two test plans exist. **Use the correct one for your context:**

**Per-bead work (implementation, review, fix cycles):**
```bash
xcodebuild test -scheme Playhead -testPlan PlayheadFastTests \
  -destination 'platform=iOS Simulator,name=iPhone 17'
```
Skips XCTest interruption-cycle integration suites. **It now takes ~46 minutes on the simulator, not ~3** — `PlayheadFastTests` is SERIALIZED (see Option A below), and the one measurement on this tree is 11,789 tests in **2,718.8 s** of Swift Testing phase / **2,809.4 s** of `Testing started completed` (playhead-vk68m, 2026-08-25). Quote that, not a multiplier: the ratios (10.6x, 7.7x) were what got quoted first and they leave a reader with no idea whether to wait or go away.

**Self-healing gate wrapper (`scripts/fast-gate.sh`, playhead-qt8y/ekpn):** prefer the wrapper over the raw command — it bootstraps a fresh worktree (links the gitignored model from the main checkout + `xcodegen generate` when the scheme is missing the plan), caps concurrent compile jobs to survive the cold-build OOM, and auto-recovers a wedged sim (`Mach error -308`):
```bash
scripts/fast-gate.sh    # PlayheadFastTests, single-host; forwards -only-testing:... etc.
# PLAYHEAD_PLAN=PlayheadIntegrationTests scripts/fast-gate.sh   # phase-close superset
```
⚠️ **Run gates ONE AT A TIME, and do NOT add `-parallel-testing-worker-count ≥2` on this box.** Two memory drivers matter here: (1) a fresh-worktree **cold build** compiles the whole project with parallel `swiftc` — heavy enough on its own to get xcodebuild **OOM-killed** (`Killed: 9`), which is why the wrapper caps `-jobs` (default 4, env `PLAYHEAD_BUILD_JOBS`); (2) running **two gates/builds at once** exhausts the 16 GB box → one is killed mid-suite (`** BUILD INTERRUPTED **`, signal 144) with **no test failure** — pure resource exhaustion. **Clone-based parallel testing is unavailable here** (playhead-ekpn): worker-count ≥2 makes Xcode spawn sim clones, and the clone helper resolves `simctl` via the *global* `xcode-select` (`/Library/Developer/CommandLineTools`, no `simctl`), so it dies with `xcrun: error: unable to find utility "simctl"` (exit 65, ~18s, zero tests run) — `DEVELOPER_DIR` fixes `xcodebuild` but not the clone helper (the 2026-07-16 gotcha; enabling real clone parallelism would need a global `xcode-select -s` change — sudo, system-wide, Dan's call). The gate runs **single-host**: XCTest serial, and — since playhead-vk68m — Swift Testing serial too (`"parallelizable": false`), so the "cheap **in-process** concurrency" this line used to advertise is GONE and the ~11,800-test bulk is no longer fast. Read the wall clock at the top of this section before planning around it. Serialization of the HOSTS + the `-jobs` cap, not in-gate clone parallelism, are the memory guardrails. **Serializing Swift Testing is not one of them** — measured on one tree it moved peak demand 13.9 → 14.2 GiB and swap 1.5 → 1.8 GiB, i.e. slightly WORSE; it is in the tree for FILE DESCRIPTORS (playhead-vk68m), and the thing that actually bought memory back is the simulator trim, B. Deferred (a coverage tradeoff for Dan's call): PerfGate-ing the load-sensitive behavioral flake families (gy2s pipeline-stall / RouteChange / Interruption / PlaybackService audio-session / playhead-7h2 runtime-shutdown) out of the default gate — those test real behaviors, so moving them is a coverage decision, not done here.

**THE BASELINE WAS A RECORD OF A THRASHING BOX, AND IT HAS BEEN REFRESHED (playhead-vary, 2026-08-21).** Everything in the paragraphs below about a gate that is "RED on a clean checkout" with ~85 known failures, and about a 269-name load-sensitive population measured over 38 runs on 2026-08-09..13, was measured while the gate ran **5-7 GiB over this box on sustained swap** (playhead-3rql, playhead-blsh). Those tests were being STARVED, not failing on their merits. With the simulator trimmed the same plan runs **11,626 tests with ZERO failures**, five consecutive times. `--accept-baseline` was run once on such a run: it did not delete anything — it recorded one passing observation, which demoted all **19 deterministic entries to load-sensitive** (`8 seen/8 failed` -> `9 seen/8 failed`) and left the 118 names in place. That is the mechanism behaving correctly: a single quiet run is not evidence a starvation flake is fixed, so the names stay until they have earned their removal. **Read the history below as the record of a machine that no longer exists in that state** — and if the numbers here start climbing again, suspect the box before the tests.

**The gate's verdict is about the DIFF, not the count (`scripts/gate_baseline.py`, playhead-voez).** The default gate is RED on a clean checkout — the load-sensitive families above blow their 60s time limits under the full run's own concurrency — so its exit code used to say nothing. The known-broken set is now committed in `scripts/gate-baseline.<plan>.json` and `fast-gate.sh` reports the difference:

```
RED (N known / 0 new)   -> exit 0
RED (N known / 2 NEW)   -> exit 65, both named
a baseline test PASSES  -> exit 65, named
```

Refresh with `scripts/fast-gate.sh --accept-baseline` and **justify the diff in the commit message** — the file is the record of what is known-broken, so a shrinking diff is good news and a growing one needs a reason. Do not reach for `PLAYHEAD_SKIP_BASELINE=1` to make a red gate quiet; that is the bypass this mechanism exists to remove.

Four things worth knowing before you argue with it:
- **The baseline is measured, not labelled.** Each entry carries how many observations it was seen in and failed in. `failed == seen` over ≥3 observations is *deterministic* and its **passing hard-fails** the gate (Dan's call: the baseline is exact, not a ceiling). Anything else is *load-sensitive*: passing is reported as a removal candidate but is not fatal, because one quiet run is not evidence a starvation flake is fixed. **How much this churns, measured 2026-08-01:** two full runs on identical code, quiet box, nothing else running gave 32 and 28 failures with only 19 in common — union 41, **Jaccard 0.46**. Fewer than half the failures recur. That is why a flat exact set is unusable here, and why promotion needs three observations rather than two. (Re-measured at o89d R3 over 703 pairs of 38 full-plan runs: median **0.505** (R2 read 0.515 over 378 pairs of 28). The churn is real and this bullet stands — but see the population paragraph below before extending it into "so the set cannot converge", which is a different claim and is false.)
- **The tolerance is not a hole, because identity includes the failure KIND.** A load-sensitive entry means "may TIME OUT", not "may fail". A known-timeout test that fails an *expectation* is reported NEW.
- **A baseline member that did not RUN fails the gate.** Renamed, deleted or newly skipped, a name nobody can reach is not evidence. This is what makes step 2 of the bead (PerfGate-ing a family) show up as a gate failure demanding a refresh, rather than silently shrinking coverage.
- **Selective runs are exempt.** `-only-testing:`/`-skip-testing:` (how `mutation-battery.sh` drives the gate) is a different population, so the check is skipped and the raw xcodebuild exit code passes through untouched.
- **A CRASHED HOST produces NO VERDICT, and the gate is ARMED on it (playhead-tl6l, armed by playhead-buvn).** A test whose host died emits no per-test line at all, so it was matched against the baseline as neither "known" nor reported as NEW — it fell out of the arithmetic in *both* directions, and the reassuring `RED (N known / 0 new)` could be printed by a run that lost a whole test family. It now reads `RED (85 known / 0 new) — 11 tests got NO VERDICT (crashed host)`, GREEN is unreachable while the count is non-zero, and `--accept-baseline` **carries those entries forward** instead of pruning them as renames. **The arm is a DIFF, not a count**, exactly like every other category, and **the record is shaped exactly like `tests`** — a **union of names carrying `seen_runs`/`lost_runs` observation counts**, with the tier falling out of the counts rather than a hand label. A name that loses its verdict and is *not* recorded is a NEW CASUALTY; a **deterministic** recorded name that reports again exits 65 (its licence has rotted — Dan's pass-direction rule, and the thing that lets a union ever shrink); a **load-sensitive** one reporting again is good news and exits 0; the count alone — or a bare host restart that cost nobody a verdict — is not fatal. Names rather than a count because substitution (eleven die, eleven different ones recover) is exactly what a count cannot see. **A union rather than a replaced set because the third run said so:** the two runs of 2026-08-12, on *different trees*, lost **the same eleven tests** (Jaccard 1.00), which is what the original replaced-set design was argued from — and the third full-plan run, on the armed code, lost **fifteen**: the same eleven again plus four that had **PASSED in both** earlier runs (Jaccard 1.00 → 0.73, nothing ever recovering). Under a replaced set an armed gate on a night like that reports four NEW casualties and exits 65 for healthy tests nobody can do anything about; under the union, accepting the three runs in order leaves the eleven at 3/3 (**deterministic**) and the four at 1/1 (**load-sensitive**). Read `1/1` as *one observation*, not *one of three* — a name accrues observations only from the accept that first records it, exactly as in `tests`. **UNRECORDED IS NOT ZERO, AND PROVISIONAL IS NEITHER** — three states, three claims: no `no_verdict` key means nobody has recorded the population, so the arm is **inert** and says so; **1–2 observations is PROVISIONAL**, where an unrecorded casualty is *named* but not fatal, because two observations do not bound a population this load-sensitive; **3+ observations is ARMED** (including a recorded *empty* census, which is a positive claim). That ladder is what let the arming land without turning main red today for the pre-existing crash `playhead-rouw` owns. Why the census gets a grace the failure baseline does not: a NEW FAILURE names a test that failed and can be triaged against your diff, while a test with **no verdict was never judged at all** and the only honest remedy is to re-run the whole plan. Blind spots, named: a test the host killed **before its start line** is in no set at all (it surfaces, if at all, as an unmatched `Failing tests:` entry, which is a LEAD and is deliberately never armed); identity is the **display name**, so same-named tests in different suites share a key; and a **rename** reads as one recovery plus one new casualty, i.e. a false RED whose remedy is the refresh a rename already demands. **The census number has been wrong three times and every correction was the same defect class** — a value that names one thing read as though it named another. 19/18 came from grepping the `Failing tests:` block's `Suite.function()` spelling against a console that prints `@Test` **display names**, which can only ever return zero. 33/15 came from requiring the literal word `seconds` when xcodebuild splices app output mid-token. 30/14 came from assuming the splice stops at the duration — **it does not**: it lands inside the name (`✔ Test "a b` + `yte-exact span…`), inside the verb (`" pa` + `ssed after 107.082 seconds.`), and once inside the word `Test` itself (`✔ Tes` + `t "watermark…`), because the intrusion carries its own newline and splits one logical line into two physical ones. 18 such lines on main and 3 on mn5e, **every one a passing test**. No pattern over `Test "` can see them; `gate_baseline.py` **rejoins** the halves before parsing, which is what makes the number **11 and 11**. **READ THAT AS THE HISTORY OF ONE FIX AND NOT AS THE CURRENT CONTRACT.** As written it says the name splice is handled, and playhead-phn3 was filed because it is not: the rejoin reached line N+1 and nothing else, which is 90.2 % of the shape and not all of it. What it says now is below. Two things not to misread: the census only works because **the 30 XCTest PerfGate skips are parsed as a third outcome** (Swift Testing's own skips contribute nothing — a trait-disabled test never emits a `started` line); and the direction that has never yet been hit is the dangerous one — 88 fail lines against 10,910 pass lines is the only reason it was passes that got severed, and a severed FAIL is a **lost failure**.

**THE CENSUS NO LONGER READS THE CONSOLE, AND THE PARAGRAPH ABOVE IS THE RECORD OF WHY IT HAD TO STOP (playhead-t53a, 2026-08-15).** Read the previous bullet's last third — the three corrections, 19/18, 33/15, 30/14 — as a history rather than a method. There was a fourth, and it is the one that ends the series: main's 06:01 full-plan run reported **seven tests with NO VERDICT (crashed host)** while carrying `host_restarts: 0` and not one crash marker in 9.9 MiB. The `.xcresult` for that same run records **all seven as `Passed`**. Three shapes, none of which any pattern over `Test "` could see **then**: a cut **mid-word** (`passed afte` + an app-log line), a cut **inside a UTF-8 codepoint** (the `✔` reduced to `\224`), and a verdict line **absent from the console entirely**. Two corrections to that sentence, both from playhead-phn3 below: the console reads the first two now, and `\224` is **not a bare continuation byte** — it is the four ASCII characters `\`, `2`, `2`, `4`, xcodebuild octal-escaping a token it could not decode, and reading the rendering as the thing rendered is the very defect class this paragraph is about. The instrument built to catch "a value that names one thing read as though it named another" was the thing exhibiting it.

**Measured across 68 preserved full-plan logs before the fix, and the split is the number this bead exists to establish:**

| | runs | lost verdicts | recoverable from the bytes | no trace at all |
|---|---|---|---|---|
| `host_restarts == 0` | 27 | 87 | **80 (92 %)** | 7 (8 %) |
| `host_restarts > 0` | 41 | 2,743 | 21 (0.8 %) | **2,722 (99.2 %)** |

So the census was **right about crashes and wrong about everything else**, and its false positives sat exactly where a run was otherwise clean. Not one of the 27 crash-free runs had a crash. The seven remaining no-trace names on those runs are unproven either way from the console — and the one of them for which a bundle survives (06:01's `tier validation resets invalid ascending order to defaults`) **passed**. One of the other six is not even a real test: an app `print()` with no os_log prefix was spliced into a *start* line and fabricated a name (`.enabled bunarl.normalizer: episodeId=… ⇒ context behaves as today`) that no verdict could ever match — a permanent phantom casualty, and a fifth splice shape, which is the argument against a fifth regex.

**What changed.** `scripts/fast-gate.sh` now passes `-resultBundlePath` and hands the bundle to `gate_baseline.py --xcresult`, which takes **every outcome** — passed, failed, skipped, and the failure KIND — from the bundle. Verified on the 06:01 run: the bundle's key set and the console's `ran | skipped | started` are the **same 12,085 names, zero unmatched in either direction**. Four things about the design, because each is a place it could have gone quiet instead of truthful:

- **The console keeps the STARTED roster, unioned rather than replaced.** A test the infrastructure never mentioned is still a casualty if its `◇ … started` line survived. Neither source can silence the other — a name leaves the census only by being *judged by the bundle*.
- **A DEAD HOST IS NOT A FAILING TEST, and the bundle says which in words.** Measured with a deliberate probe (a five-test suite, one calling `fatalError`): xcodebuild restarted the host, the restarted run executed `0 tests`, the console lost four verdicts — and the bundle recorded all four as `Failed` carrying `Test crashed with signal trap.`, identifiers intact. Taken at face value that would book three *healthy* tests as failures, and one `--accept-baseline` would write them into the known-broken file where their next genuine failure reads as known. So a crash-message failure is routed to the **census**, whose remedy ("run it again") is the right one. The census stops being *inferred from silence* and becomes something the bundle *states*. An unrecognised crash wording stays a FAILURE — the loud direction.
- **The residual re-run.** Whatever is left without a verdict is re-run scoped (`gate_baseline.py residual` prints the `-only-testing:` arguments; the bundle is what makes a display-named test addressable at all), one pass, full-plan runs only, capped at 120. On the probe: re-running A/B/C recovered all three and the deterministic killer **stayed a casualty**. The loop cannot launder a real crash.
- **`Failing tests:` stops being only a LEAD.** The block spells a test `Suite.function()` and the console prints its `@Test` display name, which is why the two could never be compared. The bundle carries both, so the match is exact — three "matched no console result" entries on the 06:01 run become three matches, and they are that run's three real failures. A blamed name no bundle knows is still reported unmatched.

**THE CONSOLE PATH IS STILL LOAD-BEARING AND IT HAD TWO MORE HOLES — the fifth correction, and the first one whose fix is CHEAP (playhead-phn3, 2026-08-18).** t53a routed the VERDICT to the bundle and left the console owning the STARTED roster, so "the console cannot read this" stopped being a reason to fix it. It is still a reason: the two sources are UNIONED, a test the console never mentions is one the bundle alone can silence, and the arm exists precisely so a run that lost a family cannot print a green. Two defects, both found on playhead-xul6's merge gate — `** TEST SUCCEEDED **`, 11,011 tests, zero failures, `host_restarts: 0`, and **four tests reported NO VERDICT (crashed host)**. All four passed.

- **The weld only ever reached line N+1.** The intrusion carries its own newline *and the app keeps writing*, so the displaced tail lands however many app-log lines later. **Measured over 136 preserved full-plan logs, 2,563 welds: span 1: 2312 · 2: 203 · 3: 33 · 4: 9 · 5: 4 · 6: 2.** The shipped bound recovered 90.2 % and left 251 records reading as silence. The scan now walks forward while — and only while — the lines in between ARE app output (timestamp and process at column 0), and stops dead at a record that stands alone, at anything unattributable, and at `_MAX_SPLICE_SPAN = 8` (observed max 6, plus a stated margin). The app-output-only rule is the safety property; the bound is the backstop.
- **The marker glyph itself gets severed, and what survives is an OCTAL ESCAPE.** Every glyph is three bytes and the cut lands on a byte, so xcodebuild escapes what it cannot decode *per damaged token* and the verdict arrives spelled `\224 Test "…"`, `\234\224 Test "…"` or `\342\227\207 Test "…"`. **92 such lines over 138 preserved logs, in all three spellings, and ZERO raw continuation bytes** — the raw em dash on the same physical line survives intact, which is how one can tell. **The bead's own text said the line "starts with a bare continuation byte 0x94"; the bytes on disk are `5c 32 32 34`, the four ASCII characters `\224`** — the standing defect class one more time, a rendering read as the thing it renders, and a byte-level repair pass would have fixed a spelling this box does not produce. The fix admits the wreckage as an alternative ANCHOR rather than dropping the anchor: **the glyph is decoration and the verb is the record**, but an anchorless pattern reads `[SkipOrchestrator] Test "x" passed after 1.0 s` as a verdict and invents a test, which is the fifth splice shape recorded above as a permanent phantom casualty.

**Measured across 138 preserved logs, old parser vs new: NO VERDICT 14,603 → 14,331 (−272), passed +271, skipped +1, started +51, failures 591 → 591 with none gained and none lost. 76 logs shed at least one casualty and ZERO gained one** — which is the check that matters, because recovering a severed START without its verdict would manufacture a casualty rather than remove one. On t53a's own merge gate, the log that motivated moving the census to the bundle: **9 → 0**. Do not read this as a reason to go back to the console for verdicts — the 68-log table above still stands and the bundle is still the instrument. It is a reason to keep the roster honest.

Rails: 26 new (246 total, ~3 s, no build), of which **20 fail against the parser as shipped and 6 are anti-fabrication guards that must hold in both directions**; the four specimens are byte-exact and are pinned against a committed 52 KB distillation of the whole 6.4 MB run, so a typo in one turns into a red rail rather than a test of the typo. 17 new mutants (RS01–RS17, 125 total, 124 killed, control survives). **Three survived first time and two of them were the new tests being vacuous** — one read the very constant it existed to pin (`APP_B * (gb._MAX_SPLICE_SPAN + 1)`, which rises with the mutant), one put an INTACT name after a damaged glyph so the tail parsed on its own and the head was never exercised, and the third was a *proven* equivalent (dropping `+` from the shard cannot matter when every pattern SEARCHES) and was repointed rather than excused.

**A DENIED RESOURCE IS NOT A FAILING TEST EITHER — and the fd hypothesis that named it was refuted, then CONFIRMED by the same run's own log (playhead-s34ux, 2026-08-24).** Full-plan gates were failing with `SQLITE_CANTOPEN` in a **rotating** set of suites while every affected suite passed scoped. Three runs on three different trees — one of them main with the candidate diff ABSENT — reported 7, 8 and 56 NEW failures of which essentially every one read `Migration failed: unable to open database file`, with not one behavioural assertion among them. 56 NEW failures on a tree whose whole diff was a measurement probe and a markdown file is exactly the shape that trains a reader to wave a red gate through, and one `--accept-baseline` on such a run writes healthy tests into the known-broken file where their next GENUINE failure reads as known.

**The bead's hypothesis was file-descriptor exhaustion in the single test host. It was measured, read as REFUTED, and the reading was WRONG BY ONE DENOMINATOR.** Measured with a probe committed in this bead, over a full plan of 11,785 tests:

| quantity | peak observed | limit | headroom |
|---|---|---|---|
| test-host open fds | **2,539** (2,536 vnode, 2 socket, 1 kqueue) | `kern.maxfilesperproc` 61,440 | **96 %** |
| system-wide open files | 7,791 | `kern.maxfiles` 122,880 | 94 % |
| disk free (min during run) | 24.7 GiB | 13.5 GiB preflight | comfortable |

That table is the trap, and it is this repo's standing defect class committed by the instrument built to catch it. **`kern.maxfilesperproc` IS NOT THE LIMIT THAT BINDS.** The test host's own `RLIMIT_NOFILE` **soft limit is 2,560** — printed by this bead's own probe, on its own line, in the same log, and unread for a day:

```
[s34ux-fd] pid=10985 getrlimit_rc=0 RLIMIT_NOFILE soft=2560 hard=9223372036854775807
```

| | peak | limit | share |
|---|---|---|---|
| the reading that was published | 2,539 | `kern.maxfilesperproc` 61,440 | 4.1 % — "vast headroom" |
| **the reading that binds** | **2,539** | **`RLIMIT_NOFILE` soft 2,560** | **99.2 % — at the ceiling** |

Same numerator, two denominators, opposite conclusions — and the bead brief asked for the soft limit *in the same sentence* that named the sysctl ("which is what actually binds and is usually far lower"). It is 24× lower.

**The 99.18 % needs no inference: 2,539 is a kernel-side `PROC_PIDLISTFDS` snapshot, so it is 21 descriptors of headroom, read directly.** A second and independent witness agrees — `maxfd = 2559 = soft − 1`, and `open()` returns the **lowest available** descriptor, so being handed 2559 means 0…2558 were all in use: the table was **completely full**. (Do not lean on the in-process count, which reads only 2,079 at its own peak: its `fcntl` scan walks 0→70,000 non-atomically while other threads close, so over a churning table it is a lower bound. That is why the external snapshot is the primary figure and `maxfd` the corroboration.) **All 63 `CANTOPEN` denials fall in 02:43:20–02:43:23, and the peak sample is at 02:43:20 — the same second**, after which the count collapses to 966. **Raising the limit is not a fix and must not be presented as one**; what holds 2,560 descriptors is `playhead-vk68m`, and the measured answer is **695 concurrent WAL stores against a table that admits 702** — the run did not approach the ceiling, it arrived at it with seven stores of headroom, which is why the denials are a four-second burst rather than a rate. (Derived from the series: peak 2,539 of which **2,536 are vnodes**, against a post-phase floor of 453 flat for **19** samples — 22 until playhead-vk68m re-derived it; measured off `fd-series-s34ux.csv` itself, both `testhost_fds` 453 and `testhost_fd_vnode` 449 hold for 19 consecutive samples spanning `elapsed_s` 292.3 → 475.0, i.e. 182.7 s, and the plateau is the LONGEST run in the whole series so no wider reading exists. The 695 below does not move: it is `(2,539 − 453) / 3.00`, which never used the sample count — at 3.00 descriptors per store. Prefer that to the `2,799 call sites × 3.00 ≈ 8,397` upper bound — measured concurrency beats a count of source lines.) **And state the caveat this owes, or the next reader refutes it with one `grep`: there is NO `EMFILE` anywhere in the log** — no "Too many open files", no `Code=24`, no `Code=23`. The exhaustion is established by arithmetic (2,539 against 2,560, `maxfd` at 2,559) and by the measurement that `open(2)` reports errno **9** at this ceiling, not by reading an errno that says "too many open files", because this kernel never emits one here. **Both obvious ways to take that measurement give a confident WRONG answer, in opposite directions, and both are the standing defect class:** `lsof -p PID | wc -l` — *the recipe the bead itself proposed* — is not the open-fd count, because it also lists `cwd`, `rtd`, `txt` and every mapped dylib (measured: 16 lines for 3 real descriptors, and a test host maps hundreds of images); and `proc_pidinfo(PROC_PIDLISTFDS)` **with a NULL buffer returns the table's CAPACITY**, a high-water allocation that never falls (measured: 3 fds → 45; open 100 → 148; close all 100 → still 148). `scripts/gate-memory-sample.py` now samples it correctly on every run — one `proc_pidinfo` with a real buffer, 1–17 µs — and a read that fails records **-1, never 0**, because a printed zero would invent a refutation.

**The error TEXT carries a discriminator the series does not, and it is why any fix aimed only at SQLite is aimed at a symptom.** One failure in the same run was a **source file** read: `NSCocoaErrorDomain Code=256 "The file "AdDetectionService.swift" couldn't be opened"`, `NSUnderlyingError = NSPOSIXErrorDomain Code=9 "Bad file descriptor"`. **`EBADF` (9) is not `EMFILE` (24)** — exhaustion fails an `open()` and says "Too many open files"; **and on this box that distinction is BACKWARDS.** Measured in C — lower `RLIMIT_NOFILE` to 64, open until refused, read `errno` on the next line — the refusing `open(2)` sets **errno 9, not 24**, reproduced through `os.open`, a raw `libc.open` and a standalone C program. So `Code=9` on that source-file read is *precisely* what a process at its descriptor ceiling produces here; it was never evidence of an over-close, and the hand-audit of every raw `Darwin.close` site was looking for something that was never there. **Reason from the errno this kernel RETURNS, not the one POSIX documents** — that is the third instance of the standing defect class in one bead, one layer below reading `kern.maxfilesperproc` as the limit that binds. The root cause — *what holds 2,560 descriptors* — is `playhead-vk68m`, and it is arithmetic rather than a leak. **Two different quantities, and they must not be quoted as one.** The UPPER BOUND, if every store-opening path were in flight at once: 2,799 call sites × 3.00 measured descriptors per WAL store ≈ **8,397**. The OPERATIVE figure, measured off the series: peak vnodes 2,536 less the host's 449-VNODE floor, over 3.00 ≈ **695 concurrent stores** — note both terms are vnodes; pairing the vnode peak with the 453 TOTAL floor is a population mismatch that reads the same to two decimal places and is wrong anyway (playhead-vk68m R4), against a table that admits about 702. Quote the second — the first predicts a run that dies three times over, while this run reached 99.18 % and kept going, and a reader who spots that gap distrusts the paragraph. Swift Testing starts essentially every test at once (playhead-3rql: 11,000–11,400 in flight, and the parallelization-width env var is ignored), so the plan completes only because stores are released faster than new ones open and every run is a race. That is the whole explanation for the rotation, the unstable count (7/8/56), the single four-second burst, and why it never reproduces scoped; `TestScratchReaper` was ruled out statically (its `sweep()` skips unowned entries by its first line, and every failing open happens strictly BEFORE the `adopt` that would make its directory reapable at all).

**What the gate does now.** `gate_baseline.py` routes a resource denial to its own category, out of the NEW column, and says so with a count:

```
RED (0 known / 56 NEW)
-> RED (0 known / 0 new) — 60 tests hit a RESOURCE FAILURE (re-run)
```

Measured on the three preserved logs: 7dgx `12 known / 7 NEW` → `11 known / 0 new, 8 RESOURCE`; main-with-the-diff-absent `0 known / 8 NEW` → **`0 known / 1 NEW`, 7 RESOURCE** — the one genuine behavioural failure in that run survives the classification, which is the property the whole design turns on. **READ THOSE THREE READINGS WITH THE CAVEAT THEY WERE FIRST PUBLISHED WITHOUT, because it is the sharpest lesson in the bead: every one of them was taken CONSOLE-ONLY (`check --log …`, no `--xcresult`), and the classifier was INERT on the bundle path — which is the path `fast-gate.sh` takes on every real run.** `_apply_bundles` builds a fresh `BundleRun` and never folded `resource` into it, so each denial `parse_xcresult` had classified was discarded again; and because the denied key had already been removed from `failures`, with nothing left in `resource` to subtract it fell out of `ran` and was reported as a **CRASHED-HOST CASUALTY**, which `--accept-baseline` would have written into the census as a host death that never happened. Strictly worse than the defect it was meant to fix, 297 rails green over it, and invisible to every measurement taken because they all used the one path that worked. Found by an adversarial review round that went and drove the path instead of re-reading the claim. **The general rule: a verification that never exercises the caller the product uses is a measurement of something else** — the standing defect class, living in the verification rather than in the code. Four rules, each because its mirror is how this class of fix goes wrong:

- **It is STATED, never inferred from silence.** The classification reads the failure MESSAGE the bundle records (console text as the no-bundle fallback). A FAILED case with no message at all stays a FAILURE.
- **An unrecognised wording stays a FAILURE, which is why the discriminator is an ERRNO and not the prose.** `The file "X" couldn't be opened` is `NSCocoaErrorDomain Code=256`, which a test asserting its own output is readable produces too. Only **EBADF (9), ENFILE (23), EMFILE (24), ENOSPC (28)** route — codes no product logic can produce. **ENOENT (2) and EACCES (13) deliberately do not**: "the file is not there" and "you may not read it" are what a product bug looks like. An errno outside the table **VETOES** rather than being ignored, so a message carrying both `Code=2` and a recognised phrase is a failure. One prose match survives — `unable to open database file` — and **the reason is the PLATFORM, not a missing call, which is the opposite of what this line used to say.** It read "because `AnalysisStore` throws `sqlite3_system_errno()` away", i.e. as a TODO waiting on `playhead-enzva`. That call was written, measured and WITHDRAWN (playhead-vk68m, `artifacts/MEASUREMENTS.md` M3/M3b/M3c): on the iOS 27 simulator three unrelated causes — a parent that is a regular file, a parent directory at mode `0o000`, and a path that IS a directory — all report `system_errno = 0` AND a bare `sqlite3_extended_errcode` of 14, while SQLite's own log line in the same run reads `os_unix.c:52971: (20) open(...) - Not a directory`. The library knows; neither public API says. Against the MAC's `/usr/lib/libsqlite3.dylib` the same two conditions return 20 (ENOTDIR) and 13 (EACCES) — and **both libraries report `sqlite3_libversion() == "3.54.0"`**, so it is a build difference, not a skew anyone can wait out. `PlayheadTests/Persistence/AnalysisStore/SQLiteSystemErrnoPlatformProbeTests.swift` re-measures it on every run and goes RED if a platform ever starts discriminating. **So the prose match is a standing limit on this platform, not unfinished work — do not re-attempt the errno capture without reading M3c first**, which prices the two consumer regressions it cost.
- **The per-test rule is UNANIMITY, and so is the collision rule — but they resolve in OPPOSITE directions from the crash rule.** One genuine assertion alongside a CANTOPEN and the whole test stays a FAILURE. Two same-named tests share one key, and there the first draft copied the crash path wholesale so the DENIAL won: **mutation RD10 survived it and was right to.** A crash outranks everything because *nothing was judged*; a denial does not, because an assertion failure names a DEFECT and a denial names the BOX — so denial-wins silently swallowed a real regression sharing a display name with a denied test, the single outcome this change exists to prevent. Denied + passed/skipped → the denial stands. Denied + genuinely failing → the FAILURE stands.
- **It is NOT quieter.** Those tests exit 65 today as NEW failures and exit 65 after this as resource casualties; GREEN is unreachable while the count is non-zero. What changes is the DIAGNOSIS, and that `--accept-baseline` can no longer learn anything from a denied run: denied entries are excluded from `tests`, PROTECTED from the rename-prune (else the file shrinks because the box was short), excluded from the crashed-host census (`no_verdict` SUBTRACTS them — a test that reported an error is not a test that said nothing), and `BASELINE IS FICTION` no longer fires on a run whose every failure was a denial.

Rails: **70 new (323 total, ~3 s, no build), of which 55 fail against the parser as shipped and 15 are anti-fabrication guards that must hold in both directions** — re-derived at audit, which is also how the earlier "44 new / 32 fail / 8 guards" was caught: the counts had grown twice under a sentence nobody re-derived, and `32 + 8` never equalled 44 — verified by restoring the pre-bead `gate_baseline.py` into a scratch copy of `scripts/` and running the new classes against it. The pair the design rests on is `EBADF_SOURCE` (byte-faithful from the real log) and `ENOENT_SOURCE`, **the same sentence with `Code=2` instead of `Code=9`**: a classifier written on the prose cannot tell them apart. 43 new mutants across two rounds (**168 total, 166 killed, 1 survived, control survives** — measured against a clean `git archive` of the merged tree, not in a worktree where an uncommitted edit could contribute; the ids are not a contiguous range and a range is not a count), each with its PREDICTED victim recorded before the run and compared against the whole suite run under the mutant — every single-victim mutant killed exactly the predicted test. **Two survived first time and neither was a rail being vacuous:** RD10 was a real defect (above), and RD14 was a *proven* equivalent — `and not self.resource` on the GREEN line is unreachable while `exit_code` arms on the same field — kept in source as a stated invariant and repointed rather than excused. **The one survivor left, `R21`, is NOT this bead's**: it survives on `105efd5f` too, verified against a clean `git archive` of that commit, and is filed as `playhead-5c006`.

The verdict now **names its instrument on every run**, including a clean one: a census of zero taken off the console is exactly the reading that most needs qualifying, because a severed *start* line makes a test invisible in both directions. Rails: 30 new unit tests (215 total, ~3 s) and 18 new mutation rails (RB01–RB18) in `scripts/mutation-battery-gate-baseline.py`, including the shell half — a stubbed `xcodebuild` that really writes a bundle and a stubbed `xcrun` that really reads it back, because "does the bundle reach the check" is a property of argument plumbing, not of Python.

**Expect the file to grow for a run or two before it settles, and do not read that as regressions.** The recorded set is the UNION of what has been observed, and with a 0.46 Jaccard each new run surfaces flakes the earlier ones missed. Capture–recapture on the first two runs (32 and 28 failures, 19 shared) estimates the true population at **~47**; 41 are recorded after two observations. So the next `--accept-baseline` or two will legitimately add a handful of names, and the gate will report them as NEW until they are recorded. That is the mechanism working — a newly-observed flake is indistinguishable from a regression until it has been seen, which is exactly why it is reported rather than absorbed. The third accepted observation is also what arms the pass-direction check.

**THE POPULATION IS BOUNDED AND THE OBSERVED UNION HAS VERY NEARLY ENUMERATED IT — measured directly at o89d R2, re-derived independently at R3, and two earlier versions of this paragraph claimed the opposite.** Both of those were estimates (a Chao1 "lower bound of 377", withdrawn below; then "the union does not saturate … there is no knee"). What follows is the direct measurement of the quantity they were estimating, so prefer it over either. **The numbers below are R3's; R2's are given beside them, because the two disagree on the SIZE of the log census and agree on everything the conclusion rests on.**

Method, so it can be re-run: every preserved log that is non-selective, started **more than 11,000 tests** and lost **fewer than 100 verdicts**, de-duplicated by `(start count, failure set)`, parsed by `gate_baseline.parse_run` itself rather than by a fresh regex. Re-run at R3 and independently again at R4 over **every** `.log` under `/private/tmp` (which is where `claude-501/-Users-dabrams-playhead/*/scratchpad` actually lives — there is no `claude-501` under `$TMPDIR`, so R3's stated root does not exist on this box), `$TMPDIR`, `/Users/dabrams/playhead` (which contains `.worktrees`) and `/Users/dabrams/.claude`: **39 qualifying logs, 38 after de-duplication, 2026-08-09 18:37 → 2026-08-13 12:41**. Union: **269 distinct failing tests**. R4 reproduced every figure in this section to the digit. Two mechanical details that matter if you re-run it: a size floor of 100 KiB is safe and provably non-binding (the smallest qualifying log is **6.8 MiB**, since >11,000 started tests cannot fit in less), and **"non-selective" must be tested on `-only-testing:` WITH THE COLON** — see the next paragraph for what happens otherwise.

**R2 stated the same criteria and reported 28 runs / union 243, and its own census of logs was the incomplete half** — the defect it had just charged R1 with ("there are nineteen preserved logs that day, not four"), one level up. Prefer 38/269, and **name the roots you walked**, because "every preserved log" is a claim about a search, not about a set. **The whole of R2's shortfall is now accounted for (R4); R3 left six of it "unexplained by any variant of the stated filter I tried", and the six are one bug.** Four of the 38 live outside the session scratchpad that R2 walked (`/private/tmp/gate-baseline-refresh.log` and three under a second session's directory). The other **six are full-plan runs whose own header line reads `REQUESTED_SUITES=ALL (PlayheadFastTests, no -only-testing)`** — a bare substring test for `-only-testing` matches the sentence that says there were none and drops the run as selective. `28 + 4 + 6 = 38`, exactly. R4 hit the identical bug on its own first pass and got 33/32/250 before fixing it, which is how it was found. It is the standing defect class in the census machinery for a third time: a string that names an ABSENCE read as evidence of a presence. Testing for the colon fixes it, and **zero** of the 48 logs that start >11,000 tests are genuinely selective, as one would expect.

- **LEAVE-ONE-OUT: the union of 37 runs covers the 38th run's failures at a median of 100 % and a mean of 97.5 %** (R2: 100 % / 97.3 % over 28). Twenty-five of the 38 are covered *completely*. The worst — 72.9 %, 76.0 %, 84.3 %, 85.9 % — are each a run carrying its own branch's breakage: 26 of one run's residual names are `SkipOrchestrator*`/`rfu-sad`, 14 of another's are a mutation control's intended kills, 13 of a third's are schema-migration tests. **The residual is the diff.** That is the gate's NEW column working, not the population moving.
- **LEAVE-ONE-OUT IS AN IN-SAMPLE STATISTIC AND THE QUANTITY YOU WANT IS FORWARD COVERAGE.** Holding run *k* out still lets it be covered by runs that came *after* it, so it is not a prediction. The forward version — what fraction of run *k*'s failures the union of runs 1…*k*−1 already contained — is computed from the same data and is the number to quote: over runs 11–38, **median 100 %, mean 96.7 %**; over the last ten runs, **mean 98.9 %**. The conclusion survives the stricter estimator, which is why it is worth stating in the stricter form.
- **The union has stopped growing.** Per-run additions in time order: `+79 +31 +18 +11 +18 +0 +0 +20 +0 +4 +1 +0 +0 +16 +2 +13 +6 +1 +0 +2 +0 +0 +0 +0 +0 +0 +0 +35 +10 +0 +0 +0 +0 +0 +0 +1 +1 +0`. Nine of the last eleven runs added nothing at all, and the one late spike (+35) is the `SkipOrchestrator` branch-breakage run above.
- **168 names have been seen in ≥3 of the 38 runs, and they alone cover a mean 96.6 % of any single run's failures** (184 names at ≥2 cover 97.5 %). The other 85 are singletons, mostly branch-specific. *That* is the size of the thing, and it is a name list.
- **The FILE is behind the population, not chasing a moving one.** All **121** committed entries appear in these 38 logs — zero are stale — but the file is a 10-observation sample and is missing **49 of the recurring 168**. So expect `--accept-baseline` to keep adding names for several more accepts. That is the file catching up to a bounded set, and it converges; it is not evidence of a tail nobody can bound.

**"THE MEASURED UNION" IS TWO DIFFERENT POPULATIONS AND THE PHRASE READS THEM AS ONE (R3) — AND THE MEASURED HALF IS A RECORD, NOT AN INFERENCE, WHICH IS WHERE R3's 136 CAME FROM (R4).** R3's structural point stands: "PerfGate the measured union" names two things. Its NUMBER for the measured half does not, and the corrected one is bigger and better evidenced.

**Read the five preserved logs rather than reconstructing a set from a count.** The isolated measurement is on disk, per test, at `…/-Users-dabrams-playhead/bee44b12-…/scratchpad/runs/scopedA-{1..5}.log`. Parsed: **52 `-only-testing:` targets, byte-identical across all five runs, 441 tests with an outcome plus one skip, zero lost verdicts, and 440 of the 441 pass 5/5.** The single 5/5 failure is `benchmark gate`, already known to be a benchmark rather than a defect. Against the **269**:

- **184 union names were measured in isolation, and 183 of them pass 5/5** (the 184th is `benchmark gate`).
- **85 have no isolated measurement** — not 133. Of those 85, **66 are singletons** and only **9 recur in ≥3 runs** (all nine timeout-kind).
- Of the recurring **168** — the population worth scoping anything from — **159 are already measured 5/5**. Nine are not.
- Of the **176** timeout-carrying names, 144 are measured and **all 144 pass 5/5**.

**R3's 136 is the SELECTION SEED, not the measured set** — the names the 52 suite targets were derived from — and it was reconstructed rather than read: by searching for a combination of the 117-entry pre-accept baseline and the 08-13 logs whose union had *cardinality* 136. A cardinality does not identify a set. **Four different pairs of 08-13 logs hit 136, and they are four different sets**, pairwise differing by 6 to 24 names; only one of the four is fully covered by the isolated run (the others contain 1, 3 and 7 never-measured names), and **two of the four reproduce R3's residual breakdown 65/68/35/82 exactly**. So the reconstruction landed on the right set and its method could not have told it so, and the residual breakdown cannot distinguish them either. The pre-accept file's own pin holds (`sha256 4d300374…`, 117 entries, 9 observations), so the seed half of the recipe is right; it is the "plus two logs" half that is under-determined.

Consequence for the advice below and for `playhead-06t7`: extending the measurement from the seed to **the recurring 168 is NINE names, not thirty-two**, and they can be appended to the existing 52 targets for one more ~28 s run. Every other name in that population already has its 5/5.

**THE UNION IS NOT THE LOAD-SENSITIVE POPULATION EITHER, AND ~93 OF IT HAS NEVER PRODUCED A TIMEOUT (measured at R3).** Split the 269 by the failure KIND the baseline already records: **155 have only ever timed out**, **21 have shown both kinds**, and **93 have only ever failed an EXPECTATION**. A timeout is the starvation signature PerfGate exists for; an assertion failure is a test reporting on the code, and moving it to the serial pass hides a real verdict — the more so while that pass is red and unread (`playhead-1och`). Of the 93, 53 appear in exactly one run (branch breakage, correctly reported as NEW at the time) and **29 recur in ≥3 of the 38 runs on many different trees** — `DownloadShowAttributionTests` ×7, `BackgroundGrantBudgetTests` ×7, `ForceQuitResumeTests` ×3, `BackgroundURLSessionTests` ×3. **The KIND does not settle which of those is a harness artefact** — measured at R4 off the five `scopedA` logs, **all 29 were run in isolation and 28 pass 5/5** (the 29th is `benchmark gate`), so an assertion CAN be the way starvation shows up (a poll loop's own bound trips before the 60 s time limit does), and `playhead-3f79`'s `settleWaitDoesNotLie` is recorded `assertion` for exactly that reason. What the split does establish is that **269 is an UPPER bound on the PerfGate-able population and 176 (the timeout-carrying names) a LOWER one**, that the isolated measurement is the only thing that discriminates inside that band, and that PerfGate-ing by NAME throws away the kind discrimination the gate's own tolerance is built on. Quote whichever bound you are using and say which.

**The failing set still ROTATES, and that half of the record is right — but rotation is CHURN INSIDE A BOUNDED SET, and the two were conflated.** Pairwise Jaccard over all **703 pairs** of the 38 runs: median **0.505**, mean 0.507, range 0.171–0.798 — consistent with the 0.46 measured on two runs on 2026-08-01 and with R2's 0.515 over 378 pairs. So *which* half of the known population fails tonight is close to a coin flip, which is exactly why a flat exact set is unusable and why the `seen`/`failed` tiering exists. It is **not** a reason a name list cannot bound the population: the same data says a 269-name list leaves a typical run with **zero** unabsorbed failures. Do not quote "the failing set rotates" as an argument that accumulation cannot converge — measured, it converges.

**WHAT THIS FOUR-DAY WINDOW CAN AND CANNOT SUPPORT.** Verified at R3: the oldest preserved `.log` of *any* size on this box is 2026-08-09 18:37, so the window is four days and there is no way to extend it backwards. It supports "the union converges over four days of ~28–38 runs" and it supports refuting the 07-29-vs-08-13 drift claim, because the 07-29 cluster is present and failing throughout. It does **not** support "the population is stable over fifteen days" — that is exactly the interval where the reading went wrong before, and the only evidence for it is one endpoint. Re-measure when the logs reach back further rather than extending the sentence.

**DO NOT QUOTE A SPECIES-RICHNESS ESTIMATE OFF THIS FILE — it is not stable enough to carry a number (measured at o89d review).** The first version of this paragraph said Chao1 gives "a lower bound of **377**", and it did: on the 9-observation / 117-entry file, f1 = 51 and f2 = 5, so `117 + 51²/10 = 377`. **The very next commit in the same series invalidated it.** One accepted run moved 21 names from f1 into f2 (f1 51 → 30, f2 5 → 30), and the same estimator on the file committed here reads **136.0**. Chao2 over the same-day full-plan runs reads anywhere from **252 to 818** depending purely on how many of them you include. Because `f1²/(2·f2)` explodes when f2 is small and this record's f2 was 5. Two further traps the original wording fell into, both the standing defect class: 377 was computed with `S_obs = 117` and then compared against "136 ever observed" (with `S_obs = 136` the same spectrum gives 396), so the "~35 % sample" mixed a numerator and denominator from different populations; and "390 tests carry a `.timeLimit(.minutes(1))`, which is where 377 lands" is a coincidence of two unrelated counts, not corroboration. **Every one of those estimates is now superseded by the leave-one-out measurement above, which asks the question directly instead of extrapolating a frequency spectrum. Estimate species richness only when you cannot go and count; here you can.**

**A related trap, because the correction repeated the defect it was correcting.** The withdrawn text said "**two** full-plan logs of 2026-08-13"; R1 corrected it to "**four** (10:23, 11:09, 11:47 and this bead's own 12:41)". There are **nineteen** preserved full-plan logs from that one day. R1's `+10, +17, +9, +6` are each contributions measured against the 117-entry file *individually* (and 11:47's is 13, not 9), while its "union of 146" is the *cumulative* union after only the first **three** of the four — the four together give **149**. Individual and cumulative increments are different quantities and the sentence mixed them, which is why `117 + 10 + 17 + 9 + 6` does not equal 146 and never could. When you fold logs into a union, say which base each increment is against, and count the logs before you name them.

**Two corrections to how a red gate is read, from the same measurement.** First, **`passed after N seconds` is enqueue-to-completion, not test cost, and the 97 s triage rule reads it as though it were cost.** In a full plan **~90 % of PASSING tests report over 60 s** (re-counted at R2 off the spliced-line rejoiner: **90.4 %** of 11,452 passing lines on the 11:47 log of 2026-08-13 and **90.5 %** of 11,421 on the 12:41 one) and the median is **115–153 s** depending on the run (152.6 s and 115.5 s respectively). The clean witness is a test that prints its own `ContinuousClock` reading: `benchmark gate` reported **220.149 s** while its own clock read **105,490.8 ms**. So "under 97 s" selects tests that failed EARLY in the run, not tests that failed for behavioural reasons. Second, **"asserts an absolute wall-clock budget" is the wrong predicate** — CPU-bound budgets are fine under full load (`evaluate() completes in <200ms`, `detectRefined … within budget` at 200 ms, and the 5 s `PerformanceSmokeTests` all PASS in the full plan). What starves is **scheduler-dependent waiting** measured on an absolute clock: a poll loop, an `AsyncStream` consumer, a cross-actor callback. **440 of the 441 tests in those 52 scoped suites pass 5/5** — re-derived at R4 from the five preserved `scopedA` logs rather than from the "443 / 135 of 136" summary, which counted the selection seed instead of what ran; a 12-suite subsample spanning both stale name lists re-ran **3/3 clean** at review (147 tests, ~22 s).

**THE PROPERTY A MUTATION BATTERY CANNOT REACH IS THE LINE THAT ISN'T THERE (playhead-o89d R5).** Four review rounds pinned four properties of `--accept-baseline`'s output — the banner's SPELLING, the EVENT it names, the CONSEQUENCE it states, and the COUNT it prints — each round closing the direction it was looking at and leaving the mirror for the next one. R5 enumerated the accept path a second time and independently (69 mutants against R4's 28) and the residual hole was not a property of any line: **a KIND WIDENING had no line at all.** `merge` UNIONS a run's failure kinds into an entry already in the file, so a name recorded `timeout` that fails an EXPECTATION comes out recorded `assertion+timeout` — its tolerance doubled. Measured end to end: `check` reports `FAILS DIFFERENTLY … recorded as timeout, failed as assertion` and exits 1, the accept prints `(membership unchanged; counts updated)`, and the identical failure afterwards is GREEN. **15 of the 121 committed entries carry two kinds today**, several at 10/10 — the widest tolerance the file can express, arrived at without anyone signing for it. It is the fifth event that makes the gate LOOSER (after the two tier demotions, the census prune and the record-level arm) and the only one that was silent. `TOLERANCE WIDENED:` closes it. **The method, since this is the fifth round on one shape: enumerate the EVENTS the command can perform and ask which of them prints nothing — a mutant can only ever interrogate a line that already exists.** R5 also found R4's own three new rails one-directional in the same way (RA29 pinned the promotion detail's `[kind]` and not the demotion's; RA22 pinned `DISARMED:`'s event and not `CENSUS DISARMED:`'s; RA27 pinned the crash headline's count and not `CARRIED FORWARD:`'s), all three mirrors survived, all three are rails now. And **`--only` can no longer drop the vacuity control**: `--only RA32` used to print `control not run` and then `All mutations killed, and the vacuity control survived.` and exit 0 — the same hole the untypeable battery closed, still open in the R engine the P series shares.

Rails: `scripts/mutation-battery-gate-baseline.py` (R series) and `scripts/mutation-battery-disk-preflight.py` (P series, playhead-3nfa, reusing the R engine); the D/E/J/K/L/Q series stay in `mutation-battery.sh`, which is structurally a Swift battery. `scripts/mutation-battery-untypeable.py` (TY series, playhead-x0lb) is Swift too but reports a different verdict — **`UNTYPEABLE`, the mutated source must FAIL TO COMPILE** — because the defect class it covers (two quantities sharing a unit, one read as the other) is invisible to any test: the two are equal on every fixture a test author would write. It is build-only, ~13 s per rail warm (38 rails, measured at R6 review; the earlier "~5 s / 35 rails" figure was neither re-timed nor re-counted after R5 added three), and checks the diagnostic KIND so a syntax break reports `WRONG-ERROR` instead of being credited. **It builds the APP target only, so it cannot see a test file** — that is deliberate (the question is whether production type-checks) and it is why a round that edits tests still owes a `build-for-testing`. Two anti-vacuity rules were added in R2 review and are worth knowing before you drive it: **`--only` cannot drop the control** (a selection that omits `TY99` has it appended, because a partial run printing `control not run` and exiting 0 is exactly the hole this battery replaces), and **`--check-inventory`** is a buildless preflight — it runs on every invocation — tying `UnsoundCursorPromotionSite` to the actual sites, since the Swift test can only read `allCases` and that is a property of the enum rather than of the code. R3 review out-spelled that preflight inside its own file — a sixth promotion written `EpisodeSeconds.init(…)` rather than `EpisodeSeconds(…)` passed it with rc=0 — so both spellings are counted now; the general escape (a promotion helper declared in another file) remains open by construction and is limit L-F. R6 review added a second clause to the same preflight — **region ASSEMBLY is confined to `AnalysisStore.swift` and `CoverageQuantities.swift`** — because `init()`/`append(start:end:)` on the four interval-carrier types are internal, so probe PJ1 assembled a `TranscribedRegion` out of the FAST ranges in `AnalysisJobRunner` and it compiled, reproducing playhead-9y9e's shipped defect one layer below rails TY32/TY34. **R6 wrote that clause as a grep over the four TYPE NAMES and R7 got three spellings past it** — `TranscribedRegion ()` (a space before the parens), `.append(start : …)` (a space before the label's colon), and probe **PK1**, `let region: TranscribedRegion = .init(fastPass: .init(spanningFromZeroTo: CoveredSeconds(watermark)), finalPass: .init())`, which models the fast watermark as a contiguous transcribed region and feeds it to the 0.95 finalize floor; the preflight returned rc=0 and the app BUILT. Dot-`.init` names no type, so no pattern over type names can ever see it. What replaced the grep is a **compile-enforced naming obligation**: `RegionFillDoor` is a token the six assembly entry points take, so a fabrication that omits it is `missing argument for parameter 'door'` (rail TY38) and one that supplies it must write the identifier `openedByCoverageReader`, which the preflight greps for — "one identifier, every spelling" instead of "the spellings the last reviewer thought of". **It is still not a capability**: the token is `internal`, so it stops an author who is not thinking about which population they hold, not a determined one, and that is L-F's worth arrived at honestly rather than by enumeration. Unit tests: `python3 -m unittest scripts.tests.test_gate_baseline` — about a second, no build.

**ONE BATTERY PER WORKTREE, ENFORCED (playhead-pu7e).** `scripts/mutation-battery.sh` now holds a lock in the worktree's own git directory for the whole run; a second invocation exits **75** naming the holder's pid, start time and `--only` argument. Do not go looking for a way around it — the reason it exists is that a concurrent battery is **undetectable from inside either run**. Between run A's restore and its next apply the tree is genuinely clean, so the dirty-tree guard lets B in, and from then on each run's restore reverts the other's mutant while both byte-exactness checks keep passing (each verifies only its OWN restore). It cost four destroyed verdict attempts in `playhead-9y9e` R1, every one of them reported as `the baseline did not run tests (rc=65)`; rails RT11, SC25, SC30 and SC33 still carry the implementer's own verdicts because no reviewer could obtain an independent one.

**Read the exit code, not just the refusal — `75` and `2` mean opposite things.** `75` is EX_TEMPFAIL: somebody holds this worktree, and waiting is the remedy. `2` means the lock could not be *established at all* and **nobody holds it**, so waiting will never help — a full volume, an unwritable git directory, or a run that took the directory but could not record who it was. The review round found the shipped code reporting every one of those as `75` with "Another run took it", which sends you to wait for a process that does not exist; and, worse, found that a holder whose identity write failed carried on mutating while its lock sat ownerless, to be reclaimed as ABANDONED by the next battery 300 s later — inside a normal 4–9 minute run. Both closed: taking the directory and recording the owner are now one step that either happens or does not, and `info` is renamed into place so no reader can ever see half of it.

Two corollaries worth knowing before the next `rc=65`:

- **`rc=65` with no tests is no longer self-diagnosed as a broken tree.** It now names CONTENTION, a WEDGED RUNNER (`blessSimulatorHub` / `service hub IS NOT still alive`, with the `simctl shutdown all` recovery), DISK, OOM, a real BUILD FAILURE, or **UNDIAGNOSED** — and prints the matched line as evidence. `xcrun: error: unable to find utility "simctl"` is labelled a **secondary** symptom: it is xcodebuild's diagnostic collection shelling out through the global `xcode-select`, not the 2026-07-16 clone-parallelism gotcha, and chasing it wastes the round.
- **The battery has never run `git checkout -- .`, and earlier prose here and in its own header said it did.** `restore_sources` is `git checkout -- "${MUTABLE_FILES[@]}"` and cannot touch a file it does not mutate. The blanket checkout was a *remedy people copied out of that wrong prose*, and on 2026-08-04 three agents destroyed their own uncommitted work running it by hand. If you want to know whether a run left residue, ask: `git status --porcelain -- Playhead`.

### THREE WAYS A MUTANT STOPS BEING EVIDENCE, and only one of them is loud (playhead-8cjo)

**A KILLED column is not evidence; the observed victim set matching a prediction written down BEFORE the run is.** Check your own mutants against these three before spending a build on them — all three were live on one branch, and the one that costs a round is not the one that costs a verdict.

- **THE LOST RAIL — killed, but not by the property you think.** playhead-8cjo's `AK11` re-creates its defect in `NowPlayingViewModel.observeBanners` and is correctly KILLED. It cannot prove the delegation canary's *strengthening*, because it keeps the parameter named `queue` and dies on the very substring the demonstrated bypass renames: `bannerQueue.enqueue(` contains no lowercase `queue.enqueue(`, so **one character of case is the whole bypass**. A canary hardened against it reports green having never exercised the new property. *Verdict shown: KILLED. Silent, and green.* This is the shape `FD06` v1 had on playhead-882eg. Remedy: a second mutant that re-creates the bypass VERBATIM, rename included, with a DIFFERENT predicted victim set — if the two mutants predict the same set, the second one is not testing the strengthening.
- **THE INERT MUTANT — the edit no longer changes behaviour.** playhead-8cjo's `MS02` injected a receipt write above a guard; that bead had already made the write unconditional above the same guard, so the injection became a second write of the same key differing only in a field nothing observes. *Verdict shown: SURVIVED. Loud, but misdirecting* — a reader goes hunting for a missing rail that is already there and already killed by five other mutants. Remedy: re-aim it at the DIRECTION it owns, which usually still exists somewhere else in the new code. **This does not weaken “every real finding came from a surviving mutant” further down** — a survivor is still the most productive signal this repo has, and that claim stands. What this case adds is one question to ask of a survivor BEFORE chasing it: does the edit still CHANGE anything on today's code? If it does not, the survivor is a false lead and the hunt is for a rail that is already there.
- **THE FALSE KILL — the edit became the shipped behaviour.** The suites go red for a change that injected nothing, and every rail that reddened is credited. *Verdict shown: KILLED. Silent, and it looks exactly like success.* This is the only one of the three that corrupts an already-recorded ledger, so it is the one to rule out first when a bead restructures code an existing mutant targets.

(`scripts/mutation-battery-untypeable.py`'s TY series reports **UNTYPEABLE** rather than KILLED/SURVIVED — the mutated source must fail to compile — but the three shapes still apply to it: an edit that compiles anyway is the INERT case wearing a different verdict word.)

**Anchor drift is the cheap case and is not on this list**: the source moved, `patch` matches 0 or 2+ times, and the battery prints `ERROR — anchor did not apply`. It announces itself. A bead that restructures a function must still re-READ every mutant aimed at it rather than re-anchoring mechanically — re-anchoring converts case 4 into case 2 or case 3 without telling you.

**Two companion rules learned the same day.** A SURVIVED verdict needs the batch's own xcodebuild log behind it, because `mutation-battery.sh` scored a NO-VERDICT test as a PASS (playhead-gjlp0) — and **that log must be pinned to the RUN, not to the batch number**: `WORK` is per-INVOCATION, `KEEP_WORK=1` on every failure path, and stale directories from other beads' runs sit in `/private/tmp` for days. A checker that searches `/private/tmp/playhead-mutation-battery.*` finds another investigation's evidence and reports OK. Use a freshness floor, and **prove it fires by making it fire** rather than reading a green result.

**BOTH OF THOSE ARE MECHANICAL NOW, AND THE FIRST ONE WAS THE THIRD WAY A MUTANT STOPS BEING EVIDENCE ALL ALONG (playhead-gjlp0, 2026-08-25).** The battery inferred SURVIVED from the ABSENCE of a failure line, so a test whose host died — `◇ … started` and then nothing — scored exactly like a test that ran and passed. Read the three shapes above again with that in mind: this is a **fourth**, and unlike the other three it is not a property of the MUTANT at all. It is the instrument.

Measured on the preserved specimen the bead was filed from (`/private/tmp/playhead-mutation-battery.F6R3wB`, 2026-08-23): the expected test had **10 start lines and ZERO result lines** — and the log already contained, **ten lines from its end, counting itself**, **`gate-memory: THE RUN DID NOT REACH A VERDICT — RESTARTED`**. The battery printed `BD37 SURVIVED … still green` over the top of its own instrument's finding, because it consulted only the failure list. Applied by hand that mutant dies deterministically in 0.147 s. (The rest of the specimen's figures are in *DRIVEN AND FOUND CORRECT* below, stated once.)

**How common is it? Measured over everything preserved on this box, twice — and THE COUNTS DECAY WHILE YOU LOOK, so quote the DIRECTION and re-derive the rest.** `/private/tmp` is cleared on every boot and other worktrees produce and reap these directories continuously; R1 stated its counts in the present tense and R2 caught it. Say which POPULATION each number is of, too: the first version of this sentence put three different denominators in one clause, and a batch log is kept only when its run FAILED, so most runs leave none.

| | R1 (2026-08-25 morning) | R2, the same day |
|---|---|---|
| run directories / size | 52 / 684 MiB | 43 / 508 MiB |
| batch logs, and the runs they come from | 55 from 19 | 46 from 10 |
| `results` tables | 49 | 34 |
| logs classifying OK / VOID | 53 / 2 | 44 / 2 |
| SURVIVED rows, of which checkable | 20, of which 18 | 9, of which 9 |
| …resting on a positive PASS | 17 — the one that does not is BD37 | 8 — the one that does not is BD37 |
| KILLED rows, all on an OK batch | 36, all from ONE run | 36, from 1 run |

**The DIRECTION is what reproduced exactly** — *every checkable SURVIVED but BD37 rests on a positive pass* — and it is the only part to quote. The two SURVIVED rows R1 could not check in either direction are a fact about the corpus, not a clean bill; and "36 KILLED with no counter-example" is one run, not 36 independent observations. Re-derive rather than carrying any of it:

```bash
# every preserved batch log, classified, and every SURVIVED row checked against
# the log IN ITS OWN RUN DIRECTORY (never by batch number — see the freshness bullet)
python3 - <<'EOF'
import glob, os, sys; sys.path.insert(0, "scripts"); import mutation_verdict as mv
for log in glob.glob("/private/tmp/playhead-mutation-battery.*/batch-*.log"):
    print(mv.read_batch(log, 0).batch_state, log)
EOF
```

So the fix is not noisy: on everything that can be checked it fires once, on the known false verdict. Do not read that as "rare and therefore cheap" — a SURVIVED verdict is the one that sends the next person to write a test that already exists, and a crash-looping batch produces it for free. And note what makes this audit possible at all: a SURVIVED row records the expectation it was about, so it can be checked against a log years later; a KILLED row records nothing, so the only thing re-derivable about it is its BATCH's health. The MUTATIONS table is also edited constantly, so looking an old row's expectation up in today's table gives you another mutation's answer — measured at review, that route reports 7 of the 20 SURVIVED rows as disagreeing, every one of them an artefact of the lookup.

**The ladder is four words now, and each is a different claim:**

```
KILLED    a STATED failure verdict for the expected test
SURVIVED  a STATED pass verdict — a positive ✔, never the absence of a ✘
VOID      nobody judged it (no verdict / crashed / resource-denied / skipped),
          or the BATCH lost its test host, or the bundle used a result string
          this parser cannot read.  exit 5.  Re-run — EXCEPT for TWO shapes,
          which a re-run can never clear and which the row says outright:
          a result string this parser cannot read (R4), and an expectation
          the MUTATION made SKIP, which the baseline had just watched PASS
          on the unmutated tree (R5).
ERROR     it could not be evaluated at all (anchor drift, build failure, an
          expectation naming a test that never ran).  exit 3.  Fix the EDIT.
```

`VOID` outranks every per-test reading **including a FAILED one**, and including an ABSENT one: a host dying mid-batch reddens tests — and swallows their start lines — for reasons that have nothing to do with the mutation, which is `playhead-4xmz`'s DW18 exactly. **The reason it is right is the ASYMMETRY, not the frequency, and no round had said so until R5 was asked to construct the counter-case.** It IS constructible: mutation M reddens its victim deterministically while a BATCH-MATE traps and takes the host down, so M's real kill is discarded and re-running the batch reproduces it for ever. The operator's move there is `--only M`, which puts M in a batch of its own — and that is a cheap re-run. The mirror costs more than a re-run: a KILL credited off a batch whose host died is a FALSE KILL, which this file calls silent and indistinguishable from success, and it corrupts the ledger permanently. Throwing away a real verdict is recoverable; manufacturing one is not. Verdicts come from the batch's own `.xcresult` bundle (`PLAYHEAD_RESULT_BUNDLE` into `$WORK`), via `scripts/mutation_verdict.py`, which imports `gate_baseline.py` and `gate_memory_verdict.py` rather than being a fifth parser. The baseline preflight widened in the same direction: an expectation must name a test that **PASSED**, not merely one that STARTED, so a rail whose host dies under it every time can no longer license every mutation that names it.

**Three things worth knowing before you argue with it:**

- **COUNT FROM THE PID, NOT FROM THE PHRASE.** The bead's own cheap first cut was `grep -c 'Restarting after unexpected exit'`, and on the very specimen it was written for that returns **11 for 10 replacements**: the eleventh hit is `gate-memory:`'s verdict block quoting the message back into the same log. A count of a phrase the run's own diagnostics also print adds two quantities together. The pid series (`Playhead[<pid>:`, the app's own testimony) says 11 distinct hosts and needs no interpretation.
- **The freshness floor is captured immediately before each batch, not at run start**, so it is a claim about THIS INVOCATION. Proven by making it refuse: at R1 this box carried **four batch numbers with more than one preserved log** (1319, 1405, 1414, 1432 — 1432 had three, and 1414's two copies were **three days apart**), and handing the checker the Aug-22 `batch-1414.log` against an Aug-25 floor made it refuse **by name and date**. Same caveat as the counts above, and it bit within the day: by R2 only **1432** still had more than one copy (three, all the same morning) and both `batch-1414.log`s were gone. **The durable claim is that batch numbers repeat at all, not how many do today** — a rail pins the mechanism, so re-derive the witness rather than quoting this list. Every non-KILL verdict now prints its own log and bundle path, so nobody looks a batch log up by number in the first place.
- **`scripts/disk-cleanup.sh` needs no change and that was measured, not assumed.** Its glob is `/private/tmp/playhead-*` at `-mtime +3` and `playhead-mutation-battery.XXXXXX` matches it; `find … -mtime +3` returns **0** today. The survivors are younger than the floor and the cron is weekly. What DID need fixing is that the new rails were adding to the pile — every one produces a non-KILL outcome, which is exactly what sets `KEEP_WORK=1` — so they now reap the path the battery itself prints.

Rails: `python3 -m unittest scripts.tests.test_mutation_verdict` — **123 total** (58 as first written, 34 added at R1, 12 at R2, 13 at R3, 2 at R4, 4 at R5). **The split is per CLASS, because that is the unit that pays**: `setUpClass` is what builds the sandbox and what runs `--list`, so a rail's cost is a property of the class it lives in and not of what its own body calls. **41 rails live in classes that drive the battery end to end** against a stubbed `fast-gate.sh` in a throwaway copy of the working tree (38 of them invoke it; 3 assert on artifacts their class already built), **10 live in classes that only read its `--list`**, and the remaining **72 need neither and run in 0.61 s**: `41 + 10 + 72 = 123`. (`39 + 10 + 68 = 117` at R3 and `39 + 10 + 70 = 119` at R4; R5 added two end-to-end and two to the cheap third.) **644.1 s for the 123, measured at R5** (616.3 s for 119 at R4, 609 s for 117 at R3), no xcodebuild and no simulator, so effectively all of the runtime is the battery at ~12-15 s per invocation and almost none of it is the rail. (**This split has been miscounted twice and both were the same defect** — a count derived by a mechanical rule, quoted as a claim about a property. R2's `62 + 29 + 4` mixed two granularities in one sentence, counting whole CLASSES in one term and individual rails in the other; R3's `36 + 4 + 77` undercounted the `--list` half by three, because `WholeTableResolutionTests`'s rails reach `battery_listing` through a helper **in their own class** and a rule reading only direct calls in the test body cannot see them. State it per CLASS — that is the unit that pays, since `setUpClass` builds the sandbox and runs `--list`.) The battery walks all 1,109 records through `rec_file`/`rec_name` before doing anything (2,218 forks), which is also why `--list` alone costs **14.0 s**; every operator invocation pays it too. Caching `--list` per root instead of running it once per shell class took **~70 s** off the module, which is how R2 added 12 rails for +94 s. **If this has to get cheaper again the lever is the battery's fork-per-record preflight, not the rails.** **8 of the original 12 end-to-end rails fail against the pre-bead battery**, verified by restoring `git show a82e52a9:scripts/mutation-battery.sh` into a scratch copy with the new module still present, so the only variable is the wiring (the run reads 8 FAIL / 4 ok). Every fix added at review was proved the same way — by making its own rail fire against the code it replaced — and the rails that did NOT fire are the anti-fabrication guards that have to hold in both directions, including the PIN, which reproduces the pre-bead algorithm in Python and asserts it reads the committed BD37 fixture as SURVIVED. Two byte-exact fixtures are committed, each verified to classify identically to its 4–5 MB original: the crash-looping one and a **healthy** one, because a checker that VOIDs everything passes every crash rail.

**AND THE FIRST CUT COULD NOT SCORE AN XCTest EXPECTATION AT ALL — every rail above was green while it could not (review R1).** `gate_baseline` keys an XCTest case off the whole bracketed name, module included (`xctest::PlayheadTests.SomeTests/testFoo`), because its `_XC_RESULT` group is greedy; the MUTATIONS table spells the same expectation `SomeTests/testFoo`, because the scraper this bead DELETED used a non-greedy prefix and threw the module away. Compared literally the two never meet, so every such expectation resolved ABSENT — and ABSENT is the arm that exits 2 out of the baseline preflight with *"an expectation names a test that never ran"*, i.e. **playhead-le02's whole XCTest half refused to run**. 48 mutations carry such an expectation as their only one; the new table rail counts **72 distinct** unresolvable expectations across the table. The fix is the dotted-suffix match `gate_baseline._blamed_matches` already uses for the `Failing tests:` block's third spelling. **The lesson is about the RAILS, not the resolver:** all twelve original shell rails drove `M05`, whose expectation is a Swift Testing DISPLAY name, so the entire XCTest naming system was outside the population they measured. When a mechanism has two identity models, a rail that only ever exercises one of them is a rail about one of them.

**FIVE MORE THINGS THE SAME ROUND FOUND, and they are one family — an instrument that goes quiet, making a claim:**

- **The batch's observed-failure LIST became a COUNT.** The pre-bead battery printed `observed failures:` and every name under it; the fix printed `3 failed` and named only the tests some mutation DECLARED. That is exactly what `docs/investigations/playhead-8cjo-mutation-ledger.md` is built on — *"a mutant that reports KILLED while killing a DIFFERENT set is a false credit"* — and a count cannot show it. Restored from the scorer's own `#failure` lines, which were already being written and were being read only by the baseline.
- **A quiet scorer made the battery blame the TREE.** `scored_field` prints `""` for a field that is missing, and `"" != "0"` read as a RED BASELINE — printed with no failures listed under it and a remedy ("fix the tree first") nobody can act on. Driven with a scorer that omits one header line. The absence has its own arm now and it names the FILE. The batch half of the same read says `observed failures: UNKNOWN` rather than `(none)`, because **`(none)` is a measurement and is earned only by a STATED zero** — and a batch can reach it with the baseline skipped.
- **`print_evidence` printed NOTHING when there was no evidence line** — which is every ERROR raised before scoring, while the script's own comment promised a path "for every verdict that is not a plain KILL". The table is the part that gets quoted, so a row with no path under it is a row whose reader has nowhere to go. A missing line and a line saying "none" are two different claims.
- **`awk -v want="$2"` ESCAPE-PROCESSES the value.** An expectation containing a backslash read NO-STATE and would have reported VOID on every run for ever, while the scorer resolved it perfectly — the two halves of one file format disagreeing, which is this bead's own shape. Not reachable from today's table (0 of 1,910 expectations carry one) and one word to close: the name goes through the environment.
- **`drop_bundle`'s `"$WORK"/*.xcresult` guard could be climbed out of.** `case`'s `*` crosses `/`, and the shipped function given `$WORK/../outside.xcresult` removed it — driven. Not reachable from either caller, and that is now ASSERTED (both call sites, and the numeric batch field in all 1,109 records) rather than left for a reader to re-derive. CLAUDE.md's rm -rf rail is that the path is proved, not assumed.

**AND THE SECOND ROUND FOUND THE SAME FAMILY ONE STEP FURTHER OUT — THE SCRIPT'S OWN CLOSING SENTENCE (R2).** R1 closed five instruments that went quiet; R2's headline is an instrument that spoke up without checking. The battery ended a red run with *"Every SURVIVED above rests on a POSITIVE pass verdict … **read from the batch's own .xcresult bundle** — the log and bundle are printed beside each one"*, printed **unconditionally**. But `score_batch` omits `--xcresult` when the directory is not there, so a run whose bundle never arrived is scored off the CONSOLE and then told, in the one place a reader decides whether to trust a SURVIVED, that it came from the bundle — and the evidence line under it prints `-` for the bundle it just claimed to have printed. **The bare `[ -d "$bundle" ]` is the `scripts/sim-trim.sh` shape exactly (playhead-81ig): a guard whose false branch makes no claim, which shipped INERT for two full plans with byte-identical logs either way.** Here the only difference between a bundle run and a console run was one line of the scorer's census — `verdicts read from: the console log` — which reads as a neutral fact about provenance rather than as the fault it is in a caller that asks for a bundle on *every* run. `note_bundle_presence` names it per batch now, the epilogue's sentence is true in both worlds, and the console-only case gets its own paragraph. Deliberately **not** fatal: a console-only SURVIVED still rests on a positive `✔`, which is the whole point of the bead, and refusing would turn a degraded run into no run.

Three smaller ones, each the MIRROR of an R1 fix — R1 closed the direction it was looking at and left the other, which is `playhead-o89d` R5's method arriving on schedule:

- **`observed failures (ALL of them, N)` could print `(none)` under a NON-ZERO N.** R1 armed "the scorer stated no count"; a stated 2 over an empty list printed the header saying 2 and then `(none)`, two contradictory readings of one file with nothing to say which to believe.
- **The no-tests ERROR arm hard-coded `-` for the bundle** while the two arms beside it check `[ -d "$BUNDLE" ]`. R1 fixed "a line that names a bundle that is not there"; this is "a line that says a bundle is not there when it is", and it meant `print_evidence` could never name it.
- **The VOID epilogue named one of its two causes.** "The expected test produced no result at all" is false for the arm that outranks every other: a batch that lost its host is VOID **even when the expected test FAILED**, which is `playhead-4xmz`'s DW18 and the reason the arm exists at all.

**AND THE RAILS WERE MONOCULTURAL IN A SECOND WAY, WHICH IS THE FINDING WORTH CARRYING FORWARD.** R1's lesson was that twelve shell rails all drove one Swift Testing display name, so a whole naming system sat outside what they measured. R2 asked the same question about the rest of the population and found four more shapes nothing drove:

- **No end-to-end rail ever READ A BUNDLE.** The single shell rail that created one asserted that an *unreadable* bundle is refused. So "the bundle is the verdict source" — the bead's entire subject — was a property of Python plus a property of a `case` statement with nothing joining them, which is the playhead-s34ux shape. Closed by shadowing `xcrun` on `PATH` in the sandbox so the scorer really parses a canned payload; every one of those rails makes the CONSOLE say the OPPOSITE of the BUNDLE, so a silent fallback fails rather than passing for the wrong reason. Removing the `--xcresult` plumbing turns all four red.
- **No rail drove a `;`-separated expectation** — 419 of the 1,109 records carry one — because `setUpClass` actively REFUSED such a mutation. The arm order that makes one *unjudged* expectation outrank one that *passed* was measured by nothing that runs the battery.
- **No rail drove a batch with more than one member, or the `*99` vacuity-control APPEND** — 337 records reach the append, and it is the only path that runs `run_focused` twice, writes two of everything and captures a freshness floor per BATCH rather than per run.
- **The table rail covered one of the two XCTest spellings.** R1's rail measures `Suite/testFoo`; a bare `testFoo` (22 expectations) had no table-level rail, and deleting that arm of `candidate_keys` left it green. The replacement takes **all 1,910 expectations across all 1,109 records**, sorts them into the three spellings the battery uses, and asserts every one resolves — through the console AND through the bundle. Deleting the dotted-suffix arm reports exactly the 72 unresolvable expectations the R1 paragraph above derives; deleting the bare arm reports 10 more that nothing had ever measured.

**AND THE THIRD ROUND FOUND A RED TEST THE FAILURE COUNT DOES NOT HOLD — a LOOSENING this bead introduced, in the one place it exists to tighten (R3).** R1 and R2 were both about the battery's own output. R3 went at the seams instead, and the finding is on the boundary between this battery and `gate_baseline.py`.

`gate_baseline` routes a **resource denial** out of `failures` and into its own category, on `playhead-s34ux`'s rule that a denial names the BOX and not the code. That rule is right and is kept. What it costs HERE is that `#failures` reads **0** for a run whose console carries `✘ … failed` lines. Driven, one denied test and one passing expectation, same console both sides:

```
pre-bead (git show a82e52a9:)   rc 2, "the focused suites are RED before any mutation", the test named
this bead as it stood           "baseline green", then  M05 SURVIVED
                                over  observed failures (ALL of them, 0):  (none)
```

So the strictest guard in the script — the one that refuses to score anything against a red tree — stopped seeing an entire category of red test, silently, and `(none)` was printed over a batch that had one. It is refused now, with its own arm and its own remedy: **"fix the tree first" is unactionable for a denial and the honest advice is RE-RUN.** It is refused rather than merely reported because the tolerance is not symmetric — the per-test rule is UNANIMITY, so a test recording one `CANTOPEN` and one genuine assertion stays a FAILURE, and on a box that is denying descriptors the next mutation's KILL may be the box rather than the edit. `mutation_verdict.py` writes `#resource` and `#denied` beside `#failures` and `#failure`; the batch half names the denials next to the failure list rather than folding them in, because the two are different claims with different remedies. (`crashed` needed nothing: it is already a `#reason`, so the batch is VOID and every caller is loud.)

**And the CONSOLE-ONLY warning was attached to the wrong verdict.** R2 made a run that lost its bundle stop claiming the bundle under **SURVIVED**. But `playhead-t53a`'s measurement is that the console MANUFACTURES lost verdicts — 80 of 87 reported casualties on 27 crash-free full-plan logs were verdicts the parser could not read — so the verdict a console-only reading most plausibly fabricates is **VOID**, not SURVIVED. That epilogue was telling a reader to re-run, and if it recurs to suspect the mutation of killing the test host, on runs where the likelier cause is that `PLAYHEAD_RESULT_BUNDLE` never reached fast-gate. **ERROR is the third one**, for the same reason one layer along: `expected test never ran` is ABSENT, and a START line the console lost makes a test ABSENT just as surely as a typo does. All three epilogues carry it now, out of one `console_only_note` so the three cannot drift, each with the sentence its own verdict needs and all three opening on the line the R2 rail greps for. Same shape as R2's own finding, one verdict over: R2 closed the direction it was looking at.

Three smaller things from the same round:

- **`62 + 29 + 4` is not 104.** The rails line quoted two different granularities in one sentence — 62 counted whole CLASSES that never touch the battery, 29 and 4 counted individual rails — and the nine rails that sit in a battery-touching class while touching nothing themselves fell out of both. Re-counted off the AST and stated so it adds up.
- **`line 24,986 of 24,996` is 24,987.** R1 corrected the DISTANCE in that sentence (from a bad "twenty-five lines from its end") and left the line number one out. The specimen is still on disk and `grep -n` settles it. Everything else R1 and R2 re-derived was re-derived again and holds to the digit — **the figures themselves are stated ONCE**, in *DRIVEN AND FOUND CORRECT* at the foot of this section. R3 and R4 each carried a full copy of the same two lists (the `--list` table figures and the preserved-corpus reading) and a reader could not tell whether the two were one measurement or two; that is the shape this section exists to police, committed by the section itself (R5).
- **A kept `$WORK` is no longer just logs, and a `kill -9` is the case that keeps one.** Driven both ways against the real battery with a gate that writes a bundle and then hangs. **Ctrl-C** (SIGINT to the process group, which is what a terminal sends) is clean: rc 130, `interrupted — restoring sources`, the mutant gone, the lock released, no orphan, and `$WORK` — bundle and all — REMOVED, because `KEEP_WORK` is still 0 at that point. **`kill -9`** leaves the mutant, the lock, one orphaned `fast-gate` child (playhead-zsqh's shape, unchanged by this bead) and the whole `$WORK` including its `.xcresult`; the next run reclaims the stale lock by name, rescues a copy, restores the file and carries on, and nothing in that path reads or touches `$WORK`. So the new bundles are reaped by exactly one thing: `scripts/disk-cleanup.sh`'s 3-day sweep. It still needs no change — same glob, same floor — but bundles are big (the R4 paragraph below measures them), so read the "684 MiB" and "508 MiB" readings above as measurements of a LOG-ONLY corpus, taken before bundles were kept at all.

**AND THE RAILS THEMSELVES WERE THE THIRD RESERVOIR (R3).** R2 made these rails reap the `$WORK` they cause, on the ground that "a rail that adds to it while testing the fix for it is not funny". The same rails' own SANDBOX — a ~67 MiB copy of the tree, one per pass — went to `$TMPDIR` via a bare `tempfile.mkdtemp()`, and **nothing in this repo sweeps `$TMPDIR`**: `scripts/disk-cleanup.sh` handles `.worktrees/*`, `/private/tmp/playhead-*` and `$TMPDIR/Deleting-*`, and that is the whole list. `tearDownModule` drops it and does not run when a pass is INTERRUPTED. Measured at R3: **10 orphaned sandboxes, 670 MiB**, all from one earlier day of review rounds, on a box whose gate refuses below 13.5 GiB. Fixed the way `mutation-battery.sh` already fixed it for `$WORK` — one keyword, `dir="/private/tmp"`, so the existing 3-day cron finds them — rather than a second cleaner that drifts from the first. Pinned in SOURCE (`ScratchHygieneTests`), because the leak is invisible on any single green run. **And the anti-vacuity guard was vacuous first time**: it asserted the glob appeared ANYWHERE in `disk-cleanup.sh` and stayed green with the loop rewritten to sweep something else, because the section's own `# 2. /private/tmp/playhead-* cleanup` comment satisfied it. It matches the loop HEADER now; driven, 3 mutants, 3 killed.

**AND ONE OPERATIONAL TRAP, LEARNED BY FALLING INTO IT (R3).** Do NOT sweep `/private/tmp/playhead-mutation-battery.*` while a rails pass is in flight. `WORK` is `mktemp -d`'d at script LOAD — before the argument parse, before the lock, before anything is written — so an EMPTY one is very often a run that started a second ago, not a corpse. Removing one mid-pass cost a rail: `scripts/mutation-battery.sh: line 30162: …/results: No such file or directory`, verdict `None`, one red test in an otherwise green 114. The house defect class, in the cleanup rather than the code — an absence read as evidence. Reap by the path the battery PRINTS (`logs kept in …`), which is what `_reap_work_dir` in the rails does, and never by age alone.

**AND THE FOURTH ROUND WENT AT WHAT THE FIX COSTS ON DISK, WHICH IS THE ONE THING NOBODY HAD MEASURED (R4).** R1-R3 were about what the battery SAYS. This bead also made it start KEEPING an `.xcresult` bundle per batch, and the arithmetic of that had never been done.

**A bundle's disk cost is FILE COUNT, not bytes — measured, and it is why a focused one is not free.** A full-plan bundle on this box is **104-122 MB** and holds **22,120-26,088 objects**; the 12,603-test one is 26,088 objects whose CONTENT is **18.4 MiB** and whose ALLOCATION is **113.9 MiB**, because 26,078 of those objects are under one 4 KiB block. Across the three bundles whose test counts differ the slope is ~**2.2 objects and ~9.5 KB per test**. The focused suites run **2,748** tests — `gate_baseline.parse_run`'s distinct started keys on a preserved healthy batch log, not the 2,805 raw `◇ Test` lines in the same file, which is the same two-quantities-one-name trap this file is about — so a focused bundle is **~26-36 MB** by two independent derivations (the per-test regression, and the fixed 12 MiB of large objects plus 2.2 x 2,748 blocks). That figure is EXTRAPOLATED and says so: nobody can produce a focused bundle without `xcodebuild`, and no preserved one exists because this bead is what started keeping them.

**What a long run holds, and why the retention policy is right as it stands.** A bundle is dropped exactly when its batch produced nothing but KILLs, so the number kept is the number of verdicts somebody might come back to. The largest series is **BD at 41 batches** (`DW` is 37, `LX` 34; 806 distinct batches across all 1,109 records, which is a run nobody makes). All-KILL costs one bundle at a time; every-batch-non-KILL — which is precisely the crash-looping box this bead exists for — costs **~1.2 GB held to the end of the run** (41 x ~30 MB), on a box whose gate REFUSES below 13.5 GiB. **No cap was added and none should be:** a cap that dropped the oldest would destroy the evidence for early batches, which is the thing the bead exists to protect. What was missing was not a cap, it was that nothing SAID what was being held.

Three things changed, and one of them gives back more than the bundles cost:

- **`$LOG.last` was a BYTE-EXACT DUPLICATE of the largest artifact in `$WORK`, kept for one `grep`.** It had three readers — `extract_ran`, `extract_failures` and the `Test run with` guard — and this bead deleted the first two. Measured on a preserved 37-batch run (`/private/tmp/playhead-mutation-battery.9NQnSD`): **178.9 MiB of `.log` beside 178.9 MiB of `.log.last`**, exactly half that directory. (R5: that figure is `du`'s MiB and was written MB in all three places that carried it — 187.6 MB. The reading was right and the unit was not, which is the standing defect class one size down.) It is a predicate now (`last_attempt_ran_tests`), which does the cut and the search in one pass and writes nothing. Driven over all four attempt shapes, including the load-bearing one — attempt 1 ran tests, attempt 2 did not — which still reports "no tests".
- **The kept-work line named a CATEGORY where a measurement belongs.** `per-batch xcodebuild logs kept in $WORK` was the whole of what a reader deciding whether to reclaim had to go on, while the directory had started holding the biggest thing in it. It states the size and the bundle count now, and a rail compares the stated count against the directory's real contents.
- **`mb_diagnose_no_tests`'s `rc=28` DISK arm named two reservoirs and not the one the run is sitting on.** That refusal is the one failure the retention policy can cause, and both remedies it printed are useless for it: `disk-cleanup.sh` sweeps `/private/tmp/playhead-*` at **3 days** and the directory is minutes old, so a reader following the advice reclaims nothing and concludes the box is short. It names `$WORK`, its size and its bundle count now. (The two cleaners do NOT disagree about ownership, checked: the `xcresult-superseded` rule is scoped to `.worktrees/*/.derivedData/Logs/Test`, and the battery's bundles are reaped only as part of the whole `$WORK` directory.)

**And a FOURTH way the batch could go quiet, found by asking which of `gate_baseline`'s vocabulary this module does not use.** `parse_xcresult` files a Test Case whose `result` is none of Passed / Failed / Skipped / Expected Failure into `run.unjudged`, keyed and carrying the word it could not read. `_state_of_key` never consults it, so such a test falls through to NO-VERDICT — conservative and right — but the BATCH read **OK**, so the run printed `VOID | the expected test reached NO VERDICT` and its epilogue told the reader to re-run and, if it recurred, to suspect the mutation of killing the test host. **Re-running cannot clear an unreadable result string**, and the module had the answer in its hand the whole time. It is a stated batch reason now, naming the count, the test, the word, and the remedy (`gate_baseline.py`'s `XCRESULT_*` vocabulary), plus an `unreadable` column in the census. **Measured before it was added, because a reason that fires on a healthy run is worse than no reason: `unjudged` is EMPTY across four preserved full-plan bundles, ~48,300 nodes.** Two rails, driven with six mutants (6/6 killed, control survives) — and the FIRST cut of the rail let one through: it pinned the consequence ("do not re-run") and left the ACTION unpinned, so deleting the only sentence saying where the fix goes was invisible. `playhead-o89d` R5's method, on schedule.

**ONE THING FOUND AND FILED RATHER THAN FIXED, because it is older than this bead and bigger than it: `playhead-fyma7`.** A Swift Testing key IS the display name, so two tests in different suites sharing one are ONE key to everything downstream. Counted off a real full-plan bundle: **63 display-name keys are claimed by more than one suite**, and **three MUTATIONS records name one** — `JC08` (2 suites) and `SF02`/`AR09` (3), with at least two of each collision's suites inside `FOCUSED_SUITES`, so both twins really do run in the same focused batch. `_PRECEDENCE` puts FAILED first, so a mutation whose declared victim's NAMESAKE fails is credited KILLED — the false-kill shape this file calls silent and indistinguishable from success. The scraper this bead deleted keyed on the same display name, so nothing about it is new; the SURVIVED direction is safe by construction, because `PASSED` is last.

**AND THE FIFTH ROUND WENT AT COHERENCE, AND AT THE ONE THING NO ROUND HAD QUESTIONED (R5).** R1-R4 each went at what the battery SAYS and each closed the direction it was looking at. R5 was set two questions none of them had asked: read the whole record end to end as a first-time reader who has to act on it, and interrogate the ladder's PRECEDENCE rather than its states.

- **The census counted two of its own columns twice.** `no_verdict` is `started - ran - skipped - resource`, so on one line of `·`-separated counts a CRASHED key and an UNREADABLE one are each counted a second time inside `NO VERDICT` while `resource-denied` is genuinely disjoint. Driven on a four-test batch: **the columns after `started` add to SIX.** Three columns that look like siblings behaving three different ways, in the census this bead built to stop exactly that. The counts that PARTITION are on the census line now; the two that DIAGNOSE are on their own, saying they are already counted above and are never extra tests. Both print unconditionally, zeroes included.
- **`mb_diagnose_no_tests` has TWO disk arms and R4 fixed one.** `DISK EXHAUSTION mid-run` sat eight lines below the `rc=28` arm still printing the two remedies R4 had just measured as useless — and it is MORE the run's own doing, because that batch's preflight had already passed. One function, called from both. **R4's own rail was vacuous on the count**: it asserted `2 .xcresult bundle(s)` against a fixture holding two, so the literal `2` survived it. The two rails build different numbers now and each asserts the other's is absent.
- **"Run it again" was in THREE places and R4 qualified ONE.** The BASELINE VOID arm printed `RE-RUNNING WILL NOT CHANGE IT` and then, two lines later, `Re-run.`; the header's exit-code list — what `--help` shows an operator looking up a 5 — said it flat. **And a SECOND shape never clears either:** an expectation reading `[SKIPPED]`. R4's exemption was scoped to batch `#reason` lines and a per-test state is not one, and the baseline has already watched that test PASS unmutated minutes earlier, so the MUTATION is the likeliest reason it skipped. That is a finding about the PAIRING of mutant and rail; the remedy is a different rail or a different mutant.
- **The console-only warning claimed more than one flag can know.** `CONSOLE_ONLY` is set by the baseline AND by every batch, so *"Those verdicts came off the CONSOLE alone"* is false whenever the BASELINE lost its bundle and the batches kept theirs. R2 fixed that note's HEADER to name RUNS and left the verdict sentence claiming the stronger thing — invisible because **every console-only rail drops BOTH bundles**, so the mixed run was outside the population they measured. **That is the THIRD monoculture finding on this branch** (R1: twelve rails, one display name; R2: four shapes nothing drove), and it is the question worth asking of any rail set: which shapes can it not build?
- **The ladder's precedence had no stated WHY, and "no round questioned it" is not one.** `VOID` outranks `FAILED` and `ABSENT`, `SKIPPED` is UNJUDGED, and all three are right — the arguments are in the source now with the counter-case CONSTRUCTED: a real kill discarded because a BATCH-MATE trapped, whose remedy is `--only`, one build. **The order is right because of the ASYMMETRY, not the frequency:** discarding a real kill costs a scoped re-run and announces itself; crediting one off a dead host is a FALSE KILL that enters the ledger silently. Also recorded: `STATED` and `UNJUDGED` have NO reader in the product — the shell's `case` enforces the rule — so a rail asserting `SKIPPED in UNJUDGED` is a rail about a frozenset, an INERT mutant wearing a KILLED verdict.
- **And the record itself had grown the defect it polices.** Two paragraphs opening *"Driven at R4 and found CORRECT"* and *"Driven and found CORRECT"* sat ADJACENT, listing overlapping mutants with nothing to say which run measured which, while the `--list` figures and the corpus reading were each stated in full twice more. Consolidated below, tagged by round, rather than corrected by appending.

**DRIVEN AND FOUND CORRECT — ONE STANDING LIST, so no round re-drives them.** R3 and R4 each wrote their own version of this paragraph and the two ended up ADJACENT, opening with almost the same sentence and listing overlapping mutants with nothing to say which run measured which; R5 merged them rather than adding a third. Each item carries the round that drove it.

- **The figures, and this is the only place they are stated.** Off `--list`, re-derived at R3, R4 and R5 and identical every time: **1,109 records, 1,910 expectations, 419 carrying a `;`, 128 `Suite/method` and 22 bare `testFoo`, 48 records whose sole expectation is qualified, 806 distinct batches of which 160 hold exactly two and 57 hold more, 337 records reaching the `*99` append, zero expectations empty or edge-whitespaced, every batch field numeric.** Off the specimen, which is still on disk: **line 24,987 of 24,996, 11 distinct pids, 11 `Restarting`, 20 `Fatal error`, a 0-byte `failed-1457.txt`, census 2,688/356/0/2,332.** The preserved-corpus reading is stated once too — the R1/R2 table above — and it is the DIRECTION there that is worth quoting, not the counts.
- **Cost.** `--list` costs **13.96 s** (14.20 s at R5) and an invocation that selects NOTHING costs **11.66 s**, which is the fork-per-record preflight and the whole of the "~12-15 s per invocation" claim (R4).
- **Paths driven, none of which turned up a defect** (R3 unless marked): the wedged-simulator retry under the new scorer, both directions — attempt 1 FAILED / attempt 2 PASSED reads PASSED and OK, and the mirror reads FAILED, because `gate_baseline.parse_run` cuts at the retry banner while `gate_memory_verdict.last_invocation` cuts at the last `Command line invocation:` and fast-gate prints the banner before it re-runs; `--dry-run` (applies, diffs, restores byte-exactly, `voids: 0`, exit 0); `--help` (**411** lines at R5 — 407 at R3, 408 at R4 which re-counted it off the same awk, and R5's three are the exit-5 entry naming which VOIDs a re-run can never clear); an unknown argument, `--series ""` and a selection matching nothing (all exit 2); `--only` on a mutation whose series has no `*99` control; and `kill -9` / Ctrl-C, written up in the `$WORK` bullet above.
- **Mutants, by the round that ran them.** R2: **ten** over `mutation_verdict.py` — the dot anchor, the precedence order, the freshness comparison, two `_classify_batch` reasons, the TAB refusal, the failure list, `ABSENT`-vs-`NO-VERDICT`, `SKIPPED`-as-`PASSED` and the whole `--xcresult` read — killed 10/10 by the rails R1 and R2 had added. R4: **seventeen** over the shipped module (R2's ten plus the unknown-key fall-through, the `#resource` line, `_named`, the `rc` 137/143 arm, both `candidate_keys` arms, the pid threshold, the terminal-marker read, the bundle-stated crash, the restart marker and the signal read), **six** over R4's own new rails and **three** over the shell. R5: **six** over the census, **five** over the disk clause. Every one killed; every control survived. **The R2 and R4 lists OVERLAP and used to be printed as though they did not** — the same nine mutants, two measurements, two paragraphs.
- **Non-questions, recorded so nobody re-asks (R4).** `RosterMismatch` is declared and RAISED NOWHERE in the repo, so no caller can survive it. `XCRESULT_EXPECTED_FAILURE` lands in `passed` and would licence a SURVIVED, but the tree contains **zero** `XCTExpectFailure` and zero `withKnownIssue`, and the vocabulary is `gate_baseline`'s, shared with the merge gate. The **three cuts over one log** agree on every preserved battery log — all 46 carry exactly ONE `Command line invocation:` and none carries a wedged-simulator banner; the one constructible disagreement is a wedged retry whose second `xcodebuild` never starts, and the `Test run with` guard refuses that run before the scorer is reached. **Log-and-bundle identity is by construction** (`$WORK/batch-$b.log` and `$WORK/batch-$b.xcresult`, both written by this batch's own `run_focused`), so the freshness floor is belt-and-braces there; the only pairing nothing bounds is the `verify` hand tool, which takes two paths from a human, has NO caller in the product, and says "freshness only" in its own help.


### Two DIAGNOSTIC instruments nobody runs automatically (playhead-vk68m)

**Neither is wired into `fast-gate.sh` or `lint.sh`, deliberately — and that
means their 88 rails run only when somebody types the command.** Both were built
to answer one question and are kept because the question recurs; if you are
touching descriptors, the ceiling, or the gate's fd line, start here rather than
writing a third one.

```bash
# WHAT is the test host holding, by PATH — not just how many.
python3 scripts/gate-fd-paths.py --pid <testhost>            # one snapshot
python3 scripts/gate-fd-paths.py --pid <testhost> --cross-check   # + lsof, split at the FD column
python3 scripts/gate-fd-paths.py --watch --interval 10 \
    --out run/summary.jsonl --last run/last.json \
    --peak run/peak.json --full-dir run/full            # alongside a gate

# Do the runs that lose their host also reach the ceiling? (preserved logs)
python3 scripts/fd_ceiling_sweep.py --csv artifacts/fd-ceiling-sweep.csv

# The rails for both — ~12 s, no build. RUN THESE IF YOU EDIT EITHER SCRIPT.
python3 -m unittest scripts.tests.test_gate_fd_paths scripts.tests.test_fd_ceiling_sweep
```

Three things to know before you trust either one:

- **`gate-fd-paths.py` proves its own ctypes layout before it will sample.** A
  wrong struct offset does not fail, it returns a plausible string from the
  wrong bytes, so `self_test()` opens a temp file at a path this process chose
  and requires the path back exactly. A failure exits **3** and samples nothing.
- **What it counts and what it excludes.** The population is the process's fd
  TABLE via `PROC_PIDLISTFDS` — the same call, with the same head-room, that
  `gate-memory-sample.py` counts — so its number and `testhost_fds` in the
  memory series are the same quantity. `lsof -p PID | wc -l` is **not** that
  number and never was: it also lists `cwd`, `rtd`, `txt` and every mapped
  dylib. `--cross-check` runs lsof and splits at the FD column, counting only
  rows whose FD is a NUMBER and naming the excluded ones by kind.
- **The watcher PINS its host and the pin is a heuristic, not a guarantee.**
  It reports `pinned host pid N (holding C descriptors)` — read C, because a
  leftover app holds a couple of dozen and a test host holds hundreds — and any
  other process's dumps go to `*.pid<N>.*` rather than over the subject's. It is
  not oscillation-proof between two simultaneously-growing hosts; `--full-dir`
  keeps every sample with its pid, so nothing is lost either way.

### The gate can die of MEMORY, and it now says so instead of leaving a bare 137 (playhead-3rql)

**Six consecutive merge gates produced NO VERDICT across two branches on 2026-08-20, and the explanation everyone worked from was wrong.** The reading was a dose–response: one bead had added 38 tests, the plan was at its ceiling, and 38 more tests tipped it. The experiment that settles it had not been run. It has now:

| run | population | result |
|---|---|---|
| `EXP1` — keep playhead-shu5's 38 new tests, skip 38 UNRELATED ones (`TargetedWindowNarrowerTests`) | 11,569 — **identical to the pre-shu5 control that passed** | host killed at 90 % of the log, `Killed: 9`, `GATE_EXIT=137` |

Same count, different 38 tests, same death at the same place. **shu5's tests are innocent and the count is not the variable.**

**THE OUTCOME IS A COIN FLIP, AND HERE IS THE WHOLE DAY'S TALLY** — eight full-plan runs on one branch, same protocol every time (`.derivedData` removed, simulator shut down + erased + re-read, no `-skip-testing:` except where stated):

| run | configuration | outcome |
|---|---|---|
| `RUN2` (two invocations) | unmodified | **completed**, `** TEST SUCCEEDED **` ×2, 11,626 passed, 230.7 s and 211.3 s |
| `EXP1` | 38 unrelated skipped | **killed**, `Killed: 9`, exit 137 |
| `ACCEPT-1` | unmodified | **completed**, `** TEST SUCCEEDED **`, 251.6 s |
| `ACCEPT-2` | unmodified | **killed**, exit 137 |
| `EXP2` | serialized | **completed**, `** TEST FAILED **` (the four of playhead-sip2), 1,147 s |
| `ACCEPT-3` | unmodified | **completed**, `** TEST FAILED **` (3 starvation flakes), 282.3 s |
| `ACCEPT-4` | unmodified | **killed**, exit 137 |

Unmodified: **four completed, two killed.** Every kill has the same signature — ~414 tests in flight at ~90 % of the log, no `Restarting after unexpected exit` line, `Killed: 9`, no result bundle. Every completion has the same memory series as every kill. **Do not read a green gate as evidence that anything was fixed, and do not read a red one as evidence that anything broke:** on this box, at this ceiling, run it again.

**WHAT IS ACTUALLY SHORT, measured by sampling `vm_stat` + `vm.swapusage` every 10 s across whole runs and around bare simulator boots.** The quantity is **demand** = `active + wired + compressor + swap`:

```
macOS with the simulator SHUT DOWN                     6.4 – 9.8 GiB
+ a booted iOS 27 simulator, idle, settled 5 min      16.5 – 20.5 GiB   (200–290 processes)
+ the app launched, before one Swift Testing test     20.9 GiB
  the same run, at the moment it died                 20.5 GiB
box RAM                                               16.0 GiB
```

Two things follow, and both contradict how this failure was being read:

- **The run is already over the box's ceiling before the first test starts, and demand is FLAT across the test phase.** So "which tests ran" cannot predict the outcome, and no PerfGating, sharding or narrowing acts on the term that is large. A booted simulator costs 10–13 GiB on a 16 GiB machine; the whole test run adds about 2 GiB on top.
- **It is neither a leak nor a late spike** — the two possibilities the bead named. The single largest step in the series is the simulator's own boot (compressor 0.65 → 7.8 GiB, swap 0.5 → 7.6 GiB), and it happens between `simctl boot` and the first `◇ Test`.

**`Pages free` IS NOT FREE MEMORY, and the bead was filed on that reading.** macOS keeps very few free pages by design; this box at rest reads ~1.0 GiB free with 7.5 GiB reclaimable and is short of nothing. The 0.29 GB that made memory the prime suspect was a correct reading of the wrong quantity — the standing defect class. Use demand, or `free + inactive + speculative + purgeable` if you want the reclaimable figure.

**THE TEST HOST'S FOOTPRINT DOES SCALE WITH CONCURRENCY, and it is the only term the harness controls.** Measured with `/usr/bin/footprint -p` (phys_footprint, not rss — a page the compressor has taken is no longer resident, so rss falls while a process grows):

```
~1,000 tests in flight (scoped run)     285 MiB
~11,000 tests in flight (full plan)   2,110 – 2,223 MiB
serialized                              ~290 MiB
```

Swift Testing starts **essentially every test at once**: peak in-flight is 11,000–11,400 on every full-plan log, cold or warm, passing or dying. So serializing buys about **1.9 GiB** — a real fraction of the 5–7 GiB overshoot, but not all of it.

**Which knobs exist, measured, because three of the four obvious ones do nothing:**

- `SWT_EXPERIMENTAL_MAXIMUM_PARALLELIZATION_WIDTH` — the string is in `Testing.framework` and the variable does reach the test process, and **Xcode's XCTest-hosted Swift Testing ignores it**. Peak in-flight was unchanged at 962 with it set to 100, and at 82 with it set to 4.
- `TEST_RUNNER_<VAR>=…` on the `xcodebuild` command line — **not forwarded to a unit-test host.** Proved with `PerfGate`: `TEST_RUNNER_PLAYHEAD_RUN_PERF=1` left the gated test skipping.
- The test plan's `defaultOptions.environmentVariableEntries` — **works.** Same probe, same test, ran and passed. This is how `PlayheadPerfTests` sets `PLAYHEAD_RUN_PERF`, and it is the only route into the test process.
- The test plan's `"parallelizable": false` on the test target — **works, and is the only knob that bounds Swift Testing.** Peak in-flight 962 → 1.

**Serialization was measured on the WHOLE plan, once, and it is a real trade rather than a fix — which is why it is not on.** `EXP2`, same protocol as every other run here:

```
                      unmodified            serialized
test phase            230 – 252 s           1,147 s        4.6x
peak demand           22.7 – 24.6 GiB       20.2 GiB       -2.5 to -4.4 GiB
swap peak             10.4 – 29.3 GiB       7.8 GiB
host peak footprint   2,110 – 2,223 MiB     ~660 MiB rss
outcome               3 completed, 2 killed completed, 0 restarts, 0 NO VERDICT
```

It buys about 2 GiB of the 5–7 GiB overshoot and roughly quadruples the merge gate. It also turns **playhead-sip2** into four hard failures: `SkipOrchestratorRevertTests` has four tests that pass only because the scheduler interleaves them, and they fail in the full serialized plan exactly as they do scoped. Adopting it is a cost decision for the whole team, so it is written up in **playhead-blsh** rather than taken here.

**WHAT THE GATE DOES NOW.** `scripts/fast-gate.sh` samples memory for the whole run (`scripts/gate-memory-sample.py`, ~10 s interval, no flag to forget) and ends by classifying the outcome (`scripts/gate_memory_verdict.py`):

```
COMPLETE    an outcome line in EITHER format, one host pid, no signal death
RESTARTED   a second test-host pid, or the restart marker
NO-VERDICT  `Killed: 9`, exit 137/143, or no outcome line at all
```

and, when it is not COMPLETE, printing RAM against the measured peak demand. Four things it deliberately gets right, each because a reading went wrong that way first:

- **The host pid is checked independently of the restart marker.** `Restarting after unexpected exit, crash, or test timeout` is xcodebuild's, it is buffered, and EXP1 lost its host without it ever being printed. The pid is the app's own testimony.
- **The peak DURING the run is reported separately from the peak AFTER it went quiet.** The most expensive memory event in a killed run is the aftermath: xcodebuild collecting diagnostics from a 200-process simulator drove swap from 8.3 GiB to 30.2 GiB, six minutes after the last test. Quoting that as what the tests needed is the same defect one layer along.
- **A column an older sampler did not write reads `not recorded`, never `0.0 GiB`** — a printed zero would invent a disk-full diagnosis.
- **Only the LAST `xcodebuild` invocation in a log is judged.** A log can hold several (the wedged-sim retry appends to it; the residual pass is a second one), and each brings its own host pid, so counting pids over the file reports one-pid-per-invocation as a mid-run restart.

Rails: `python3 -m unittest scripts.tests.test_gate_memory_verdict` — 20 tests, ~0.03 s, no build.

**DO NOT EDIT A SHELL SCRIPT WHILE A RUN OF IT IS IN FLIGHT.** `bash`/`zsh` read a script incrementally by byte offset, so an edit mid-run makes the shell resume at a shifted offset. This bead's own RUN2 did it: `scripts/fast-gate.sh: line 284: he: command not found`, followed by a second complete `xcodebuild test` of the whole plan. It cost a run and nearly cost a wrong conclusion — the second invocation's `tee "$LOG"` (no `-a`) truncated the log the verdict was about to read.


### The gate boots a TRIMMED simulator (playhead-blsh)

**The merge gate used to run 5-7 GiB over this box and whether it survived was close to a coin flip.** On 2026-08-20 the same plan produced six void merge gates, two `GATE_EXIT=137` kills and four clean passes, and **nothing about which tests ran predicted it** — proven directly by keeping the 38 tests everyone suspected and removing 38 unrelated ones, which reproduced the death exactly (playhead-3rql). The term that is large is not the tests. It is the simulator.

Measured here, `demand = active + wired + compressor + swap`, sampled every 10 s. **`Pages free` is NOT free memory** — this box reads ~1 GiB at rest and is short of nothing; an earlier diagnosis was built on `Pages free` and was wrong.

| | processes | demand |
|---|---|---|
| macOS, simulator SHUT DOWN | 0 | 6.98 GiB |
| booted iOS 27 simulator, idle, settled 5 min | **301** | **20.33 GiB** |
| the same, `scripts/sim-trim.sh` applied | **112** | **13.10 GiB** |

So an idle simulator costs **+13.35 GiB** on a 16 GiB box, and the trim removes **7.23 GiB of it** — more than half — before xcodebuild has compiled anything.

**Full plan, four configurations, identical protocol each time (`.derivedData` removed, simulator shut down + erased + both verified by re-reading state, memory sampled every 10 s, no `-skip-testing:`). Only the `B only` row is the shipped configuration; the rest are the measurement that decided it.**

| config | pre-run procs | pre-run demand | PEAK demand | peak swap | test-host peak rss | test phase | outcome |
|---|---|---|---|---|---|---|---|
| neither | 320 | 20.77 GiB | **23.36 GiB** | 10.62 GiB | 2.05 GiB | 207.3 s | 11,626 passed |
| B only | 108 | 12.70 GiB | **13.20 GiB** | 0.88 GiB | 2.42 GiB | 243.2 s | 11,626 passed |
| C only | 313 | 19.94 GiB | **22.06 GiB** | 9.21 GiB | 1.28 GiB | 1052.6 s | 11,626 passed |
| B+C | 122 | 12.74 GiB | **13.74 GiB** | 1.63 GiB | 1.28 GiB | 1004.3 s | 11,626 passed |
| B+C | 108 | 11.52 GiB | **13.00 GiB** | 0.64 GiB | 1.25 GiB | 1677.1 s | 11,626 passed |

Every one of the five had **0 host restarts and 0 NO VERDICT**. **B and C do not weigh the same, and the table is why the order matters if either is ever questioned:** B removes **10.16 GiB** of peak demand and 9.7 GiB of swap and costs nothing; C removes **1.30 GiB** and costs **5.08x on the test phase** — a second observation of the bead's 4.6x, on a different tree, and worse rather than better. They are close to independent (B acts on the simulator, C on the test host) but **C adds nothing measurable on top of B**: once the simulator is gone the box is no longer short, and the test host's 0.8 GiB was never what killed the run. Baseline peaks 7.4 GiB OVER a 16 GiB box on 10.6 GiB of swap; with B it peaks 2.8 GiB UNDER with swap untouched; **C alone still peaks 6.1 GiB over on 9.2 GiB of swap.** **That is why C was dropped and B shipped alone (Dan, 2026-08-21).** Note also that the two B+C phases differ by 673 s with near-identical memory, so the serialized phase's DURATION is itself load-sensitive — one more reason its 5.08x is a floor rather than a price.

**`simctl boot --disabledJob=<label>` is INERT on this runtime and that is measured, not assumed.** Booted with six `--disabledJob` flags on a freshly erased device and settled five minutes, all six were RUNNING, 293 processes against a vanilla 301, demand 20.17 against 20.33 GiB; `launchctl print-disabled` inside the device lists none of them. **Read the settling curve before you re-test it**: at ONE minute three of those six read "absent" and it looks like a partial win — they were merely not started yet. The earlier report that "process count fell only 230 → 201" is almost certainly that same artefact.

What works is launchctl's own, inside the device: `simctl spawn <udid> launchctl disable user/<uid>/<label>` (so it cannot be demand-launched) followed by `bootout` (so the running one goes). Both are supported launchctl operations. `scripts/fast-gate.sh` applies them on every run; the list is `scripts/sim-trim-jobs.txt`, one label per line with the family and the reason.

**READ THE LOG FOR `fast-gate: simulator processes after trim: <n>` BEFORE BELIEVING A RUN WAS TRIMMED (playhead-81ig).** It shipped INERT and nothing said so: the UDID was resolved only from an `id=` destination, the default destination is a NAME (`platform=iOS Simulator,name=iPhone 17`), so `SIM_ID` was empty, the `[ -n "$SIM_ID" ]` guard took its false branch in silence, and a trimmed run and an untrimmed run produced byte-identical logs. Two full plans were accepted as evidence for a trim applied to neither — while every run in the bead's own measurement set `PLAYHEAD_SIM_ID` through its harness, which is precisely why the hole was invisible there. A name destination now resolves too, and **every skip path prints that the run is UNTRIMMED**. It is the house defect class one layer up from where `sim-trim.sh` already fights it three times over: a guard that names an ABSENCE, whose false branch makes no claim.

Four things to know before you touch it:

- **The admission rule is mechanical, and `scripts/sim-trim.sh` enforces the other half.** A label goes in the default tier only when the framework it backs is imported NOWHERE in `Playhead/` or `PlayheadTests/` — measured, `HealthKit CoreLocation MapKit Photos EventKit Contacts WidgetKit ActivityKit CoreSpotlight CallKit Intents WebKit Vision` are all **0 imports**. The KEEP list in the script names the daemons behind the frameworks that ARE imported (SpringBoard and RunningBoard for launching the host and for SCENE PHASE, `dasd` for BGTaskScheduler, `nsurlsessiond`, `mediaremoted`, `usernotificationsd`, `storekitd`, `mobileassetd`, `installd`…), and **a job file that names one of them fails the run** rather than being skipped.
- **Siri / Apple Intelligence / speech is NOT trimmed, and that is deliberate.** Those 23 labels are in `scripts/sim-trim-jobs-tier-b.txt`, applied only with `--include-tier-b`, because `Speech` and `FoundationModels` really are imported. Turning them off is a COVERAGE decision and it is Dan's.
- **The verdict is the process count, not the exit code.** Every operation here can report success and change nothing — that is exactly what `--disabledJob` does — so the script re-reads `launchctl list` afterwards and names every label it was told to remove that is still running.
- **THE TRIM OUTLIVES `simctl erase`.** launchd's overrides live in `/private/var/tmp/com.apple.CoreSimulator.SimDevice.<udid>/disabled.plist`, OUTSIDE the device's data directory, so erasing and rebooting leaves every disable in place. Convenient for the gate and a trap for a CONTROL: a run you believe is untrimmed is trimmed unless somebody undoes it. `scripts/sim-trim.sh --restore` is the undo, and the restored jobs only start on the NEXT boot.

**OPTION C — SERIALIZING SWIFT TESTING — WAS MEASURED AND WAS NOT SHIPPED FOR MEMORY (Dan, 2026-08-21). IT IS IN THE TREE NOW, FOR A DIFFERENT REASON (Dan, playhead-vk68m, 2026-08-24) — read this paragraph and the next together.** `"parallelizable": false` on the PlayheadTests target is the ONLY knob that bounds Swift Testing's concurrency under Xcode (the experimental env var reaches the process and is not honoured, playhead-3rql), and it works: the test host's peak rss falls 2.05 → 1.28 GiB. On the MEMORY question the 08-21 call still stands and its arithmetic is unchanged — the framing then was "saves ~1.9 GiB of a 5-7 GiB overshoot", B removes the overshoot outright, so on top of B, C buys 0.2-1.2 GiB of margin nobody needs. What changed is that a second, unrelated resource turned out to be the binding one.

**WHAT OPTION A DOES NOT FIX: THE FLOOR (playhead-882eg).** Serialization collapses the PEAK and leaves the floor exactly where it was — **449 vnodes in BOTH regimes**, measured on two full plans one line apart. That floor is ~81 `PlayheadRuntime` object graphs that tests build and never release, each holding ~5 descriptors on the app's PRODUCTION store paths, and it is now **98 % of everything the test host holds**. It grows by ~5 descriptors for every new runtime-constructing test, so the headroom serialization just bought is not permanent. Do not read a 17.9 % peak as "descriptors are solved".

**WHAT PUT IT IN THE TREE: FILE DESCRIPTORS, NOT MEMORY (playhead-vk68m).** `TestPlans/PlayheadFastTests.xctestplan` carries `"parallelizable": false` as of playhead-vk68m. `PlayheadIntegrationTests` is deliberately left PARALLEL — its ~11,800 Swift Testing tests are the same population and would behave the same way, but the change is UNMEASURED there and nothing on this box has run that plan under it. **Do not repeat the reason this line first gave**, which was that the plan adds twelve XCTest classes including eight interruption-CYCLE suites whose subject is timing: `parallelizable: false` bounds SWIFT TESTING's width, and XCTest in a single host is already serial, so the flag does not touch those eight at all. Right decision, wrong reason — the standing defect class, written into standing guidance. The consequence to know is that the phase-close gate still runs at the descriptor ceiling. The reason is the ceiling playhead-s34ux measured above: the parallel plan runs at **95.3 % of `RLIMIT_NOFILE` soft 2,560** with a rotating set of tests denied a file, and serialization takes the peak to **17.9 %** and the RESOURCE casualties from **27 to 0**. **Quote 10.56x, not 5.08x, for what it costs.** Measured on this tree, same simulator, same trim, both runs preserved: Swift Testing phase **252.792 s → 2,668.459 s (10.56x)**, whole test operation 359.741 s → 2,766.389 s (7.69x) — roughly double blsh's figure, which its own two runs already showed to be a floor rather than a price. Memory did NOT improve (13.9 → 14.2 GiB peak demand): serialization bought descriptors here, not RAM. The full before/after is `artifacts/MEASUREMENTS.md`, sections M0 and M4. **And know the consequence before the next merge gate:** `BASELINE IS FICTION` fires on ANY run with zero failures and zero resource casualties against a non-empty baseline — it is unconditional on the regime and has been since it was written, and this file already records five consecutive PARALLEL zero-failure plans that would have tripped it identically. What serialization changes is the FREQUENCY: the 118 entries were recorded under the parallel regime, they pass serialized, and the denials that used to keep `run.resource` non-empty are gone — so a clean run becomes the normal case rather than a rarity, and it exits 65 with nothing wrong (observed: playhead-vk68m's merge gate, 11,789 tests, `** TEST SUCCEEDED **`, zero failures) — and `--accept-baseline` cannot clear it, because `merge()` drops an entry only at `failed_runs == 0` and every recorded entry is already above that. Emptying `tests` is defensible on that evidence and it is **Dan's call**; `artifacts/MEASUREMENTS.md` M7 drives it rather than reasoning about it.

**KEEP `playhead-sip2`'s FIX — IT IS NOT AN ARTEFACT OF C, AND IT IS LOAD-BEARING IN BOTH REGIMES.** (This paragraph used to argue from "the parallel plan — the one that ships", which the paragraph above has now falsified for `PlayheadFastTests`. The fix is right either way, and playhead-vk68m's serialized merge gate ran all six `SkipOrchestratorRevertTests` green, which is the direct evidence.) C is what *exposed* four `SkipOrchestratorRevertTests`; it is not what broke them. In `.auto` mode `receiveAdWindows` promotes a managed window and persists `applied` from a **detached `Task`**. Under the parallel plan — which is what `PlayheadFastTests` was when this was measured, and is what `PlayheadIntegrationTests` still is — that task is starved past the end of the test, the row still reads `confirmed`, and the assertion passes **by luck**; serialized it lands first and reads `applied`. Measured: all five failing expectations reported `decisionState was applied`, and the veto's rollback was correct in both regimes. **A test that passes by luck under concurrency is worth fixing whether or not anything is serialized — more so, because concurrency is what hid it.** The general shape, which is the part to remember: **a test that reads a row the orchestrator writes from a detached Task, without draining `_waitForRecurrenceBackgroundWorkForTesting()`, is reading a value it never synchronized with.**

### The full plan runs ONCE, before merge — not per review round (Dan 2026-08-13)

**A review round uses `-only-testing:` over the suites its diff touches. The full plan is the merge gate and nothing else.** This is a measured decision, not a preference.

Across **61 preserved full-plan logs**: **649 NEW failures reported, and essentially none real.** Only 8 of the 61 contain a single Swift Testing failure under 97 s or any XCTest failure — and most of those 8 are deliberate mutant runs, controls or probes, where the failure *is* the intended result. Every other one of the 649 was `Time limit was exceeded: 60.000 seconds`, reported at 150–200 s wall clock, in the same handful of suites.

**Which suites, ranked rather than sampled (re-derived at o89d R3 over 38 full-plan runs, by the SOURCE FILE on the issue line, mean failures per run / runs present in):** `BackgroundGrantBudgetTests` 10.9 / 38-of-38 · `SilenceCompressionCoordinatorTests` 9.2 / 36 · `BackgroundProcessingServiceTests` 7.9 / 34 · `PlaybackInterruptionTests` 4.4 / 34 · `SpeechRecognitionRequestGateTests` 3.8 / 38 · `ForceQuitResumeTests` 3.7 / 37 · `DownloadShowAttributionTests` 3.7 / 20 · `BackgroundProcessingServiceLedgerTests` 3.5 / 35 · `RouteChangeTests` 2.9 / 37 · `BackgroundProcessingServiceTelemetryTests` 2.7 / 37 · `AnalysisPipelineStallInvariantHarnessIntegrationTests` 2.6 / 34 · `InterruptionHandlingTests` 1.8 / 35. R2's ranking over 28 runs reproduces within 0.4 on every shared entry, so **this is the stable part of the record**. Two additions worth reading: `DownloadShowAttributionTests` ranks 7th and is missing from R2's list, and it is an **assertion-kind** family rather than a starvation one — see the kind split above before treating the ranking as a PerfGate shortlist. **Two earlier top-6 lists — 2026-07-29's and 2026-08-13's — were read as evidence the population had MOVED ("near-disjoint, not one name in common"), and it had not.** They are two truncations of one stable ranking: the 07-29 list omitted the single largest contributor and the 08-13 list omitted the second and third, each of which is present in 34–36 of the 38 runs. Rank the whole thing before concluding anything drifted.

Those tests assert an **absolute wall-clock budget** while ~8,000 tests saturate a 16 GB box, so the quantity they measure is machine load and the quantity they are read as measuring is code correctness — this repo's standing defect class, living in the harness instead of the product. Selectively re-run, the same tests pass in **2–9 s** (measured repeatedly: 117 tests/10 suites in 8.6 s; 188/10 in 7.2 s; 74/11 in 4.4 s). `playhead-o89d` carries PerfGate-ing them.

The cost being removed is not machine time alone. Every RED gate obliges a reader to triage both failure formats, check every duration against 97 s, and read the `Failing tests:` summary block — a decision point on every round of every bead, which across those runs produced 649 judgements with no defects behind them.

**What this does NOT relax, because "fewer full runs" is easy to misread as "verify less":**

- **Mutation testing is the priority, not the casualty.** Across the seven beads of 2026-08-12/13, *every* real finding came from a surviving mutant, a behavioural probe, or a device pull — **not one came from a full-plan gate**. A scoped run was seconds when this was written and has NOT been re-measured since `PlayheadFastTests` was serialized (playhead-vk68m) — `mutation-battery.sh` drives the gate with `-only-testing:` against that plan, so re-time it before budgeting a round around the old figure. The point stands either way: a round should afford *more* mutants, not fewer.
- The merge gate is still a real full plan, still triaged in both formats, still with its `NO VERDICT` census read.
- `scripts/lint.sh`, the SHA-256 pin / byte-exact restore discipline, and the never-`git checkout --` rule are unchanged.

**Phase-close verification only (final gate before closing an epic):**
```bash
xcodebuild test -scheme Playhead -testPlan PlayheadIntegrationTests \
  -destination 'platform=iOS Simulator,name=iPhone 17'
```
True superset of FastTests — it skips nothing at all, so it adds back exactly the **twelve** XCTest classes the fast plan skips: ten in `InterruptionHarnessTests.swift` (eight `InterruptionCycleSuiteBase` subclasses plus `InterruptionHarnessTests` and `InterruptionHarnessInjectTests`), plus `MetricsCorpusIntegrationTests` and `FoundationModelShadowBenchmarkTests`, which are not interruption-cycle suites. **It said "the 20 XCTest interruption-cycle suites" until playhead-wwbr R1**, which is the same miscount the skip list itself carried — 20 is the number of entries that never worked, and it was never the number of classes this plan adds. `TestPlanSkipListCanaryTests` now pins the superset relation mechanically. Real-device runs are slower than the simulator because FM tests actually execute (on the simulator FoundationModels is gracefully unavailable). **Both the "~1 minute" this line used to claim and the "~3 minutes" quoted above were unmeasured, and the fast plan's real figure is now on record: 2,809 s (~46 min) serialized, playhead-vk68m 2026-08-25. This plan is a strict SUPERSET of it and is still PARALLEL, so it is not comparable to either number** — a superset cannot be three times faster than the plan it contains; the one datum on record is the 2026-08-13 wwbr merge gate, a full `PlayheadFastTests` at **~8 minutes** wall clock. Measure before quoting either.

**Why both plans use `skippedTests` only (no `selectedTests`):** Xcode's xctestplan filter honors XCTest class names but **silently ignores Swift Testing identifiers** in both `selectedTests` and `skippedTests`. Selecting Swift Testing tests via `selectedTests` ends up enabling 0 of them. We work around this by using `skippedTests` (which leaves Swift Testing always enabled) and only filtering XCTest classes. Per-class filtering of Swift Testing requires `-only-testing:'PlayheadTests/StructName/method()'` on the command line, not the test plan.

**That limitation had eaten two-thirds of the fast plan's declared contract, and the entries are now GONE rather than fixed (playhead-wwbr).** `skippedTests` listed 32 suites; **12 were honoured and 20 did nothing**, so 163 tests ran in every fast gate the plan said they did not (162 as of 8772c07d — see below). Every one of the 20 is a Swift Testing `@Suite`, and every one was added by the plan's very first commit (d55b0c1c, 2026-04-10), which declared 21 skips of which exactly one was an XCTest class; the two later commits added XCTest classes only and both worked. So the failure was total from day one and nothing regressed since. The harm was not the load. It was that **`Phase3ShadowReplayHarnessTests/benchmarkGate` failed every gate for four months** and was triaged each time as one more starvation flake, because nobody reads a test the plan says is not running — the 2026-08-13 gpdb log has it failing at **225.360 s against a 5,000 ms budget**, and the o89d log, one commit later, has it not starting at all because playhead-o89d `PerfGate`d it.

The 20 entries were **withdrawn, not re-implemented in source**, and the reason is measurement rather than taste. Scoped (`-only-testing:` over the 20 suites, 2026-08-13), in Swift Testing's own words: **`Test run with 163 tests in 20 suites passed after 25.503 seconds`** — and essentially all of it is one suite, `CombinedTuningReplayTests` at 25.50 s, against `FoundationModelClassifierTests` at **3.25 s for 107 tests** and every other suite at 2.2–4.3 s. None of the 163 appears in the 121-entry gate baseline, and the two triaged merge-gate logs of 2026-08-13 contain **zero** failures from them — the pre-o89d one contains exactly one, `benchmark gate`. Swept across **39 qualifying full-plan logs** (>11,000 started, non-selective, <100 lost verdicts, deduplicated by `(start count, failure set)`, parsed by `gate_baseline.parse_run`, walking `/private/tmp`, `$TMPDIR`, `/Users/dabrams/playhead`, `/Users/dabrams/.claude` and `/Users/dabrams/playhead-gate-artifacts`) the population's whole recorded failure history is **`benchmark gate` and one branch**:

- **`benchmark gate` failed 37, passed 0, and did not run 2** — the two are the post-o89d logs where the `PerfGate` holds. So it is not "32 of 40", it is **every single run in which it was reachable**, which is the stronger statement and the one the source comment already makes.
- **Five other names, all in two logs, and both logs are `.worktrees/mn5e`** — `mn5e-r2/fullgate.log` and `fullgate2.log`, the same branch in the same review round. `playhead-mn5e` is *the trust-scoring change* ("the trust counter counts EPISODES, and the auto rung opens only on rediff-grade evidence", 50dad4b5), and the names are `Sigmoid calibration…`, `Corpus aggregate quality…`, `Trust promotion speed…` (`CombinedTuningReplayTests`) plus `Full trust lifecycle…` (`TrustScoringLifecycleTests`). Two of the five are **pre-rename spellings**: 50dad4b5 renamed `Trust promotion speed: bonus=0.10 reaches auto mode within expected observations` → `…reaches manual fast, and auto not at all` and `Full trust lifecycle: shadow -> manual -> auto with observations` → `…the closed auto rung, then demotion`, which is why the two runs' failing sets differ — one is before that commit and one after.

**An earlier version of this paragraph read those two runs as "a ~5 % assertion flake … a NEW failure on roughly one merge gate in twenty", and that is the standing defect class again** — a quantity that names one thing (one branch, mid-flight, on tests it was deliberately rewriting) read as though it named another (a per-run flake rate over any future tree). It is not a flake and there is no rate: it is `the residual is the diff`, the case this file already documents under leave-one-out, and it is the population **doing its job** — the trust tests caught a semantic change on the branch that made it, and that branch then updated them. It also under-counted: four names in two suites, not three in one, and the two runs carried 101 and 88 failures, not "68–72". So the honest figure for the 163 really is **zero standing failures** once the already-`PerfGate`d benchmark is set aside, which strengthens the case for keeping them rather than weakening it. Their assertions are load-INDEPENDENT — the two "benchmark" suites assert recall and coverage deltas, never a clock; the only `Date()` reads are printed for eyeballing. So honouring a four-month-old skip would have removed 163 green, cheap, load-insensitive tests — including the detection-quality regression guards (`RealEpisodeBenchmarkTests` asserts six recall/coverage deltas at ≥ −0.05, of which **four bite and two cannot fail**: their baseline is 0.0 and both are non-negative rates — playhead-5do6) and the prompt-injection sanitization tests — from what is now the merge gate, in exchange for ~4 % of its wall clock. **The plan now says what it has actually done since April:** run everything except the twelve XCTest classes listed above. That is what the section heading always *meant*; it is not what it said, which claimed twenty — see the phase-close paragraph, corrected in the same round.

Two things that follow, so nobody re-derives them:

- **`playhead-06t7` is unblocked with the load UNCHANGED.** The fast plan's population is byte-identical before and after this change; the 163 were always running. Measure the load-sensitive population against today's plan — there is no reduction coming.
- **If you ever do need a Swift Testing suite out of a plan, the plan file cannot do it and `-skip-testing:` in `fast-gate.sh` must not.** A `-skip-testing:` argument sets `SELECTIVE=1`, which makes the gate skip the baseline diff **on every run** — it would silence the very mechanism the gate exists for. Gate it in SOURCE with an env-var trait: `PerfGate` (`PlayheadTests/Helpers/PerfGate.swift`) is the worked example, it works uniformly across XCTest and Swift Testing, and `benchmarkGate` is a live instance of it. Note the direction hazard before you copy it: `PerfGate` is opt-IN, and that shape already cost this repo six weeks of silently-unrun tests when the one pass that set the variable could not start.

`TestPlanSkipListCanaryTests` (in `PlayheadTests/Design/`) now fails the build's test phase if any plan's `skippedTests`/`selectedTests` names anything that is not a class reaching `XCTestCase` — transitively, since eight of the twelve honoured entries subclass `InterruptionCycleSuiteBase` rather than `XCTestCase` directly. It also pins the claim two paragraphs up that `PlayheadIntegrationTests` is a true superset, which nothing checked: a skip that existed only in the superset plan would leave those tests running in no plan at all.

The `PlayheadFastTests` plan is the default in Xcode (Cmd+U).

**Load-sensitive measurement tests (`scripts/perf-tests.sh`):** Latency/timing tests — `MainActorFreedomTests`, `PlayheadRuntimeLaunchPerfTests`, and the cancel-mid-decode scheduler tests — assert absolute wall-clock budgets that only hold on a quiescent CPU. The parallel FastTests suite (~7,900 tests) saturates the machine and makes them flake, so they are gated (`PerfGate`, opt-in via `PLAYHEAD_RUN_PERF=1`) to **skip** in FastTests/IntegrationTests and run **only** through the dedicated serial pass:
```bash
scripts/perf-tests.sh    # PlayheadPerfTests plan, parallelism off, measurement tests only
```
When adding a new load-sensitive test, gate it with `PerfGate` in the source **and** add it to the `MEASUREMENT_TESTS` list in the script. See playhead-zx0l.

**Run the pass after gating anything, and read its output — it was unrunnable for six weeks and nobody noticed (playhead-o89d).** The script's default destination was `iPhone 17 Pro, OS=27.0`, a device that does not exist on this box, so `xcodebuild` exited with `Unable to find a device matching the provided destination specifier` before running one test. That is not a typo-grade bug: this script is the ONLY place a `PerfGate`d test runs, so while it could not start, "gated" and "deleted" were the same thing for every test admitted since playhead-zx0l. Fixed to match `fast-gate.sh` (`name=iPhone 17`).

**READ BOTH FORMATS HERE TOO — `Test run with 13 tests in 12 suites passed` is the SWIFT TESTING summary, not the pass's verdict (playhead-o89d review).** This pass runs XCTest suites as well, and they report in the other format entirely; a reader who stops at the Swift Testing line sees a green pass while `** TEST FAILED **` sits at the bottom. Measured over two consecutive runs on a quiet box, 2026-08-13: the 13 Swift Testing tests passed both times, and **two XCTest tests failed both times** — `PlayheadRuntimeLaunchPerfTests.testInitFitsLaunchBudget` (init median 407 ms against a 250 ms budget) and `PlayheadRuntimeMainActorFreedomTests.testMainActorIsNotHeldDuringRuntimeInit`, which on one run took the **test host down** with `Fatal error: Index out of range`, after which xcodebuild restarted and printed `Test Suite 'PlayheadRuntimeMainActorFreedomTests' passed … Executed 0 tests`. Both are `PerfGate`d, so both had been unrun for six weeks and are newly visible rather than new — filed as **playhead-1och**, which triaged them and is summarised in the next paragraph. `AdmissionControllerTests` (`elapsed < 0.1`, playhead-0vju) tripped by 617 µs on the run that first fixed the destination and **PASSED 2/2 at review** at 0.022 s, so it is the marginal one, not the standing failure this pass is red for.

**THE PASS WAS RED FOR ONE PRODUCT DEFECT, NOT TWO FLAKY TESTS — AND IT IS GREEN NOW (playhead-1och diagnosed it, playhead-xul6 fixed it, 2026-08-13).** Both XCTest failures were `CapabilitiesService()` on the main actor; the fix took the `SystemLanguageModel` read out of the synchronous launch snapshot entirely. Measured over three consecutive serial passes on the fixed tree: `MainActorFreedom` **max = 6.5 / 3.4 / 2.9 ms** against the unchanged 100 ms budget (it had failed 7 of 7), and `LaunchPerf` **median = 2.0 / 2.3 / 2.1 ms** against the unchanged 250 ms budget. **No threshold was moved.** The paragraphs below are kept as the diagnosis record — read them in the past tense. The crash is gone: the count guard in `MainActorFreedomTests` was a non-fatal `XCTAssertGreaterThan` and the next line subscripted the empty post-warmup array, so a probe that had correctly detected it measured nothing walked off the end of its own array, killed the host, and let the restarted host print `Executed 0 tests`. It is a `guard … else { XCTFail(…); return }` now, and each hop records WHEN it was submitted as well as how long it took — a bare sample count cannot distinguish a starved probe from a held main actor, and the timeline can.

Both remaining failures are **`CapabilitiesService()`, one line of `PlayheadRuntime.init`, holding the main actor 0.47–2.13 s** (filed as **playhead-xul6**, P1). Per-section timing of the init showed `CapabilitiesService` at 473–2130 ms while *every other section of a 1,086-statement init is single-digit to tens of ms*; the probe agrees from the other side (`median=0.0ms max=557.7ms samples=304`, one hop spanning the whole init body and 304 completing in ~0 ms). It is `CapabilitiesService.init` → `captureSnapshot()` → `checkFoundationModelsState()` → `SystemLanguageModel.default` / `.contextSize`, and the repeated **2003 ms** readings are a 2-second timeout, not work. Note where it hid: `PlayheadRuntimeInitLaunchPathSourceCanaryTests` bans `SystemLanguageModel(` **in init's own body text** and PASSED — the construction lived one call deeper, in a service whose initialiser looks free at the call site. Same shape as every other canary this repo has out-spelled, plus a second blindness nobody had noticed: the pattern requires a `(`, so `let model = SystemLanguageModel.default` written **directly in init's body** would also have passed it. **playhead-xul6 added the transitive rail** — `testInitSynchronousCallGraphIsFreeOfFoundationModels` walks the call graph (`SwiftSourceCallGraph`), following only edges that run SYNCHRONOUSLY from init and treating a closure LITERAL as the value it is, so `Task { }` and `PermissiveClassifierBox { }` are invisible with **no allowlist** to grow. It indexes 470 files / ~12 MB in ~1.0 s. Re-introducing the defect at its original depth makes it report both paths by name while the text canary above still passes — that is the mutation proof, and the reason the two tests are not redundant. **Read a green result as "no CALL path", not as "no touch" (review round 1).** Two idiomatic shapes get past it and are named as limits L-6 and L-7 in `SwiftSourceCallGraph`: a member ACCESS that runs a getter (`_ = Self.someComputedProperty` — `callees` emits an edge only for `name(`, and a computed property's body is never indexed at all) and a **type-scope property initialiser** (`private let x = Foo.expensive()`, which runs inside every `init` of that type — i.e. exactly where this bead's defect lived). Both were re-introduced into `CapabilitiesService` as real main-actor `SystemLanguageModel` reads at review and the canary passed on both. They are documented rather than closed because both cures cost the property that makes the walker worth reading — that every path it reports is a real one: scanning type-scope text reads TYPE ANNOTATIONS as executed code (`private let model: SystemLanguageModel` in `PermissiveAdClassifier`, `private let session: LanguageModelSession` in `FoundationModelClassifier`), and telling an annotation from an expression is parsing rather than scanning.

**Which of the two you see red depends on the run, and that is the assertions' shapes, not noise.** `MainActorFreedomTests` asserts on the MAX hop and has failed 7 of 7 observations — its mildest reading is `max=138.0ms init body=138.0ms`, still over the 100 ms budget. `LaunchPerfTests` asserts on the MEDIAN of 5 constructions against 250 ms, so it only trips when at least three of the five hit the probe: 2 PASS / 4 FAIL observed, `median=170.1ms samples=[710.4, 478.6, 168.5, 170.1, 108.3]` on one of the passes. A green `LaunchPerf` is not evidence the hold is gone; read the `[MainActorFreedom]` line, which now prints the init-body duration and where the worst hop sits relative to it. Post-fix the two agree: `max=6.5/3.4/2.9 ms` with the init body itself at 6.5/3.3/2.9 ms, i.e. the worst hop IS the whole init body and the whole init body is now under 7 ms.

**And the cost of gating them is now a measured number rather than a worry.** The regression is bracketed to one commit: `b0a6cc58` (2026-07-04 19:04) added `let contextSize = model.contextSize` to that snapshot, and its direct parent `8e610c74` (2026-07-04 06:15) — the commit that made both tests `PerfGate` opt-in and shipped `perf-tests.sh` pointed at the destination that does not exist — records in its own message `Perf pass: 7/7 pass (MainActorFreedom max=11ms vs 100ms budget)`. **11 ms before, 138–2008 ms after, thirteen hours apart.** The two tests that exist precisely to catch a synchronous FoundationModels construction on the launch path were made opt-in hours before the thing they guard against landed, into a pass that could not start, and it held for six weeks.

**The 250 ms budget was deliberately NOT widened, and the measurement is why.** On the constructions that miss the probe, init reads 57.0 / 60.7 / 62.8 / 67.6 ms — the budget is met with 4× headroom and matches the "~50–80 ms on simulator" its own comment claims. The floor is not the problem; an intermittent 2 s block is. Widening to 2.5 s would have deleted the only two tests that can see it. **Everything above is the SIMULATOR** (FoundationModels is unavailable there, which is what the timeout is waiting out); **nobody has measured a device since this call landed, and that is still true after the fix** — playhead-jndk's precedent is a multi-minute device freeze from the same API, so do not assume the device is fine. What playhead-xul6 changes is that the cost, whatever it is, is no longer paid on the main actor: the read now runs on the capabilities actor's own executor (an `await`, i.e. a suspension) or, for `contextSize`, only when Settings asks. The fix is correct for either device answer; the device NUMBER remains unmeasured.

**What earns a place here, measured rather than argued (playhead-o89d).** Not "asserts a wall-clock budget" — 774 tests are subject to some absolute deadline and the great majority are fine. The sub-property that does not rotate with the box's load is **the wall-clock quantity IS the assertion**: a clock read plus an expectation comparing a derived value to a constant. Enumerated mechanically that is 16 tests, of which 11 are CPU-bound and measured to PASS under the full plan. **The "exactly 2 of them are load-sensitive" figure was one short**, found at review: `ExpiredWindowAttemptAccountingTests.settleWaitDoesNotLie` reads `ContinuousClock.now` and asserts the elapsed span is `< .seconds(2)` to separate "returns AT ONCE" from "burned the whole 5 s budget" — the same shape as the 20 s / 60 s bound that WAS gated, with a tighter margin, in the same file, and it is in the committed baseline as an `assertion` failure. It is **left un-gated deliberately**: moving it now would make its baseline entry read `DID NOT RUN` and force a second `--accept-baseline` in one branch. Filed as **playhead-3f79**, and deliberately sequenced BEHIND **playhead-1och**: that pass was red on two XCTest tests, so a test moved here would have landed where its result is not read — the same "gated and deleted were the same thing" defect this section exists to record, one turn later. **playhead-xul6 cleared that block**: the serial pass is green as of 2026-08-13, so 3f79 and playhead-06t7 can proceed. Everything else on the load-sensitive list — **269 names observed over 38 full-plan runs, of which 168 recur and cover ~97 % of any single run** (see the baseline section) — is a **coverage decision, not a measurement one**; see playhead-o89d before adding any of it here. Say which population you mean when you say it has been measured: **184 of the 269 have a recorded isolated result and 183 pass 5/5; the other 85 have none**, and 66 of those are singletons. **And 93 of those 269 have only ever failed an EXPECTATION, never a time limit** — do not read the union as a PerfGate shortlist; the kind split in the baseline section is what bounds it.


## Disk Hygiene

Each bead worktree runs `xcodebuild -derivedDataPath .derivedData`, producing ~2 GB of cache. Cleanup must be deliberate — a missed step leaves orphan gigabytes. Real paths are `.worktrees/<slug>/.derivedData` (depth 3, camelCase).

### Canonical bead-close sequence

Run from the repo root after PR merge. Substitute `<slug>` (e.g. `bd-r835`) and `<branch>` (e.g. `bead/playhead-r835`).

```bash
cd /Users/dabrams/playhead && git checkout main
git pull --ff-only || git pull --rebase   # see the divergence trap below
BR=<branch>; WT=/Users/dabrams/playhead/.worktrees/<slug>; PR=<pr-number>
# Merged-ness. PR state is the AUTHORITY on this repo because PRs are SQUASH-merged.
[ "$(gh pr view "$PR" --json state -q .state)" = MERGED ] || { echo "NOT MERGED — abort"; exit 1; }
[ -z "$(git -C "$WT" status --porcelain)" ] || { echo "DIRTY — stash or commit first"; exit 1; }
bd close playhead-<slug>
git worktree remove "$WT"
[ -d "$WT/.derivedData" ] && rm -rf "$WT/.derivedData" && echo "removed $WT/.derivedData"
git worktree prune -v
git push origin --delete "$BR"
git branch -D "$BR"   # -D is CORRECT after a squash merge; see below
```

**The squash-merge trap (hit on 2026-07-28 closing playhead-8m2w).** This repo squash-merges PRs, so
the branch tip is **never** an ancestor of `main` — the squash is a brand-new commit with a different
hash and no parent link to the branch. Two consequences that make the older sequence unusable:

- `git merge-base --is-ancestor "$BR" origin/main` **always reports NOT MERGED** after a squash, for
  branches that are fully merged. Use the PR's state instead; that is the only thing that actually
  knows a squash happened.
- `git branch -d` **always refuses** for the same reason, and no amount of deleting the remote first
  will help — the refusal is not about the remote lagging, it is about the commits genuinely not
  being reachable from `main`. So `-D` is correct **here**, and the PR-state check above is what
  replaces `-d`'s refusal as the safety net. Do not drop that check: without it `-D` is a real
  footgun. (If a future PR is merged with a true merge commit rather than squashed, `-d` will
  succeed on its own — prefer it when it does.)

**The diverged-main trap (same close).** `git pull --ff-only` aborts with "Not possible to
fast-forward" whenever local `main` carries a commit that was never pushed — easy to accumulate,
since main-branch commits (docs, scripts, CLAUDE.md edits) do not go through a bead worktree. Inspect
with `git log --oneline origin/main..main` and `main..origin/main` before doing anything. If the
divergence is your own unpushed commits and the tree is clean, `git pull --rebase` replays them on
top of the merge; then push. Do **not** reach for `--force` or reset — check what diverged first.

Two further traps this sequence exists to avoid (both hit on 2026-07-26):

- **Don't test merged-ness with `git branch --merged main | grep -qx "  <branch>"`.** A branch checked
  out in a linked worktree is listed as `+ bead/foo`, not `  bead/foo` — and at close time every bead
  branch is in a worktree, so that guard reports "NOT MERGED" for branches that are merged, every time.
  `git merge-base --is-ancestor` asks the real question and is indifferent to the listing prefix.
  Compare against `origin/main`, not `main`, so you also prove the merge was actually pushed.
- **`git branch -d` refuses while the remote branch still predates the merge**, with a confusing "not
  fully merged" error even though it is merged to HEAD. Delete the remote branch first and `-d`
  succeeds. This is a *different* cause from the squash-merge refusal above, and the remedies differ:
  here `-d` genuinely succeeds once the remote is gone, so use it; after a squash it never will.
  Either way the PR-state check is what authorises the delete — never let `-D` stand alone.

### Safety rails

- Before `rm -rf`: path must start with `/Users/dabrams/playhead/.worktrees/`, `/private/tmp/playhead-`, or `$TMPDIR/Deleting-` (CoreSimulator's own trash — see below), and must NOT appear in `git worktree list --porcelain`.
- Never pass `--force` to `git worktree remove` without explicit user approval — the refusal is the safety net.
- Echo what was removed so the transcript audits the session.

### The gate REFUSES to start below 13.5 GiB free (playhead-3nfa)

**You no longer have to remember to check.** `scripts/fast-gate.sh` runs `scripts/disk_preflight.py` before lint, before the xcodegen bootstrap, before xcodebuild — and if the volume is short it prints a loud refusal naming the remedy and **exits 28** (POSIX `ENOSPC`, the error this would otherwise have hidden). Nothing is deleted; the refusal only *reports* what `scripts/disk-cleanup.sh` could reclaim.

```bash
scripts/fast-gate.sh                  # refuses under 13.5 GiB, exit 28
scripts/fast-gate.sh --reclaim-disk   # run the cleaner ONCE, re-check, then proceed or refuse
PLAYHEAD_DISK_MIN_GIB=9 scripts/fast-gate.sh    # a different threshold, deliberately
```

This exists because the failure mode is not a failure. A gate that runs out of room **wedges**: xcodebuild stays alive with zero output and never exits, having failed to write its result bundle. No POSIX 28, no non-zero exit, nothing in the log — indistinguishable from a slow test run. This box hit 100 % capacity four times on 2026-08-01 and every one was caught by somebody happening to look.

**Where 13.5 comes from — measured 2026-08-02, two full runs to a terminal verdict**, sampling `df -k` on `/System/Volumes/Data` every 5 s with `scripts/gate-disk-sample.sh`. The quantity is the **drawdown** (free at start minus the minimum during the run), because a gate is a transient event and the end-state delta understates it by more than half:

| run | conditions | start | min | end | drawdown |
|---|---|---|---|---|---|
| 1 | fresh worktree, cache from empty, sim already at 7.88 GiB | 15.07 | **3.58** | 11.39 | **11.49 GiB** |
| 2 | cache warm, sim freshly erased to ~0 | 19.08 | **6.93** | 8.86 | **12.15 GiB** |

`13.5 = 12.15 (worst observed) + 1.35`, and the margin is measured too: the largest fall inside a single 5 s sampling gap was 1.27 GiB, so 1.35 covers what the sampler cannot see. **The old "~8 GB" figure was well under what a full plan actually draws** — do not restore it without re-measuring.

Three things that will bite whoever re-measures:

- **A cold/warm split was tried and the data rejected it.** The obvious refinement is a cheaper threshold when `.derivedData/Build` exists, since a fresh worktree must create ~2.8 GiB of cache. Run 2 was the warm one and drew down **more**. Simulator state moves by ~8 GiB depending on whether the destination was recently erased, and it swamps the cache saving. Two variables, two runs, neither isolated.
- **`df` Avail on APFS lags on the way back up.** Run 2 read 8.86 GiB free the moment it exited and 16.00 GiB two minutes later. The *minimum* is still the number to use — it is the same metric by which this box "hit 100 % capacity" — but never compute what a run keeps from a reading taken at exit.
- **THE 13.5 GiB DOES NOT COVER SWAP, AND SWAP IS WHAT ACTUALLY FILLS THE VOLUME (playhead-3rql, 2026-08-20).** The two runs above were measured on a box that was not short of memory, so their drawdown is cache and build products. On a run that exceeds RAM the kernel pages, the swapfile lives on this same volume, and the arithmetic changes completely: `ACCEPT-2` started with **31.75 GiB free**, passed the preflight comfortably, and ended with **0.48 GiB** — because `vm.swapusage used` had grown to **29.97 GiB**. Most of that is not the test phase (steady at ~8 GiB of swap and ~17 GiB free) but the AFTERMATH: once the test host died, xcodebuild spent minutes collecting diagnostics from a 200-process simulator and swap went 13 → 30 GiB. That is what converts a recoverable host restart into `Killed: 9` with no result bundle at all. So a preflight figure measured on a healthy run is not a bound on a sick one, and the honest reading of a 137 is to look at `disk free min` on the memory series (`scripts/gate-memory-sample.py` records both) before concluding anything about RAM.
- **The threshold applies to selective runs too** (`-only-testing:`, i.e. how `mutation-battery.sh` drives the gate). A selective run costs less, but how much less was not measured, so the conservative number governs. If that bites, `--reclaim-disk` or `PLAYHEAD_DISK_MIN_GIB` are the answers.

`PLAYHEAD_SKIP_DISK_PREFLIGHT=1` bypasses the check entirely. It is deliberately **not** printed in the refusal, for the same reason as `PLAYHEAD_SKIP_BASELINE`: an override quoted in the failure message stops being an override and becomes the documented workaround.

A wedged run is also the case the baseline check is built to survive: it printed `CANNOT EVALUATE — the log is incomplete` and passed xcodebuild's own exit through (143), rather than reading ~9,900 unfinished tests as a clean sweep.

### `simctl erase` can report success and free nothing (playhead-cgka)

**Measured 2026-08-02, and it is why this box kept arriving at ~8 GiB of headroom on a 228 GB disk.** `simctl erase` does not delete a device's data in place: CoreSimulator *moves* it to `$TMPDIR/Deleting-<uuid>/` and reaps it asynchronously. If that reap hits a directory it cannot read, it dies and the bytes stay forever — the erase still reports success, and `du` on the device directory still shows the space as freed.

The test suite creates exactly such a directory by design: `DownloadManagerTests` chmods a `complete/` directory to `0o300` to prove a permission failure is handled, and restores it in a `defer` that an abnormal exit skips. Seven orphaned devices had accumulated **15 GiB**, every one of them stuck on a single unreadable `PlayheadTestScratch/…/complete`. Clearing them took the volume from 9.0 GiB to 25.4 GiB free.

```bash
du -sk "$TMPDIR"Deleting-* 2>/dev/null | sort -rn      # what is stranded
chmod -R u+rwx "$TMPDIR"Deleting-*                     # u+w is NOT enough
rm -rf "$TMPDIR"Deleting-*
```

**`chmod -R u+w` does not work here and looks like it should.** `0o300` is `-wx------`: write is already granted; READ is what an enumerator needs to walk the directory, so `rm -rf` fails with EACCES on precisely the directories that need repairing. It is `u+rwx` or nothing. (The suite's own wipe now goes through `TestScratchReaper.forceRemove`, which repairs permissions before giving up, so new instances should not accumulate — but devices stranded before that landed are still out there.)

Check `$TMPDIR/Deleting-*` **before** concluding the box is out of disk. It is the largest single reservoir on this machine and it is invisible to every `du` of the worktree, the derivedData or the simulator.

### Orphan sweep script

`scripts/disk-cleanup.sh` runs weekly via cron. Safe to run manually:

```bash
scripts/disk-cleanup.sh --dry-run   # preview
scripts/disk-cleanup.sh             # actually clean
```

It removes `.worktrees/<slug>/.derivedData` whose worktree is no longer registered, and stale `/private/tmp/playhead-*` dirs older than 3 days that are not active worktrees. Logs to `.logs/disk-cleanup.log`.

**This is the only cleaner in the repo — extend it, don't write a second one.** `scripts/disk_preflight.py` measures and refuses but delegates every removal here, so there is one set of safety rails rather than two that drift. playhead-3nfa added two more classes and one guard:

- **Superseded `.xcresult` bundles** (~93 MB each, measured). The most recent per worktree is always kept — it is what you open after a failure — and only strictly older ones go. `.derivedData/Build` is untouched.
- **`$TMPDIR/Deleting-*`**, the stranded CoreSimulator trash. Every removal `chmod -R u+rwx` first; see the `simctl erase` section above for why `u+w` looks right and does nothing.
- **A live-build guard.** Working directories of running `xcodebuild`/`swift-frontend` are resolved with `lsof -a -p <pid> -d cwd` and anything under them is skipped, so a sweep during a long run cannot destroy it. Resolution uses **`pgrep -x`, never `pgrep -f`** — `-f` matches the full argv and self-matches any shell whose command line merely contains the string, which on 2026-08-01 killed a healthy gate and then raised a phantom second-build alarm.

Verify with `python3 -m unittest scripts.tests.test_disk_preflight` (47 tests, under a second, no build) and `scripts/mutation-battery-disk-preflight.py` (P series, 16 rails + a vacuity control).
