import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("Playhead")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

#Preview {
    ContentView()
}
